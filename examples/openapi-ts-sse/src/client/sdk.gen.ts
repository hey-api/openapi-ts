// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type {
  GetStockHistoryData,
  GetStockHistoryResponses,
  WatchSelectedStocksData,
  WatchSelectedStocksResponses,
  WatchSingleStockData,
  WatchSingleStockErrors,
  WatchSingleStockResponses,
  WatchStockPricesData,
  WatchStockPricesResponses,
} from './types.gen';

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
> = Options2<TData, ThrowOnError> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: Record<string, unknown>;
};

/**
 * Watch stock prices
 *
 * Stream real-time stock price updates.
 */
export const watchStockPrices = <ThrowOnError extends boolean = false>(
  options?: Options<WatchStockPricesData, ThrowOnError>,
) =>
  (options?.client ?? client).sse.get<WatchStockPricesResponses, unknown, ThrowOnError>({
    url: '/stock/watch',
    ...options,
  });

/**
 * Watch selected stocks
 *
 * Stream real-time price updates for selected stock symbols.
 */
export const watchSelectedStocks = <ThrowOnError extends boolean = false>(
  options: Options<WatchSelectedStocksData, ThrowOnError>,
) =>
  (options.client ?? client).sse.post<WatchSelectedStocksResponses, unknown, ThrowOnError>({
    url: '/stock/watchlist',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * Watch a single stock
 *
 * Stream real-time price updates for a specific stock symbol.
 */
export const watchSingleStock = <ThrowOnError extends boolean = false>(
  options: Options<WatchSingleStockData, ThrowOnError>,
) =>
  (options.client ?? client).sse.get<
    WatchSingleStockResponses,
    WatchSingleStockErrors,
    ThrowOnError
  >({ url: '/stock/{symbol}/watch', ...options });

/**
 * Get stock history
 *
 * Retrieve historical stock price data.
 */
export const getStockHistory = <ThrowOnError extends boolean = false>(
  options?: Options<GetStockHistoryData, ThrowOnError>,
) =>
  (options?.client ?? client).get<GetStockHistoryResponses, unknown, ThrowOnError>({
    url: '/stock/history',
    ...options,
  });
