/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export interface StainlessConfig {
  edition?: '2026-05-06' | '2026-02-23' | '2026-01-30' | '2025-10-10' | '2025-10-08' | string;
  /**
   * How the organization is represented in the SDKs, such as `name` and `github_org`.
   */
  organization: {
    /**
     * Name of your organization or company, in lowercase. Used in documentation.
     * The generated client name defaults to this. To customize the capitalization,
     * use `custom_casings`.
     */
    name: string;
    /**
     * Link to your API documentation.
     */
    docs?: string;
    /**
     * Contact email for bug reports, questions, and support requests.
     */
    contact?: string;
    /**
     * Security email address for reporting vulnerabilities. Will be mentioned in the generated
     * SECURITY.md. Defaults to the contact email.
     */
    security_contact?: string;
    /**
     * Link to a page covering your security policy. Will be mentioned in the generated SECURITY.md.
     */
    security_policy_url?: string;
    /**
     * Terms for your security policy. Will be added to the generated SECURITY.md.
     *
     * Stainless always generates a default security policy covering SDKs security issues.
     * This field is for additional terms you want to add and will be included in a section
     * '[Your org name] Terms and Policies'.
     */
    security_policy_terms?: string;
    /**
     * Name of the GitHub organization that the production SDKs live in.
     */
    github_org?: string;
    /**
     * Whether or not to enable uploading the input openapi spec and publicly exposing it in the SDK.
     *
     * This allows running tests in the SDKs against a mock server, and if disabled will disable tests from automatically running in CI.
     */
    upload_spec?: boolean;
    [k: string]: unknown;
  };
  /**
   * Customization for each language's repo name, package manager name, etc.
   */
  targets?: {
    node?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'node.2025-10-08' | string;
      /**
       * The name of the package in the import such as `import … from "<package_name>"` and the name in package.json.
       */
      package_name: string;
      publish: {
        npm: boolean;
        jsr?:
          | boolean
          | {
              /**
               * GitHub environment to run the publish workflow in.
               */
              release_environment?: string;
              /**
               * The name of the package in the jsr repository.
               */
              package_name: string;
              /**
               * By default, publishing to jsr uses GitHub Actions OIDC tokens. This should be set to true if you want to publish using an access tokens instead.
               */
              use_access_token?: boolean;
              [k: string]: unknown;
            };
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        /**
         * Enable MCP server generation.
         *
         * If enabled, will create a sub-directory in the SDK under `packages/mcp-server`.
         * For more information: https://modelcontextprotocol.io/introduction
         */
        mcp_server?:
          | boolean
          | {
              /**
               * The name of the mcp package. Defaults to `<typescript-package>-mcp`
               */
              package_name?: string;
              /**
               * Generate a tool for the MCP server to take action with your SDK. This allows agents to perform tasks using your SDK by writing TypeScript code, which is executed securely in a sandbox.
               */
              enable_code_tool?: boolean;
              /**
               * Generate a tool for the MCP server to look up documentation for your Stainless SDK.
               *
               * This requires you to expose the SDK docs search API for your project.
               */
              enable_docs_tool?: boolean;
              /**
               * @deprecated
               * Deprecated: The get_agentic_help tool has been removed from MCP server generation.
               */
              enable_get_agentic_help_tool?: boolean;
              /**
               * Generates a cloudflare worker that you can deploy as a Remote MCP Server in your own
               * Cloudflare account. This is useful if you want to easily set up an MCP server that handles
               * OAuth for use in web clients like claude.ai.
               *
               * See https://www.stainless.com/docs/guides/generate-an-mcp-server#deploy-a-remote-mcp-server for more information.
               */
              generate_cloudflare_worker?: boolean;
              /**
               * Instructions for the MCP client on to use the MCP server. This is sent as a part of MCP server initialization, and enters the client's context.
               * Defaults to no instructions being sent.
               *
               * See https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle#initialization for more information.
               */
              instructions?: string;
              /**
               * OAuth protected resource metadata to expose when running with Streamable HTTP transport.
               */
              oauth_resource_metadata?: {
                /**
                 * List of OAuth authorization server URLs to advertise to MCP clients
                 */
                authorization_servers: string[];
                /**
                 * List of scopes to advertise to MCP clients
                 */
                scopes_supported?: string[];
                [k: string]: unknown;
              };
              publish?: {
                /**
                 * Docker image publishing configuration. Set to false to disable, string for image name, or object for full config.
                 */
                docker?:
                  | false
                  | string
                  | {
                      /**
                       * GitHub environment to run the publish workflow in.
                       */
                      release_environment?: string;
                      /**
                       * The Docker image name, e.g. "myorg/my-api-mcp"
                       */
                      image_name: string;
                      /**
                       * The Docker registry to publish to
                       */
                      registry?: string;
                      [k: string]: unknown;
                    };
                github?: {
                  /**
                   * Additional files to upload to GitHub Releases after publishing.
                   */
                  release_assets: (
                    | string
                    | {
                        /**
                         * Name for this release asset in GitHub Releases.
                         */
                        name?: string;
                        /**
                         * Path to the release asset file, relative to the repository root.
                         */
                        path: string;
                        [k: string]: unknown;
                      }
                  )[];
                  [k: string]: unknown;
                };
                /**
                 * Release-please configuration. Controls the generated release-please workflow and config files.
                 */
                release?: {
                  /**
                   * Branch that release-please targets for release PRs and changelog generation.
                   */
                  branch?: string;
                  /**
                   * Whether to mark GitHub Releases as pre-releases.
                   */
                  prerelease?: boolean;
                  [k: string]: unknown;
                };
                [k: string]: unknown;
              };
              /**
               * @deprecated
               * Deprecated: MCP server hosting is now controlled via the toggle in the Stainless dashboard.
               */
              host?: {
                /**
                 * Whether to publish the package to the Stainless registry and host via Stainless.
                 */
                stainless: boolean;
                [k: string]: unknown;
              };
              [k: string]: unknown;
            };
        browser?: {
          /**
           * If set to `dangerous_allow` then the SDK will only be usable in the browser when passing an explicit flag.
           */
          state?: 'allow' | 'disallow' | 'dangerous_allow';
          /**
           * The error message to throw.
           */
          message?: string;
          /**
           * Headers to use when making requests from a web browser.
           */
          headers?: {
            [k: string]: string;
          };
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    typescript?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'typescript.2025-10-10' | 'typescript.2025-10-08' | string;
      /**
       * The name of the package in the import such as `import … from "<package_name>"` and the name in package.json.
       */
      package_name: string;
      publish: {
        npm:
          | boolean
          | {
              /**
               * By default, publishing to npm uses Access Tokens. You can instead opt to use Github Actions OIDC tokens.
               */
              auth_method?: 'access-token' | 'oidc';
              /**
               * GitHub environment to run the publish workflow in.
               */
              release_environment?: string;
              [k: string]: unknown;
            };
        jsr?:
          | boolean
          | {
              /**
               * GitHub environment to run the publish workflow in.
               */
              release_environment?: string;
              /**
               * The name of the package in the jsr repository.
               */
              package_name: string;
              /**
               * By default, publishing to jsr uses GitHub Actions OIDC tokens. This should be set to true if you want to publish using an access tokens instead.
               */
              use_access_token?: boolean;
              [k: string]: unknown;
            };
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        /**
         * Enable MCP server generation.
         *
         * If enabled, will create a sub-directory in the SDK under `packages/mcp-server`.
         * For more information: https://modelcontextprotocol.io/introduction
         */
        mcp_server?:
          | boolean
          | {
              /**
               * The name of the mcp package. Defaults to `<typescript-package>-mcp`
               */
              package_name?: string;
              /**
               * Generate a tool for the MCP server to take action with your SDK. This allows agents to perform tasks using your SDK by writing TypeScript code, which is executed securely in a sandbox.
               */
              enable_code_tool?: boolean;
              /**
               * Generate a tool for the MCP server to look up documentation for your Stainless SDK.
               *
               * This requires you to expose the SDK docs search API for your project.
               */
              enable_docs_tool?: boolean;
              /**
               * @deprecated
               * Deprecated: The get_agentic_help tool has been removed from MCP server generation.
               */
              enable_get_agentic_help_tool?: boolean;
              /**
               * Generates a cloudflare worker that you can deploy as a Remote MCP Server in your own
               * Cloudflare account. This is useful if you want to easily set up an MCP server that handles
               * OAuth for use in web clients like claude.ai.
               *
               * See https://www.stainless.com/docs/guides/generate-an-mcp-server#deploy-a-remote-mcp-server for more information.
               */
              generate_cloudflare_worker?: boolean;
              /**
               * Instructions for the MCP client on to use the MCP server. This is sent as a part of MCP server initialization, and enters the client's context.
               * Defaults to no instructions being sent.
               *
               * See https://modelcontextprotocol.io/specification/2025-06-18/basic/lifecycle#initialization for more information.
               */
              instructions?: string;
              /**
               * OAuth protected resource metadata to expose when running with Streamable HTTP transport.
               */
              oauth_resource_metadata?: {
                /**
                 * List of OAuth authorization server URLs to advertise to MCP clients
                 */
                authorization_servers: string[];
                /**
                 * List of scopes to advertise to MCP clients
                 */
                scopes_supported?: string[];
                [k: string]: unknown;
              };
              publish?: {
                /**
                 * Docker image publishing configuration. Set to false to disable, string for image name, or object for full config.
                 */
                docker?:
                  | false
                  | string
                  | {
                      /**
                       * GitHub environment to run the publish workflow in.
                       */
                      release_environment?: string;
                      /**
                       * The Docker image name, e.g. "myorg/my-api-mcp"
                       */
                      image_name: string;
                      /**
                       * The Docker registry to publish to
                       */
                      registry?: string;
                      [k: string]: unknown;
                    };
                github?: {
                  /**
                   * Additional files to upload to GitHub Releases after publishing.
                   */
                  release_assets: (
                    | string
                    | {
                        /**
                         * Name for this release asset in GitHub Releases.
                         */
                        name?: string;
                        /**
                         * Path to the release asset file, relative to the repository root.
                         */
                        path: string;
                        [k: string]: unknown;
                      }
                  )[];
                  [k: string]: unknown;
                };
                /**
                 * Release-please configuration. Controls the generated release-please workflow and config files.
                 */
                release?: {
                  /**
                   * Branch that release-please targets for release PRs and changelog generation.
                   */
                  branch?: string;
                  /**
                   * Whether to mark GitHub Releases as pre-releases.
                   */
                  prerelease?: boolean;
                  [k: string]: unknown;
                };
                [k: string]: unknown;
              };
              /**
               * @deprecated
               * Deprecated: MCP server hosting is now controlled via the toggle in the Stainless dashboard.
               */
              host?: {
                /**
                 * Whether to publish the package to the Stainless registry and host via Stainless.
                 */
                stainless: boolean;
                [k: string]: unknown;
              };
              [k: string]: unknown;
            };
        browser?: {
          /**
           * If set to `dangerous_allow` then the SDK will only be usable in the browser when passing an explicit flag.
           */
          state?: 'allow' | 'disallow' | 'dangerous_allow';
          /**
           * The error message to throw.
           */
          message?: string;
          /**
           * Headers to use when making requests from a web browser.
           */
          headers?: {
            [k: string]: string;
          };
          [k: string]: unknown;
        };
        /**
         * If you're migrating from a `node` target to `typescript` you can specify 2 version strings that will be used in the migration guides and scripts.
         *
         * The versions will be used verbatim, such as in "Run migrations from your-sdk v{previous_version} to v{migrated_version}".
         *
         * If your SDK is post-1.0, we recommend to only include the major version, such as `previous_version: "2", migrated_version: "3"`.
         */
        node_migration?: {
          previous_version: string;
          migrated_version: string;
          [k: string]: unknown;
        };
        /**
         * Allows your users to include a subset of the SDK's resources, resulting in smaller bundles.
         */
        enable_treeshaking?: boolean;
        /**
         * The package manager used to install dependencies and manage project builds.
         */
        package_manager?: 'pnpm' | 'yarn';
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    python?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'python.2025-11-20' | 'python.2025-10-08' | string;
      /**
       * The name of the import, e.g. `from <package_name> import …`
       */
      package_name: string;
      /**
       * The Python project name, e.g. `pip install <project_name>`
       */
      project_name?: string;
      publish: {
        pypi:
          | boolean
          | {
              /**
               * GitHub environment to run the publish workflow in.
               */
              release_environment?: string;
              /**
               * Configure the authentication strategy for publishing to PyPi. Defaults to `api-token`, specify `oidc` to enable GitHub Actions OIDC support.
               */
              auth_method?: 'api-token' | 'oidc';
              /**
               * The name of the package in pypi, e.g. `pip install <package_name>`
               */
              package_name?: string;
              [k: string]: unknown;
            };
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        /**
         * In the python.2025-11-20 edition we switched the default package manger from Rye (https://rye.astral.sh) to uv (https://docs.astral.sh/uv/).  This property allows you to control if uv is used regardless of edition.
         */
        use_uv?: boolean;
        /**
         * When enabled, codegen uses a custom `EagerIterable` type instead of a plain `Iterable`.
         *
         * By default, Pydantic consumes `Iterable` fields lazily and does not validate items upfront, which can cause
         * data to be silently lost on repeated `model_dump()` calls[0]. `EagerIterable` eagerly consumes the input,
         * validates all items, and preserves the original container type at runtime (e.g. a `set` stays a `set`).
         * Serialization (e.g. `model_dump()`) always produces a `list`.
         *
         * [0]: https://github.com/pydantic/pydantic/issues/9541
         */
        use_eager_iterables?: boolean;
        /**
         * Customise the required python range for users. Useful if you have custom dependencies with different version ranges.
         */
        python_version_range?: string;
        /**
         * Change the name of the dependency group for websocket support, e.g. pip install apiclient[websocket] to pip install apiclient[my_websocket_api].
         */
        websockets_extra_name?: string;
        /**
         * Change the name of the dependency group for standard webhooks support, e.g. `pip install apiclient[webhooks]` to `pip install apiclient[my_webhooks_name]`.
         */
        standardwebhooks_extra_name?: string;
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    java?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'java.2025-10-08' | string;
      /**
       * The reverse domain to generate the SDK under, e.g. if `reverse_domain` is "com.example.api", then you would import the client as `import com.example.api.client.okhttp.ExampleOkHttpClient`
       */
      reverse_domain: string;
      /**
       * See [our publishing guide](/docs/guides/publish#java--kotlin-sonatype-maven-central) for more information on how to publish your SDK.
       */
      publish: {
        maven:
          | boolean
          | {
              /**
               * The maven groupId to publish this artifact to, e.g. `com.example.api`. By default, uses the value of reverse_domain.
               */
              group_id?: string;
              /**
               * The maven artifactId to publish this artifact as, defaults to `<organization.name>-java`, e.g. `example-java`
               */
              artifact_id?: string;
              /**
               * Customers who do not already have a Sonatype OSSRH account should use the newer "portal" option.
               */
              sonatype_platform?: 'portal' | 'ossrh';
              sonatype_host?: string;
              [k: string]: unknown;
            };
        /**
         * Whether to generate and include HTML docs in the published JAR.
         */
        docs?: boolean;
        /**
         * Whether to generate and include [ProGuard keep rules](https://www.guardsquare.com/manual/configuration/usage) in the published JAR.
         */
        proguard?: boolean;
        /**
         * Whether to generate and include [GraalVM reachability metadata](https://www.graalvm.org/latest/reference-manual/native-image/metadata) in the published JAR.
         */
        graalvm_metadata?: boolean;
        /**
         * Whether to generate and publish a [Spring Boot starter](https://docs.spring.io/spring-boot/docs/2.7.18/reference/htmlsingle/#using.build-systems.starters) artifact.
         */
        spring_boot_starter?: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        gradle_properties?: {
          /**
           * Override the JVM min memory in gradle.properties. Defaults to "2g". Format should be [number][unit], e.g. "8g" or "8192m".
           */
          jvm_min_memory?: string;
          /**
           * Override the JVM max memory in gradle.properties. Defaults to "8g". Format should be [number][unit], e.g. "8g" or "8192m".
           */
          jvm_max_memory?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    kotlin?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'kotlin.2025-10-08' | string;
      /**
       * The reverse domain to generate the SDK under, e.g. if `reverse_domain` is "com.example.api", then you would import the client as `import com.example.api.client.okhttp.ExampleOkHttpClient`
       */
      reverse_domain: string;
      /**
       * See [our publishing guide](/docs/guides/publish#java--kotlin-sonatype-maven-central) for more information on how to publish your SDK.
       */
      publish: {
        maven:
          | boolean
          | {
              /**
               * The maven groupId to publish this artifact to, e.g. `com.example.api`. By default, uses the value of reverse_domain.
               */
              group_id?: string;
              /**
               * The maven artifactId to publish this artifact as, defaults to `<organization.name>-java`, e.g. `example-java`
               */
              artifact_id?: string;
              /**
               * Customers who do not already have a Sonatype OSSRH account should use the newer "portal" option.
               */
              sonatype_platform?: 'portal' | 'ossrh';
              sonatype_host?: string;
              [k: string]: unknown;
            };
        /**
         * Whether to generate and include HTML docs in the published JAR.
         */
        docs?: boolean;
        /**
         * Whether to generate and include [ProGuard keep rules](https://www.guardsquare.com/manual/configuration/usage) in the published JAR.
         */
        proguard?: boolean;
        /**
         * Whether to generate and include [GraalVM reachability metadata](https://www.graalvm.org/latest/reference-manual/native-image/metadata) in the published JAR.
         */
        graalvm_metadata?: boolean;
        /**
         * Whether to generate and publish a [Spring Boot starter](https://docs.spring.io/spring-boot/docs/2.7.18/reference/htmlsingle/#using.build-systems.starters) artifact.
         */
        spring_boot_starter?: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        gradle_properties?: {
          /**
           * Override the JVM min memory in gradle.properties. Defaults to "2g". Format should be [number][unit], e.g. "8g" or "8192m".
           */
          jvm_min_memory?: string;
          /**
           * Override the JVM max memory in gradle.properties. Defaults to "8g". Format should be [number][unit], e.g. "8g" or "8192m".
           */
          jvm_max_memory?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    go?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'go.2026-04-15' | 'go.2026-03-10' | 'go.2026-2-3' | 'go.2025-10-08' | string;
      /**
       * The name of the root package, containing the top-level client, options, and methods.
       */
      package_name: string;
      options?: {
        /**
         * Override the Go module path used in imports. For example, if your module is hosted at my-custom-host.com/acme/terraform-provider-acme instead of the default {repository.host}/{repository.full_name}. The major version suffix (e.g. `/v2`) is still appended automatically.
         */
        go_module_path_override?: string;
        /**
         * Override the name used for the internal `option` package
         */
        option_package_name?: string;
        /**
         * Overrides the name used for the API client struct. This name should be pascal cased and a valid public Go identifier. By default, this is `Client`, and the constructor will be called `NewClient`.
         *
         * Overriding it to e.g. `APIClient` will change the struct to be `APIClient`, and the corresponding constructor will be called `NewAPIClient`.
         */
        client_name?: string;
        /**
         * Explicitly set the Go version to use
         */
        go_language_version?: string;
        code_style?: ('v2' | 'v2_migration' | 'v1') | null;
        /**
         * Enable the V2 Go SDK generator with "true" or "migration". **Caution:** the V2 SDK is not backwards compatible with V1.
         *
         * Defaults to 'false' if not specified.
         */
        enable_v2?: boolean;
        /**
         * Generates getter methods to access nested properties in unions without needing to switch on the variant.
         */
        union_property_getters?: boolean;
        /**
         * Doesn't suffix union types with '-Union', and removes the 'Of-' prefix from union variant names. This option is **not recommended** for large (or soon-to-be large) APIs, as it comes with visual clarity and forwards compatibility costs.
         */
        subtle_union_naming?: boolean;
        /**
         * Doesn't suffix params types with '-Param'. Use with caution: this means models cannot be used in both params and response (without additional configuration). Currently available for Go V2 only.
         */
        remove_params_suffix?: boolean;
        /**
         * Setting to false will not automatically apply URI encoding to path parameters.
         */
        path_param_encoding?: boolean;
        /**
         * In the Go Standard Library HTML in JSON is escaped by default, so this defaults to true.
         */
        escape_html_in_json?: boolean;
        /**
         * Generate pointers for nullable values inside maps and arrays (e.g. `map[string]*string` instead of `map[string]string`, `[]*string` instead of `[]string`).
         *
         * Go V2 only — Go V3 always applies this behavior. Defaults to `true` starting with the `go.2026-04-15` edition.
         */
        nullable_map_items?: boolean;
        /**
         * Setting to `false` makes the `options` field in the client / service structs private
         */
        back_compat_options_field_name?: boolean;
        [k: string]: unknown;
      };
      publish?: {
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    ruby?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'ruby.2025-10-08' | string;
      /**
       * The name of the gem.
       */
      gem_name: string;
      /**
       * Overrides the name used for the root module. This name should a valid public Ruby identifier. By default, this is the pascal case gem name.
       *
       * i.e. if the gem name is "super-acme", the default root module name would be "SuperAcme".
       */
      package_name?: string | null;
      publish: {
        rubygems: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        /**
         * Overrides the name used for the API client struct. This name should be a valid public Ruby identifier. By default, this is `Client`.
         *
         * Overriding it to e.g. `APIClient` will change the class to be `APIClient`.
         */
        client_name?: string;
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    terraform?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'terraform.2025-10-08' | string;
      /**
       * The production repo that publishes to the Terraform Registry.
       *
       * The Terraform Registry requires GitHub repos to be prefixed with `terraform-provider-<providername>`, e.g. `Acme-Org/terraform-provider-acme`.
       */
      production_repo: string | null;
      /**
       * The name of the provider that users import. This is usually a single word, and is prefixed by your org name.
       */
      provider_name: string;
      publish: {
        /**
         * Enable releasing to Hashicorp. See https://www.stainless.com/docs/guides/publish#terraform-terraform-registry
         */
        hashicorp_registry: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        /**
         * Override the Go module path used in imports. For example, if your module is hosted at my-custom-host.com/acme/terraform-provider-acme instead of the default {repository.host}/{repository.full_name}.
         */
        go_module_path_override?: string;
        /**
         * Explicitly set the Go SDK package to depend on
         */
        go_sdk_package?: string;
        /**
         * Explicitly set the Go SDK version to depend on
         */
        go_sdk_version?: string;
        /**
         * Explicitly set the Go version to use
         */
        go_language_version?: string;
        /**
         * Automatically infer all possible resources and data sources from the stainless config
         */
        infer_all_services?: boolean;
        /**
         * Generate tfplugindocs (https://github.com/hashicorp/terraform-plugin-docs) for the provider
         */
        generate_docs?: boolean;
        /**
         * whether to enable a timeout property for resources
         */
        timeouts?:
          | boolean
          | {
              /**
               * Attribute name for timeout parameter
               */
              name?: string;
              /**
               * Default timeout for the resource and subresources' Terraform operations in milliseconds or ISO8601.
               *
               * You can override this in subresources or individual methods.
               *
               * This doesn't affect the request-level timeout.
               */
              default?: number | string;
            };
        data_source?: {
          filtering: {
            /**
             * Attribute name for the find one by filter for singular data sources
             */
            find_one_by_property_name?: string;
          };
          response_items: {
            /**
             * Attribute name for the result collection for list data sources (default: `items`)
             */
            collection_property_name?: string;
            /**
             * The maximum number of rows to fetch by default
             */
            default_max_rows?: number;
            /**
             * Attribute name for the maximum number of rows in the response
             */
            max_rows_property_name?: string;
          };
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    cli?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'cli.2025-10-08' | string;
      /**
       * The name of the binary, defaults to the repository name.
       */
      binary_name?: string;
      options?: {
        /**
         * Override the Go module path used in imports. For example, if your module is hosted at my-custom-host.com/acme/terraform-provider-acme instead of the default {repository.host}/{repository.full_name}. The major version suffix (e.g. `/v2`) is still appended automatically.
         */
        go_module_path_override?: string;
        /**
         * Set which package to use for your API's Go SDK to use
         */
        go_sdk_package?: string;
        /**
         * Set which version of your API's Go SDK to use
         */
        go_sdk_version?: string;
        /**
         * Explicitly set the Go version to use
         */
        go_language_version?: string;
        [k: string]: unknown;
      };
      /**
       * The default output format for CLI methods. This can be overridden on each
       * endpoint in its `cli.format` option or with the `--format` flag when
       * running the command.
       */
      default_format?: 'auto' | 'explore' | 'json' | 'jsonl' | 'pretty' | 'raw' | 'yaml';
      /**
       * The default output format for CLI GET endpoints that return JSON (excludes
       * binary responses). When set, this overrides `default_format` for those
       * endpoints. This can be overridden on each endpoint in its `cli.format`
       * option or with the `--format` flag when running the command.
       */
      default_json_get_format?: 'auto' | 'explore' | 'json' | 'jsonl' | 'pretty' | 'raw' | 'yaml';
      /**
       * The default output format for CLI error responses. This can be overridden
       * on each endpoint in its `cli.error_format` option or with the
       * `--format-error` flag when running the command.
       */
      default_error_format?: 'auto' | 'explore' | 'json' | 'jsonl' | 'pretty' | 'raw' | 'yaml';
      publish?: {
        /**
         * GitHub environment to run the CLI goreleaser publish workflow in. Overrides `codeflow.release_environment` for this workflow.
         */
        release_environment?: string;
        macos?:
          | boolean
          | {
              [k: string]: unknown;
            };
        /**
         * Configuration for publishing to Homebrew
         */
        homebrew?: {
          /**
           * The homebrew tap repository, e.g. AcmeOrg/homebrew-acme
           */
          tap_repo: string;
          /**
           * Homepage URL for the homebrew formula
           */
          homepage?: string;
          /**
           * Description for the homebrew formula
           */
          description?: string;
          [k: string]: unknown;
        };
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    php?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'php.2025-10-08' | string;
      /**
       * The name of the root package, containing the top-level client, options, and methods.
       *
       * For example, `package_name: acme` would result in `use Acme\Client` for SDK users.
       *
       * Unlike the composer_package_name, this name is only used **within the SDK library code**.
       */
      package_name: string;
      /**
       * The ecosystem name of the package as used by Composer & Packagist. The name should resemble 'acme/acme-php'.
       */
      composer_package_name?: string;
      /**
       * The repository url of the package as used by Composer & Packagist. The url should resemble 'https://github.com/acme/acme-php'.
       */
      composer_repository_url?: string;
      publish?: {
        /**
         * Whether to publish the package to releases to Packagist. Use Packagist to verify the `composer_package_name` is available first.
         */
        packagist: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    csharp?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'csharp.2025-10-08' | string;
      /**
       * The name of the root package, containing the top-level client, options, and methods.
       */
      package_name: string;
      publish: {
        nuget: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Configuring strong naming. For more information, see https://learn.microsoft.com/en-us/dotnet/standard/assembly/strong-named
       */
      strong_name?: {
        /**
         * The assembly originator key file (snk file), in base64 format.
         */
        originator_key: string;
        /**
         * The public key associated with the assembly originator key file.
         */
        public_key: string;
        [k: string]: unknown;
      };
      options?: {
        /**
         * Set the C# language version (not the .NET version).  Stainless currently supports `10` or `12`.
         */
        language_version?: '10' | '12';
        /**
         * Whether to run unit tests against .NET 4.6.2. Useful for ensuring the SDK is compatible with .NET Standard 2.0.
         */
        dotnetStandardTests?: boolean;
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    openapi?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'openapi.2025-10-08' | string;
      options?: {
        files?: {
          [k: string]: {
            with: ('code-samples' | 'transforms')[];
            [k: string]: unknown;
          };
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    sql?: {
      /**
       * Title for this package, typically used to display the README header in the format `<Title> API Library` (for example, "Your company language library")'
       */
      readme_title?: string;
      production_repo?: string | null;
      /**
       * URL of a screencast to showcase on the project README. Must be an .mp4, .webm, or .mov file less than 10MB.
       */
      screencast?: string;
      /**
       * Skip generation for this target
       */
      skip?: boolean;
      /**
       * An array of file paths, relative to the root of the SDK repository, that will not be deleted during codegen.  Generally we recommend you use [custom code](/docs/sdks/configure/custom-code) instead, but `keep_files` can be useful for other files you manually add that need to persist through codegen.
       *
       * For example:
       *
       * ~~~yaml
       * keep_files:
       *   - .github/workflows/ci_tests.yml
       * ~~~
       */
      keep_files?: string[];
      edition?: 'sql.2025-12-08' | string;
      /**
       * The name of the SQL extension.
       */
      extension_name: string;
      publish: {
        pgxn: boolean;
        github?: {
          /**
           * Additional files to upload to GitHub Releases after publishing.
           */
          release_assets: (
            | string
            | {
                /**
                 * Name for this release asset in GitHub Releases.
                 */
                name?: string;
                /**
                 * Path to the release asset file, relative to the repository root.
                 */
                path: string;
                [k: string]: unknown;
              }
          )[];
          [k: string]: unknown;
        };
        /**
         * Release-please configuration. Controls the generated release-please workflow and config files.
         */
        release?: {
          /**
           * Branch that release-please targets for release PRs and changelog generation.
           */
          branch?: string;
          /**
           * Whether to mark GitHub Releases as pre-releases.
           */
          prerelease?: boolean;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      options?: {
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    [k: string]: unknown;
  };
  /**
   * Map of the name of the environment which appears in the SDK to the corresponding url to use.
   * The first environment in the map will be used as the default environment.
   *
   * ~~~yaml
   * environments:
   *   production: https://example.com/api
   *   sandbox: https://sandbox.example.com/api
   * ~~~
   */
  environments: {
    [k: string]: string;
  };
  /**
   * Resources define the organization for your API and the endpoints that are available under which resources.
   */
  resources: {
    $shared?: ResourceConfig;
    $client?: ResourceConfig;
    [k: string]: ResourceConfig | undefined;
  };
  /**
   * Configuration of the examples in the README.md of the generated SDKs.
   *
   * Learn more: https://www.stainless.com/docs/configure/readme
   */
  readme: {
    /**
     * Configuration for the request examples used in the README.md for the various SDKs. Each example by default inherits off the default endpoint, and the default endpoint must have a response body.
     *
     * We suggest you use the most standard endpoint for the `default` example, and the most important example for the `headline` example
     */
    example_requests: {
      default: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      headline: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * Example of how to handle errors in the SDK.
       *
       * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
       * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
       */
      errors?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      } & {
        /**
         * The response properties to show in this example code. E.g.:
         *
         * example_properties:
         *   error: 'Rate limit exceeded.'
         *   request_id: 'req_123'
         */
        example_properties?: {
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to retry requests.
       */
      retries?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that demonstrates nested parameters.
       */
      nested_params?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to use a custom HTTP agent.
       */
      http_agent?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to make a raw API request.
       */
      raw_response?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to handle timeouts.
       */
      timeouts?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to set default headers.
       */
      default_headers?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to use pagination.
       */
      pagination?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      /**
       * For example code that shows how to use streaming.
       */
      streaming?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        [k: string]: unknown;
      };
      file_uploads?: {
        type: 'request';
        /**
         * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
         */
        endpoint: string;
        /**
         * The request params to include in the example.
         *
         * e.g.
         *
         * params:
         *   query_param: 'foo'
         *   body_param: true
         *   path_param: id
         */
        params: {
          [k: string]: unknown;
        };
        /**
         * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
         */
        response_property?: string;
        /**
         * The name of the variable to assign the result of this request to for this example.
         *
         * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
         *
         * ~~~ts
         * customer = client.customers.create();
         * ~~~
         */
        assign_to?: string;
        /**
         * The name of the request parameter for the file. E.g., `file`
         */
        file_param: string;
        /**
         * The path and filename of the file being uploaded. E.g., `my/file.txt`
         */
        file_path?: string;
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the Go SDK
       */
      go?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the TypeScript SDK
       */
      typescript?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the Java SDK
       */
      java?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the Kotlin SDK
       */
      kotlin?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the Ruby SDK
       */
      ruby?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the PHP SDK
       */
      php?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the C# SDK
       */
      csharp?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the Terraform provider
       */
      terraform?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the CLI
       */
      cli?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Override specific examples for the SQL SDK
       */
      sql?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      /**
       * Examples specific to the Python SDK
       */
      python?: {
        default?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        headline?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * Example of how to handle errors in the SDK.
         *
         * NOTE: Even though this is an "error" example, it should be well-formed! (E.g. it shouldn't have missing or invalid parameters.)
         * It demonstrates how to handle network errors and the like, not how to handle malformed requests, since the SDK ensures that requests are well-formed.
         */
        errors?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        } & {
          /**
           * The response properties to show in this example code. E.g.:
           *
           * example_properties:
           *   error: 'Rate limit exceeded.'
           *   request_id: 'req_123'
           */
          example_properties?: {
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to retry requests.
         */
        retries?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that demonstrates nested parameters.
         */
        nested_params?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use a custom HTTP agent.
         */
        http_agent?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to make a raw API request.
         */
        raw_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to handle timeouts.
         */
        timeouts?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to set default headers.
         */
        default_headers?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use pagination.
         */
        pagination?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        /**
         * For example code that shows how to use streaming.
         */
        streaming?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        file_uploads?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          /**
           * The name of the request parameter for the file. E.g., `file`
           */
          file_param: string;
          /**
           * The path and filename of the file being uploaded. E.g., `my/file.txt`
           */
          file_path?: string;
          [k: string]: unknown;
        };
        raw_streaming_response?: {
          type: 'request';
          /**
           * Endpoint to use as the example method, in the format `post /foo/bar/{id}`
           */
          endpoint: string;
          /**
           * The request params to include in the example.
           *
           * e.g.
           *
           * params:
           *   query_param: 'foo'
           *   body_param: true
           *   path_param: id
           */
          params: {
            [k: string]: unknown;
          };
          /**
           * The property on the response to log, e.g. `id` on a `get /accounts` endpoints adds a `console.log(account.id)` or equivalent in the example
           */
          response_property?: string;
          /**
           * The name of the variable to assign the result of this request to for this example.
           *
           * For example, given the method `post /v1/customers`, set `assign_to: customer` to produce example code like this:
           *
           * ~~~ts
           * customer = client.customers.create();
           * ~~~
           */
          assign_to?: string;
          [k: string]: unknown;
        };
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    /**
     * Configuration for the models/types to use for demonstration of how to instantiate and access different types in the SDK.
     */
    example_types?: {
      object?: {
        type: 'model';
        /**
         * The dotted path to the model defined in the config.
         *
         * For example, a 'transaction' model defined in the 'cards.transactions'
         * resource would be 'cards.transactions.transaction'
         */
        model: string;
        /**
         * The property on the model schema to use as an example
         */
        property: string;
        [k: string]: unknown;
      };
      nullable?: {
        type: 'model';
        /**
         * The dotted path to the model defined in the config.
         *
         * For example, a 'transaction' model defined in the 'cards.transactions'
         * resource would be 'cards.transactions.transaction'
         */
        model: string;
        /**
         * The property on the model schema to use as an example
         */
        property: string;
        [k: string]: unknown;
      };
      enum?: {
        type: 'model';
        /**
         * The dotted path to the model defined in the config.
         *
         * For example, a 'transaction' model defined in the 'cards.transactions'
         * resource would be 'cards.transactions.transaction'
         */
        model: string;
        /**
         * The enum member to reference in examples
         */
        option: string;
        [k: string]: unknown;
      };
      union?: {
        type: 'model';
        /**
         * The dotted path to the model defined in the config.
         *
         * For example, a 'transaction' model defined in the 'cards.transactions'
         * resource would be 'cards.transactions.transaction'
         */
        model: string;
        /**
         * The property on the model schema to use as an example
         */
        property: string;
        [k: string]: unknown;
      };
      intersection?: {
        type: 'model';
        /**
         * The dotted path to the model defined in the config.
         *
         * For example, a 'transaction' model defined in the 'cards.transactions'
         * resource would be 'cards.transactions.transaction'
         */
        model: string;
        /**
         * The property on the model schema to use as an example
         */
        property: string;
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    /**
     * Include a powered by Stainless attribution in the readme. A paid plan is required to set this to `false`; see [our pricing page](https://stainless.com/pricing) for details.
     */
    include_stainless_attribution?: boolean;
    [k: string]: unknown;
  };
  settings?: {
    /**
     * License to use in all SDKs (defaults to Apache-2.0)
     */
    license?:
      | ('Apache-2.0' | 'MIT')
      | {
          /**
           * The name given to this license, should be a valid SPDX identifier or expression
           */
          name: string;
          /**
           * The license contents.
           */
          contents: string;
          /**
           * The file extension that the LICENSE file will be generated to
           */
          extension?: string;
          [k: string]: unknown;
        };
    /**
     * When `true`, the `security` field on an operation will be respected and the SDK will only
     * send matching security schemes.
     *
     * For example:
     *
     * ```yaml
     * security: [{ BearerAuth: [] }, { APIKeyAuth }]
     * paths:
     *   /accounts:
     *     post:
     *       security: [{ BearerAuth: [] }]
     * ```
     *
     * By default, if the SDK is given the client option for the `APIKeyAuth` scheme, then it will happily send it.
     *
     * When `per_endpoint_security` is set to `true`, the SDK will *only* try and send the `BearerAuth` header.
     */
    per_endpoint_security?: boolean;
    /**
     * Custom header to add at the top of all generated SDK files.
     *
     * The header will be added as a comment at the very top of each generated file,
     * before the "File generated from our OpenAPI spec by Stainless" line.
     * Each language will use its appropriate comment syntax automatically.
     *
     * Example usage for copyright headers:
     * ~~~yaml
     * settings:
     *   file_header: |
     *     Copyright (c) 2025 ACME Corporation.
     *     All rights reserved.
     *
     *     This source code is licensed under the terms described in the LICENSE file in
     *     the root directory of this source tree.
     * ~~~
     *
     * The above will generate (in TypeScript):
     * ~~~ts
     * // Copyright (c) 2025 ACME Corporation.
     * // All rights reserved.
     * //
     * // This source code is licensed under the terms described in the LICENSE file in
     * // the root directory of this source tree.
     * // File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
     * ~~~
     */
    file_header?: string;
    /**
     * The mock server to run the auto-generated tests against.  Currently, we only support [steady](https://www.npmjs.com/package/@stdy/cli).
     */
    mock_server?: 'steady';
    python?: {
      /**
       * The license classifier to use in the Python package metadata. https://pypi.org/classifiers/
       */
      license_classifier?: string;
      [k: string]: unknown;
    };
    /**
     * If set to true, all generated integration tests that hit the mock HTTP server are marked as skipped.
     */
    disable_mock_tests?: boolean;
    /**
     * @deprecated
     * Deprecated: use `disable_mock_tests` instead.
     */
    disable_tests?: boolean;
    /**
     * Response envelopes, like a top-level "data" field, can be described here with [data]
     */
    unwrap_response_fields?: string[];
    /**
     * If specified as false, then we disable positional params for the entire SDK.
     */
    positional_params?: false;
    /**
     * When set to true, WebSocket dependencies are listed as required dependencies rather than optional. Use this when the API primarily communicates over WebSockets and most consumers will need them.
     */
    require_websocket_dependencies?: boolean;
    /**
     * Normally, response envelopes are not unwrapped if they have more than one top-level property. This unwraps them anyway.
     */
    unwrap_multiproperty_response_fields?: boolean;
    /**
     * Suggest every schema that could be a model as Model/Recommended, not just those that are repeated
     */
    suggest_all_potential_models?: boolean;
    [k: string]: unknown;
  };
  /**
   * Query string serialization options.
   */
  query_settings?: {
    /**
     * Configures how query parameters with nested objects render in the query string. Accepted values are `brackets` (the default) and `dots`.
     *
     * Example object: `{ a: { b: "c" } }`
     *
     * - brackets syntax -> `a[b]=c`
     * - dot syntax -> `a.b=c`
     */
    nested_format?: 'dots' | 'brackets';
    /**
     * Configures how query parameters with array values render in the query string. Accepted values are `brackets`, `comma` (the default), and `repeat`.
     *
     * Example object: `{ in: ["foo", "bar"] }`
     *
     * - brackets syntax -> `in[]=foo&in[]=bar`
     * - comma syntax -> `in=foo,bar`
     * - repeat syntax -> `in=foo&in=bar`
     */
    array_format?: 'comma' | 'repeat' | 'indices' | 'brackets';
    [k: string]: unknown;
  };
  /**
   * Multipart form data serialization options.
   */
  multipart_settings?: {
    /**
     * Configures how arrays are serialized in multipart/form-data request bodies. Accepted values are `brackets`, `comma`, `repeat`, and `indices`.
     *
     * When not set, each language SDK uses its existing default:
     * - TypeScript, Python: `brackets`
     * - Ruby, PHP: `repeat`
     * - Go v1: `indices` (dot-separated)
     * - Java, Kotlin, C#, Go v2: inherits from `query_settings.array_format`
     *
     * Example array: `{ items: ["foo", "bar"] }`
     *
     * - brackets syntax -> `items[]=foo&items[]=bar`
     * - comma syntax -> `items=foo,bar`
     * - repeat syntax -> `items=foo&items=bar`
     * - indices syntax -> `items[0]=foo&items[1]=bar`
     */
    array_format?: 'comma' | 'repeat' | 'indices' | 'brackets';
    [k: string]: unknown;
  };
  /**
   * Overrides the top-level security key in the OpenAPI spec. See [our authentication documentation](/docs/sdks/configure/client/#authentication) for more details.
   */
  security?: {
    [k: string]: string[];
  }[];
  /**
   * Overrides the `components.securitySchemes` in the OpenAPI spec.
   */
  security_schemes?: {
    [k: string]:
      | {
          type: 'http';
          description?: string;
          scheme: 'bearer';
          bearerFormat?: string;
          'x-stainless-auth'?: {
            scheme_keyword?: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          type: 'http';
          description?: string;
          scheme: 'basic';
          'x-stainless-auth'?: {
            disable_base64?: boolean;
            scheme_keyword?: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          type: 'http';
          description?: string;
          scheme: 'digest';
          'x-stainless-auth'?: {
            scheme_keyword?: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          type: 'apiKey';
          description?: string;
          in?: 'header' | 'query';
          name: string;
          [k: string]: unknown;
        }
      | {
          type: 'oauth2';
          description?: string;
          flows: {
            implicit?: {
              authorizationUrl: string;
              refreshUrl?: string;
              scopes: {
                [k: string]: string;
              };
              [k: string]: unknown;
            };
            password?: {
              tokenUrl: string;
              refreshUrl?: string;
              scopes: {
                [k: string]: string;
              };
              [k: string]: unknown;
            };
            clientCredentials?: {
              tokenUrl: string;
              refreshUrl?: string;
              scopes: {
                [k: string]: string;
              };
              [k: string]: unknown;
            };
            authorizationCode?: {
              authorizationUrl: string;
              tokenUrl: string;
              refreshUrl?: string;
              scopes: {
                [k: string]: string;
              };
              [k: string]: unknown;
            };
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
  };
  /**
   * Settings for customizing the [Client class](https://www.stainless.com/docs/configure/client) in the SDKs.
   */
  client_settings?: {
    /**
     * Extra arguments that the client accepts, such as an API key.
     *
     * Learn more: https://www.stainless.com/docs/configure/client#client-opts
     */
    opts?: {
      [k: string]: {
        type: 'boolean' | 'number' | 'string' | 'integer';
        /**
         * Description used for this option in the generated SDKs
         */
        description?: string;
        /**
         * Example value used in tests or example snippets
         */
        example?: {
          [k: string]: unknown;
        };
        /**
         * Default value to use if no value provided by the user
         */
        default?: {
          [k: string]: unknown;
        };
        /**
         * Whether this client option is required
         */
        nullable?: boolean;
        /**
         * Environment variable that this option should read from
         */
        read_env?: string;
        /**
         * Indicates that this client option is used to provide credentials for the given security scheme.
         *
         * Learn more: https://www.stainless.com/docs/configure/client/#authentication
         */
        auth?: {
          security_scheme: string;
          role?: 'value' | 'username' | 'password' | 'client_id' | 'client_secret';
          [k: string]: unknown;
        };
        /**
         * Links this client option to a variable in the url configured by `environments`.
         *
         * ~~~yaml
         * environments:
         *   production: https://{region}.acme.com
         * ~~~
         *
         * Then `server_variable: "region"` will mean the value of the `{region}` will be replaced by the value of this client option.
         */
        server_variable?: string;
        /**
         * Send the value given to this option in a header on every request
         */
        send_in_header?: string;
        /**
         * Use this client opt to fill out the query parameters in requests.
         */
        send_as_query_param?: string;
        /**
         * Use this client opt to fill out the body parameters in requests.
         */
        send_as_body_param?: string;
        /**
         * Use this client opt to fill out the path parameters in requests. Note that the path parameter is required to make the request, and should either be provided either on the client or on the request if the requests requires it.
         */
        send_as_path_param?: string;
        /**
         * Whether to provide this argument in unit tests. Set to true if this argument is required for internal request tests to run successfully
         */
        required_in_tests?: boolean;
        [k: string]: unknown;
      };
    };
    /**
     * The default name to use for the generated example client instantiated variable. If not set, defaults to 'client'.
     *
     * This is used in the generated examples, e.g. `const client = new Acme();`.
     */
    default_client_example_name?: string;
    /**
     * The default name to use for the generated client class(es). If not set, defaults to the organization name.
     */
    default_client_name?: string;
    /**
     * The default prefix to use for environment variables, e.g. `ACME` -> `ACME_BASE_URL`. If not set, defaults to the organization name.
     *
     * Note this is only used for cases where Stainless generates an environment variable name, if you have an explicit environment variable set, e.g.
     * in `client_settings.opts.read_env`, this env prefix will **not** apply.
     */
    default_env_prefix?: string;
    /**
     * Configure [idempotency behaviour](https://www.stainless.com/docs/configure/client#idempotency-key)
     */
    idempotency?: {
      /**
       * Header to send the idempotency token in
       */
      header: string;
      [k: string]: unknown;
    };
    /**
     * Do not send [`X-Stainless-*` headers](https://www.stainless.com/docs/configure/client/#default-headers) with each request.
     *
     * This can be useful if your API has a headers allowlist.
     */
    omit_stainless_headers?: boolean;
    /**
     * Headers sent with every API request
     */
    default_headers?: {
      [k: string]: string;
    };
    /**
     * Configure the [default timeout for client calls](https://www.stainless.com/docs/configure/client#timeouts), in milliseconds or ISO 8601 (default is 60 seconds)
     */
    default_timeout?: number | string;
    /**
     * Default [retry settings](https://www.stainless.com/docs/configure/client#retries) for all endpoints.
     */
    default_retries?: {
      /**
       * Maximum number of times to retry for all endpoints. Use 0 to disable retries.
       */
      max_retries: number;
      /**
       * Seconds to wait before the first retry.
       */
      initial_delay_seconds: number;
      /**
       * We increase the retry time with exponential backoff, each retry taking twice as long as the last, until this value of max seconds to retry.
       */
      max_delay_seconds: number;
      [k: string]: unknown;
    };
    response_headers?: {
      [k: string]: string;
    };
    /**
     * Python-specific client settings
     */
    python?: {
      /**
       * Default resource limits for the HTTP client. See https://www.python-httpx.org/advanced/resource-limits for defaults and more information.
       */
      default_http_limits: {
        /**
         * Maximum number of allowable connections
         */
        max_connections?: number;
        /**
         * Number of allowable keep-alive connections
         */
        max_keepalive_connections?: number;
        /**
         * Time limit on idle keep-alive connections in seconds
         */
        keepalive_expiry?: number;
        [k: string]: unknown;
      };
      [k: string]: unknown;
    };
    [k: string]: unknown;
  };
  /**
   * Defines pagination schemes for pagination helpers and auto-pagination in the SDKs.
   *
   * See [our pagination guide](/docs/sdks/configure/pagination) for more details and examples.
   */
  pagination?:
    | {
        description?: string;
        type: 'cursor' | 'cursor_id' | 'cursor_url' | 'fake_page' | 'offset' | 'page_number';
        request: {
          [k: string]: unknown;
        };
        response: {
          [k: string]: unknown;
        };
        param_location?: 'query' | 'body';
        /**
         * If set to true, the auto pagination helpers will continue to fetch pages even if there are no items returned, and will only stop when it is impossible to fetch another page
         */
        continue_on_empty_items?: boolean;
        [k: string]: unknown;
      }
    | {
        name: string;
        description?: string;
        type: 'cursor' | 'cursor_id' | 'cursor_url' | 'fake_page' | 'offset' | 'page_number';
        request: {
          [k: string]: unknown;
        };
        response: {
          [k: string]: unknown;
        };
        param_location?: 'query' | 'body';
        /**
         * If set to true, the auto pagination helpers will continue to fetch pages even if there are no items returned, and will only stop when it is impossible to fetch another page
         */
        continue_on_empty_items?: boolean;
        [k: string]: unknown;
      }[];
  /**
   * List of endpoints to explicitly ignore, such as `["post /internal_endpoint", "get /redundant_endpoint"]`
   */
  unspecified_endpoints?: string[];
  /**
   * Configures various codeflow (automated releases to package managers) options.
   */
  codeflow?: {
    /**
     * GitHub environment to run the release workflow in.
     */
    release_environment?: string;
    /**
     * Whether publishing to package managers should be included in GitHub automations.
     */
    publish_packages?: boolean;
    /**
     * List of default reviewers to assign to a customer release PR
     */
    reviewers?: string[];
    /**
     * Github CODEOWNERS file content to add to SDK repositories
     *
     * Example:
     *
     *     code_owners:
     *       '*': ['@acme/sdk-team-1']
     *       'src/**': ['@acme/sdk-team-2']
     */
    code_owners?: {
      [k: string]: string[];
    };
    /**
     * Enable to automatically detect some breaking changes.
     *
     * With `detect_breaking_changes` enabled, we'll check out the public API test files in their pre-release state and attempt to compile/lint them against your new SDK version.  If your previous test code fails to compile with your new SDK, we surface the errors as an indicator that your new release contains breaking changes.
     */
    detect_breaking_changes?: boolean;
    [k: string]: unknown;
  };
  /**
   * Defines how phrases are rendered in different SDKs. Can be either an 'initialism' which tells the generator to treat the word as an abbreviation like 'API' or 'CPU', or a more powerful `CasingConfig` which can specify how that word is represented in the different casing formats. In rare cases where customization of pluralization is required, you can also specify singular/plural versions of the word.
   */
  custom_casings?: {
    [k: string]:
      | {
          /**
           * If set to true, the term is used as an initialism: treated as an abbreviation of initial letters (for example, "CPU" and "API").
           * If set to false, the term is removed from the set of initialisms.
           *
           * By default we use `[3ds, ach, acl, ai, api, ats, cpu, crm, db, dns, e2e, eeoc, gb, gpu, hd, hris, html, http, https, id, ip, iso, jsonl, kb, kyb, kyc, lfs, mb, mp3, mp4, nft, saml, sdk, sku, sms, ss, ssh, sso, url]` as initialisms.
           *
           * Example:
           *
           * ~~~yaml
           * custom_casings:
           *   mcp: { initialism: true }
           * ~~~
           */
          initialism: boolean;
          [k: string]: unknown;
        }
      | {
          /**
           * Term rendered with lowercase characters separated by underscores.
           *
           * Example:
           *
           * ~~~yaml
           * custom_casings:
           *   payment_method:
           *     snake: "payment_method"
           * ~~~
           */
          snake: string;
          /**
           * Term rendered with the first character of each word capitalized and joined together.
           *
           *     Example:
           *
           *     ~~~yaml
           *     custom_casings:
           *       payment_method:
           *         pascal: "PaymentMethod"
           *     ~~~
           */
          pascal: string;
          /**
           * Term rendered with the first character of each word capitalized and separated by a space.
           *
           *     Example:
           *
           *     ~~~yaml
           *     custom_casings:
           *       payment_method:
           *         capital: "Payment Method"
           *     ~~~
           */
          capital: string;
          /**
           * Term rendered with the first word in lowercase, followed by the first character of each subsequent word capitalized and joined together.
           *
           * Example:
           *
           * ~~~yaml
           * custom_casings:
           *   payment_method:
           *     camel: "paymentMethod"
           * ~~~
           */
          camel: string;
          [k: string]: unknown;
        }
      | {
          singular: {
            /**
             * Term rendered with lowercase characters separated by underscores.
             *
             * Example:
             *
             * ~~~yaml
             * custom_casings:
             *   payment_method:
             *     snake: "payment_method"
             * ~~~
             */
            snake: string;
            /**
             * Term rendered with the first character of each word capitalized and joined together.
             *
             *     Example:
             *
             *     ~~~yaml
             *     custom_casings:
             *       payment_method:
             *         pascal: "PaymentMethod"
             *     ~~~
             */
            pascal: string;
            /**
             * Term rendered with the first character of each word capitalized and separated by a space.
             *
             *     Example:
             *
             *     ~~~yaml
             *     custom_casings:
             *       payment_method:
             *         capital: "Payment Method"
             *     ~~~
             */
            capital: string;
            /**
             * Term rendered with the first word in lowercase, followed by the first character of each subsequent word capitalized and joined together.
             *
             * Example:
             *
             * ~~~yaml
             * custom_casings:
             *   payment_method:
             *     camel: "paymentMethod"
             * ~~~
             */
            camel: string;
            [k: string]: unknown;
          };
          plural: {
            /**
             * Term rendered with lowercase characters separated by underscores.
             *
             * Example:
             *
             * ~~~yaml
             * custom_casings:
             *   payment_method:
             *     snake: "payment_method"
             * ~~~
             */
            snake: string;
            /**
             * Term rendered with the first character of each word capitalized and joined together.
             *
             *     Example:
             *
             *     ~~~yaml
             *     custom_casings:
             *       payment_method:
             *         pascal: "PaymentMethod"
             *     ~~~
             */
            pascal: string;
            /**
             * Term rendered with the first character of each word capitalized and separated by a space.
             *
             *     Example:
             *
             *     ~~~yaml
             *     custom_casings:
             *       payment_method:
             *         capital: "Payment Method"
             *     ~~~
             */
            capital: string;
            /**
             * Term rendered with the first word in lowercase, followed by the first character of each subsequent word capitalized and joined together.
             *
             * Example:
             *
             * ~~~yaml
             * custom_casings:
             *   payment_method:
             *     camel: "paymentMethod"
             * ~~~
             */
            camel: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        };
  };
  /**
   * Describes changes to the input OpenAPI spec and whether or not to add code samples to the output OpenAPI spec. See [our transforms guide](/docs/openapi/transforms) for more details.
   */
  openapi?: {
    /**
     * List of transforms to apply to the OpenAPI spec before SDK generation. See [our transforms reference](/docs/openapi/transforms/reference) for available commands.
     *
     * Note: `openapi.transforms` was named `openapi.transformations` in early versions of Stainless.  The names are interchangeable, but we recommend you use the newer `openapi.transforms`.
     */
    transforms?: (
      | {
          command: 'update';
          reason: string;
          /**
           * Replace values at existing paths. Fails if path doesn't exist.
           *
           * Supports {{value}} templating for string manipulation. Supports matches_schema for finding duplicate schemas.
           *
           * Examples:
           * ~~~yaml
           * # Fix incorrect type with filter
           * - command: update
           *   reason: "Account IDs are strings"
           *   args:
           *     target: '$.paths..parameters[?(@.name == "account_id")].schema.type'
           *     value: "string"
           *
           * # String templating: append text
           * - command: update
           *   reason: "Add deprecation notice"
           *   args:
           *     target: "$.components.schemas.LegacyUser.description"
           *     value: "{{value}}\n\nDEPRECATED: Use NewUser instead"
           *     template: true
           *
           * # Schema matching: replace duplicates
           * - command: update
           *   reason: "Replace duplicate error schemas with $ref"
           *   args:
           *     target:
           *       - matches_schema: $.components.schemas.ErrorTemplate
           *         ignore: $.components.schemas.ErrorTemplate
           *     value:
           *       $ref: "#/components/schemas/Error"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the value(s) to replace. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * The new value to set at the target location(s).
             */
            value: {
              [k: string]: unknown;
            };
            /**
             * If true, enables {{value}} substitution in string values. Use for appending/prepending to strings. Example: "{{value}}\nAdditional notes" or "[BETA] {{value}}". Requires value to contain {{value}} and target to be a string.
             */
            template?: boolean;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'append';
          reason: string;
          /**
           * Add items to arrays or add new properties to objects. Attempting to add a property that already exists will fail. This alerts you when the spec is fixed.
           *
           * Use for temporary fixes. For intentional overrides, use merge.
           *
           * Examples:
           * ~~~yaml
           * # Add to array
           * - command: append
           *   reason: "Add id to required fields"
           *   args:
           *     target: "$.components.schemas.User.required"
           *     value: "id"
           *
           * # Add property (fails if exists)
           * - command: append
           *   reason: "API spec missing id field, adding temporarily"
           *   args:
           *     target: "$.components.schemas.User.properties"
           *     value:
           *       id:
           *         type: "string"
           *         format: "uuid"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to array(s) or object(s) to append to. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * For arrays: item(s) to push. For objects: properties to add (fails if property already exists).
             */
            value: {
              [k: string]: unknown;
            };
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'merge';
          reason: string;
          /**
           * Merge an object into targets. Overwrites existing properties. Uses deep merge, so nested objects are merged recursively.
           *
           * Use for permanent overrides like x-stainless-naming or x-stainless-skip. For temporary fixes, use append.
           *
           * Examples:
           * ~~~yaml
           * # Add x-stainless extension
           * - command: merge
           *   reason: "Terraform doesn't need pagination params"
           *   args:
           *     target: '$..parameters[?(@.name == "limit")]'
           *     value:
           *       schema:
           *         x-stainless-skip: ["terraform"]
           *
           * # Set language-specific naming
           * - command: merge
           *   reason: "Avoid Python parameter conflict"
           *   args:
           *     target: "$.components.schemas.Request.properties.timeout"
           *     value:
           *       x-stainless-naming:
           *         python:
           *           method_argument: "api_timeout"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to object(s) to merge into. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * The object to deep merge into the target location(s). Properties from this object will be merged into existing properties.
             */
            value: {
              [k: string]: unknown;
            };
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'remove';
          reason: string;
          /**
           * Delete nodes or specific keys from objects.
           *
           * Examples:
           * ~~~yaml
           * # Remove nodes
           * - command: remove
           *   reason: "Remove problematic examples"
           *   args:
           *     target:
           *       - "$.paths.*.*.examples"
           *       - "$.components.schemas.*.example"
           *
           * # Remove specific keys
           * - command: remove
           *   reason: "Remove incorrect default"
           *   args:
           *     target: "$.components.schemas.User.properties.role"
           *     keys: ["default"]
           *
           * # Remove with filter
           * - command: remove
           *   reason: "Remove deprecated parameters"
           *   args:
           *     target: '$.paths..parameters[?(@.deprecated == true)]'
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the node(s) to remove. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * If target points to an object, remove these specific keys from the object. If not provided, removes the entire target node.
             */
            keys?: string[];
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'copy';
          reason: string;
          /**
           * Copy a value to one or more destinations. Creates destination if parent exists. The source value is deep cloned to each destination.
           *
           * Examples:
           * ~~~yaml
           * # Copy schema
           * - command: copy
           *   reason: "Duplicate User schema"
           *   args:
           *     from: "$.components.schemas.User"
           *     to: "$.components.schemas.Admin"
           *
           * # Copy to multiple destinations
           * - command: copy
           *   reason: "Reuse error schema"
           *   args:
           *     from: "$.components.schemas.NotFoundError"
           *     to:
           *       - "$.paths./users/{id}.get.responses.404.content.application/json.schema"
           *       - "$.paths./posts/{id}.get.responses.404.content.application/json.schema"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the source value to copy. Must match exactly one node.
             */
            from: string;
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the destination(s). Can be a string, array of strings, or array mixing strings with matches_schema queries. If destination doesn't exist, creates it (parent must exist).
             */
            to:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'move';
          reason: string;
          /**
           * Move or rename a value from one location to another.
           *
           * If `from` and `to` share the same parent the node will be renamed. Otherwise it will be relocated.
           * Only accept a single source and destination target.
           *
           * Examples:
           * ~~~yaml
           * # Rename a schema key
           * - command: move
           *   reason: "Normalize schema name to PascalCase"
           *   args:
           *     from: "$.components.schemas.user_response"
           *     to: "$.components.schemas.UserResponse"
           *
           * # Move property between schemas
           * - command: move
           *   reason: "Relocate userId from Request to User schema"
           *   args:
           *     from: "$.components.schemas.Request.properties.userId"
           *     to: "$.components.schemas.User.properties.id"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the source value to move. Must match exactly one node.
             */
            from: string;
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the destination. The parent of the target should exist.
             */
            to: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
    )[];
    /**
     * @deprecated
     * Deprecated: use `transforms` instead.
     */
    transformations?: (
      | {
          command: 'update';
          reason: string;
          /**
           * Replace values at existing paths. Fails if path doesn't exist.
           *
           * Supports {{value}} templating for string manipulation. Supports matches_schema for finding duplicate schemas.
           *
           * Examples:
           * ~~~yaml
           * # Fix incorrect type with filter
           * - command: update
           *   reason: "Account IDs are strings"
           *   args:
           *     target: '$.paths..parameters[?(@.name == "account_id")].schema.type'
           *     value: "string"
           *
           * # String templating: append text
           * - command: update
           *   reason: "Add deprecation notice"
           *   args:
           *     target: "$.components.schemas.LegacyUser.description"
           *     value: "{{value}}\n\nDEPRECATED: Use NewUser instead"
           *     template: true
           *
           * # Schema matching: replace duplicates
           * - command: update
           *   reason: "Replace duplicate error schemas with $ref"
           *   args:
           *     target:
           *       - matches_schema: $.components.schemas.ErrorTemplate
           *         ignore: $.components.schemas.ErrorTemplate
           *     value:
           *       $ref: "#/components/schemas/Error"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the value(s) to replace. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * The new value to set at the target location(s).
             */
            value: {
              [k: string]: unknown;
            };
            /**
             * If true, enables {{value}} substitution in string values. Use for appending/prepending to strings. Example: "{{value}}\nAdditional notes" or "[BETA] {{value}}". Requires value to contain {{value}} and target to be a string.
             */
            template?: boolean;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'append';
          reason: string;
          /**
           * Add items to arrays or add new properties to objects. Attempting to add a property that already exists will fail. This alerts you when the spec is fixed.
           *
           * Use for temporary fixes. For intentional overrides, use merge.
           *
           * Examples:
           * ~~~yaml
           * # Add to array
           * - command: append
           *   reason: "Add id to required fields"
           *   args:
           *     target: "$.components.schemas.User.required"
           *     value: "id"
           *
           * # Add property (fails if exists)
           * - command: append
           *   reason: "API spec missing id field, adding temporarily"
           *   args:
           *     target: "$.components.schemas.User.properties"
           *     value:
           *       id:
           *         type: "string"
           *         format: "uuid"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to array(s) or object(s) to append to. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * For arrays: item(s) to push. For objects: properties to add (fails if property already exists).
             */
            value: {
              [k: string]: unknown;
            };
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'merge';
          reason: string;
          /**
           * Merge an object into targets. Overwrites existing properties. Uses deep merge, so nested objects are merged recursively.
           *
           * Use for permanent overrides like x-stainless-naming or x-stainless-skip. For temporary fixes, use append.
           *
           * Examples:
           * ~~~yaml
           * # Add x-stainless extension
           * - command: merge
           *   reason: "Terraform doesn't need pagination params"
           *   args:
           *     target: '$..parameters[?(@.name == "limit")]'
           *     value:
           *       schema:
           *         x-stainless-skip: ["terraform"]
           *
           * # Set language-specific naming
           * - command: merge
           *   reason: "Avoid Python parameter conflict"
           *   args:
           *     target: "$.components.schemas.Request.properties.timeout"
           *     value:
           *       x-stainless-naming:
           *         python:
           *           method_argument: "api_timeout"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to object(s) to merge into. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * The object to deep merge into the target location(s). Properties from this object will be merged into existing properties.
             */
            value: {
              [k: string]: unknown;
            };
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'remove';
          reason: string;
          /**
           * Delete nodes or specific keys from objects.
           *
           * Examples:
           * ~~~yaml
           * # Remove nodes
           * - command: remove
           *   reason: "Remove problematic examples"
           *   args:
           *     target:
           *       - "$.paths.*.*.examples"
           *       - "$.components.schemas.*.example"
           *
           * # Remove specific keys
           * - command: remove
           *   reason: "Remove incorrect default"
           *   args:
           *     target: "$.components.schemas.User.properties.role"
           *     keys: ["default"]
           *
           * # Remove with filter
           * - command: remove
           *   reason: "Remove deprecated parameters"
           *   args:
           *     target: '$.paths..parameters[?(@.deprecated == true)]'
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the node(s) to remove. Can be a string, array of strings, or array mixing strings with matches_schema queries.
             */
            target:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            /**
             * If target points to an object, remove these specific keys from the object. If not provided, removes the entire target node.
             */
            keys?: string[];
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'copy';
          reason: string;
          /**
           * Copy a value to one or more destinations. Creates destination if parent exists. The source value is deep cloned to each destination.
           *
           * Examples:
           * ~~~yaml
           * # Copy schema
           * - command: copy
           *   reason: "Duplicate User schema"
           *   args:
           *     from: "$.components.schemas.User"
           *     to: "$.components.schemas.Admin"
           *
           * # Copy to multiple destinations
           * - command: copy
           *   reason: "Reuse error schema"
           *   args:
           *     from: "$.components.schemas.NotFoundError"
           *     to:
           *       - "$.paths./users/{id}.get.responses.404.content.application/json.schema"
           *       - "$.paths./posts/{id}.get.responses.404.content.application/json.schema"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the source value to copy. Must match exactly one node.
             */
            from: string;
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the destination(s). Can be a string, array of strings, or array mixing strings with matches_schema queries. If destination doesn't exist, creates it (parent must exist).
             */
            to:
              | string
              | (
                  | string
                  | {
                      /**
                       * JSONPath to template schema. Finds all schemas that deep-equal this one.
                       */
                      matches_schema: string;
                      /**
                       * Path(s) to exclude from matching (e.g., the template itself).
                       */
                      ignore?: string | string[];
                      [k: string]: unknown;
                    }
                )[];
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
      | {
          command: 'move';
          reason: string;
          /**
           * Move or rename a value from one location to another.
           *
           * If `from` and `to` share the same parent the node will be renamed. Otherwise it will be relocated.
           * Only accept a single source and destination target.
           *
           * Examples:
           * ~~~yaml
           * # Rename a schema key
           * - command: move
           *   reason: "Normalize schema name to PascalCase"
           *   args:
           *     from: "$.components.schemas.user_response"
           *     to: "$.components.schemas.UserResponse"
           *
           * # Move property between schemas
           * - command: move
           *   reason: "Relocate userId from Request to User schema"
           *   args:
           *     from: "$.components.schemas.Request.properties.userId"
           *     to: "$.components.schemas.User.properties.id"
           * ~~~
           */
          args: {
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the source value to move. Must match exactly one node.
             */
            from: string;
            /**
             * A JSONPath Plus (https://www.npmjs.com/package/jsonpath-plus) query pointing to the destination. The parent of the target should exist.
             */
            to: string;
            [k: string]: unknown;
          };
          [k: string]: unknown;
        }
    )[];
    /**
     * Defines automatic snippet generation directly into your OpenAPI spec.
     *
     * We support the following providers: Stainless (adds `x-stainless-snippets`), [ReadMe](https://readme.com/), [x-codeSamples](https://redocly.com/docs/api-reference-docs/specification-extensions/x-code-samples/), [Bump.sh](https://bump.sh/), [GitBook](https://www.gitbook.com/), [Redocly](https://redocly.com/), and [Mintlify](https://mintlify.com/) (the latter four are aliases for `x-codeSamples`).
     *
     * See [our third-party docs integration docs](/docs/docs-integrations/#-enable-provider-compatible-code-samples-in-stainless) for more details.
     */
    code_samples?:
      | {
          /**
           * adds `x-readme.samples-languages`
           */
          readme?: boolean;
          /**
           * alias for `x-codeSamples`
           */
          redocly?: boolean;
          /**
           * alias for `x-codeSamples`
           */
          mintlify?: boolean;
          /**
           * alias for `x-codeSamples`
           */
          'bump.sh'?: boolean;
          /**
           * alias for `x-codeSamples`
           */
          gitbook?: boolean;
          /**
           * alias for `x-codeSamples`
           */
          'x-codeSamples'?: boolean;
          /**
           * adds `x-stainless-snippets`
           */
          stainless?: boolean;
          [k: string]: unknown;
        }
      | ('readme' | 'redocly' | 'mintlify' | 'bump.sh' | 'gitbook' | 'x-codeSamples' | 'stainless');
    /**
     * Controls which languages to include in generated code samples.
     *
     * By default, all languages configured in `targets` are enabled, and curl, powershell are disabled.
     */
    code_sample_languages?: {
      [k: string]: boolean;
    };
    [k: string]: unknown;
  };
  /**
   * See [our streaming configuration guide](/docs/sdks/configure/streaming) for more details.
   */
  streaming?: {
    /**
     * An array of streaming event handlers.
     *
     * This can be used to define how specific events / data messages should be handled.
     *
     * Note: the order of the events defined here will be the same order that the SDK checks each event in.
     *
     * See [our streaming configuration guide](/docs/sdks/configure/streaming) for more details.
     */
    on_event: (
      | {
          kind?: 'event';
          handle: 'break' | 'continue' | 'done' | 'yield' | 'error';
          /**
           * The SSE event type to match against. `null` indicates an event with a missing `event_type`
           */
          event_type: null | string | string[];
          /**
           * Specify a property that if present on a chunk, indicates that an error occurred
           */
          error_property?: string;
          [k: string]: unknown;
        }
      | {
          kind: 'fallthrough';
          handle: 'break' | 'continue' | 'done' | 'yield' | 'error';
          /**
           * Specify a property that if present on a chunk, indicates that an error occurred
           */
          error_property?: string;
          [k: string]: unknown;
        }
      | {
          kind?: 'data';
          handle: 'break' | 'continue' | 'done' | 'yield' | 'error';
          data_starts_with: string;
          [k: string]: unknown;
        }
    )[];
    [k: string]: unknown;
  };
  constants?: {
    /**
     * Constant values that should be exported from the root package in an SDK.  Currently only supported for Python and TypeScript SDKs.
     */
    package: {
      [k: string]: unknown;
    };
    [k: string]: unknown;
  };
  /**
   * Configures diagnostics that appear in builds and the Studio.
   */
  diagnostics?: {
    /**
     * Diagnostic types to ignore.
     */
    ignored: {
      [k: string]:
        | true
        | (
            | string
            | {
                location: string;
                reason: string;
                [k: string]: unknown;
              }
            | {
                target:
                  | 'node'
                  | 'typescript'
                  | 'python'
                  | 'go'
                  | 'java'
                  | 'kotlin'
                  | 'ruby'
                  | 'terraform'
                  | 'cli'
                  | 'php'
                  | 'csharp'
                  | 'sql'
                  | 'http'
                  | 'openapi';
                reason: string;
                [k: string]: unknown;
              }
          )[];
    };
    [k: string]: unknown;
  };
  [k: string]: unknown;
}
/**
 * Used to declare any models that should be shared across resources. Defining methods in this resource is not supported.
 */
export interface ResourceConfig {
  /**
   * Set `custom: true` to use `customer_code` content for the resource instead of generated code
   */
  custom?: boolean;
  /**
   * Configure the models--named types--defined in the resource.
   *
   * Each key in the object is the name of the model and the value
   * is either the name of a schema in `#/components/schemas` or an object with more detail.
   */
  models?: {
    /**
     * The name of a schema in `#/components/schemas/` in your OpenAPI spec
     */
    [k: string]:
      | string
      | (
          | {
              type?: 'model';
              /**
               * `openapi_uri` is a reference to the OpenAPI definition of the schema, e.g. `#/components/schemas/Card` or `#/components/schemas/CardList/items`.
               *
               * All `$ref` and indirect references to the schema this path points to will have the same canonical name and share a type definition.
               *
               * For schemas under `#/components/schemas/*`, you can specify just the last path segment instead of the full JSON path (e.g. `Card` instead of `#/components/schemas/Card`).
               *
               * If you need a more powerful way to match a schema, `openapi_uri` can also be a [JMESPath](https://jmespath.org/) or a [JSONPath-plus](https://jsonpath-plus.github.io/JSONPath/docs/ts/) query. This query should match exactly one schema, and not mutate or create new references.
               */
              openapi_uri: string;
              /**
               * Declaring a schema to be a model transforms all referentially-equal versions of that schema in the OpenAPI spec to be rendered as a reference, rather than re-declaring the definition.
               *
               * This doesn't work if the schemas you want to reference the model are not referentially the same (i.e. a `$ref` in the OpenAPI spec).
               *
               * `deduplicate` tells the Stainless generator to render the schemas listed on it to render them as a reference to the model. This is useful when you have schemas that are either exactly the same, but referentially not equal (e.g. not defined in `#/components/schemas/…` OR if the schemas are similar, but close enough that you would want them to be the same type in the generated SDK.
               */
              deduplicate?: string[];
              /**
               * Stainless won't generate models that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_generation?: boolean;
              /**
               * Stainless won't generate parameter classes that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_params_generation?: boolean;
              /**
               * The path to the model that is the param version of this response model. Results in generating utilities for converting from this response model to its param version.
               *
               * This should be in the format: `$resource.$model`.
               */
              param_model?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
          | {
              type: 'alias';
              /**
               * The path to the model this alias should point to. The model does not need to be in the same resource.
               *
               * This should be in the format: `$resource.$model`.
               */
              to: string;
              /**
               * Set the deprecation message for this model
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
        );
  };
  /**
   * Configure the methods defined in this resource.
   *
   * Each key in the object is the name of the method and the value
   * is either an endpoint (for example, `get /foo`) or an object with more detail.
   */
  methods?: {
    [k: string]:
      | string
      | (
          | {
              /**
               * Describes a method for a standard http endpoint.
               */
              type?: 'http';
              /**
               * A pair of HTTP verb & path, e.g. `get /foo`, `post /transactions/{user_id}`
               */
              endpoint: string;
              /**
               * Response property to be unwrapped so users don't have to access it themselves
               */
              unwrap_response?: string | false;
              /**
               * Provide `paginated: true` for methods whose responses are paginated. Or provide a string naming the page whose pagination should be matched.
               * If omitted, pagination configuration is automatically determined using matching pages in the configuration.
               *
               * Learn more: https://www.stainless.com/docs/configure/pagination
               */
              paginated?: boolean | string;
              /**
               * Skip the generated unit test in all languages or particular languages with a given reason.
               *
               * This will skip the test in all languages:
               *
               * ~~~yaml
               * skip_test_reason: "Requires body parameter not yet implement by test harness."
               * ~~~
               *
               * This will skip the test in a specific language (multiple languages also can be specified):
               * ~~~yaml
               * skip_test_reason:
               *   ruby: "TODO: Investigate Ruby test failure."
               * ~~~
               */
              skip_test_reason?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Parameter name to be used for the request parameters argument. Required when the request body is not an object (for example, an array).
               */
              body_param_name?: string;
              /**
               * When specified, this overrides the default behavior (to use the *last* path parameter as a positional parameter).
               *
               * If a parameter is in this list, it is generated as a positional parameter in the given order.
               * If a parameter is not in this list, it is generated as a structured/named parameter.
               */
              positional_params?: string[];
              streaming?: {
                /**
                 * The streaming protocol.
                 *
                 * Defaults to the protocol inferred from the method's response schema, or `sse` if none could be inferred.
                 */
                type?: 'sse' | 'jsonl';
                /**
                 * The params type name to use for the generated params types, for example:
                 *
                 * If given: 'create_completion_request' then the Node SDK types will be 'CreateCompletionRequestNonStreaming' & 'CreateCompletionRequestStreaming'.
                 */
                params_type_name?: string;
                /**
                 * The request parameter that indicates whether or not the response is streamed. If the response is always streamed this should be set to `null`
                 */
                param_discriminator: string | null;
                /**
                 * Name for the streaming method variant, in applicable languages.
                 */
                method_name?:
                  | string
                  | {
                      go?: string;
                      ruby?: string;
                      [k: string]: unknown;
                    };
                /**
                 * The path to the model that corresponds to the data that will be sent in each Server-Sent-Event. The model has to be defined in the same resource as this method. If not given, defaults to the same as the response schema.
                 *
                 * This should be in the format: `$resource.$model`.
                 */
                stream_event_model?: string;
                /**
                 * Whether to use the entire Server-Sent-Event, serialized as `{ "event": ..., "data": ... }`, for the `stream_event_model`.
                 */
                synthesize_event_and_data_properties?: boolean;
                /**
                 * Skip configuring streaming in certain languages
                 */
                skip?: (
                  | 'node'
                  | 'typescript'
                  | 'python'
                  | 'go'
                  | 'java'
                  | 'kotlin'
                  | 'ruby'
                  | 'terraform'
                  | 'cli'
                  | 'php'
                  | 'csharp'
                  | 'sql'
                  | 'http'
                  | 'openapi'
                )[];
                [k: string]: unknown;
              };
              /**
               * Configure the default request options for an individual method
               */
              default_request_options?: {
                timeout?: (number | string) | null;
                /**
                 * The default number of times to retry a failing request
                 */
                max_retries?: number;
                [k: string]: unknown;
              };
              docs?: {
                /**
                 * Specify the response property to reference in example snippets. Set to 'false' to disable property references, e.g. for logging the entire response.
                 */
                response_property?: false | string;
                [k: string]: unknown;
              };
              terraform?: {
                /**
                 * Define the type of method this endpoint represents.
                 * - create: handles creation of the resource. Must be a POST
                 * - read: handles reading of the resource. Must be a GET
                 * - list: handles listing of the resource. Must be a GET
                 * - update: handles updating of the resource. Must be a PUT or PATCH
                 * - delete: handles deleting the resource. Must be a DELETE
                 * - upsert: handles both updating and creating of the resource. Can be PUT or PATCH
                 * - skip: don't include this method in generation
                 */
                method?: 'create' | 'read' | 'list' | 'upsert' | 'update' | 'delete' | 'skip';
                /**
                 * Defined the name of the path param that is used as the ID of the resource.
                 */
                id_path_param?: string;
                /**
                 * Define the name of the property that represents the ID for the resource. It may exist as a path, body, or response parameter.
                 */
                id_property?: string;
                /**
                 * Specify how the update method on the resource should encode the request body.
                 * - put: The entire resource is replaced with the new resource.
                 * - json_merge_patch: The resource is updated as a JSON Merge Patch, only providing changed fields.
                 *
                 * If null, then json_merge_patch will be used for PATCH endpoints, and put otherwise.
                 */
                update_behavior?: 'put' | 'json_merge_patch';
                /**
                 * Timeout for the entire Terraform operation in milliseconds or ISO8601.
                 *
                 * This doesn't affect the request-level timeout.
                 */
                default_timeout?: number | string;
              };
              /**
               * whether to generate MCP Server tools for this endpoint
               */
              mcp?:
                | boolean
                | {
                    /**
                     * A list of tags that end-users can use to filter which resources and endpoints to include
                     */
                    tags?: string[];
                    /**
                     * Override the tool name for this endpoint
                     */
                    tool_name?: string;
                    /**
                     * Override the description for this endpoint
                     */
                    description?: string;
                    [k: string]: unknown;
                  };
              cli?: {
                /**
                 * Whether or not to generate this method as a CLI command.
                 */
                enabled?: boolean;
                /**
                 * The GJSON filter to use to filter response data into a more manageable
                 * format. This can be overridden with the `--filter` flag.
                 *
                 * For GJSON syntax, see: https://github.com/tidwall/gjson/blob/master/SYNTAX.md
                 */
                filter?: string;
                /**
                 * The default output format for this endpoint. This can be overridden with
                 * the `--format` flag when running the command.
                 */
                format?: 'auto' | 'explore' | 'json' | 'pretty' | 'raw' | 'yaml';
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'webhook_unwrap';
              /**
               * Map of webhook event types in the format [event_name]: event operation in OAS spec. If empty, it will infer the names and use all operations.
               */
              event_types?: {
                [k: string]: string;
              };
              /**
               * The name of the client option that stores the webhook key used for verifying webhooks (or null to skip verification checks).
               */
              webhook_key_opt?:
                | (
                    | string
                    | {
                        name: string;
                        /**
                         * The encoding that the webhook signing key should be provided in. Defaults to base64.
                         */
                        encoding?: 'base64' | 'utf-8';
                        [k: string]: unknown;
                      }
                  )
                | null;
              description?: string;
              summary?: string;
              /**
               * Specify the name of the field used to differentiate between webhook payloads as a `string`, or provide an [OpenAPI discriminator definition](https://swagger.io/docs/specification/v3_0/data-models/inheritance-and-polymorphism/#discriminator).
               */
              discriminator?: {
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'websocket';
              description?: string;
              summary?: string;
              path: string;
              /**
               * Used to generate helper methods for each client event in Python, e.g. `.` for `audio.input_buffer.commit` to then output `ws.audio.input_buffer.commit(*params)`
               */
              event_name_separator?: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              client_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              server_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              query_params_schema_uri?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              /**
               * Describes an 'alias' to a method in the same resource. Used mainly for renaming a method and maintaining backwards compatibility.
               */
              type: 'alias';
              /**
               * The name of the method that this alias should point to.
               *
               * This currently only supports aliasing HTTP methods within the same resource.
               */
              to: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
        );
  };
  /**
   * Add a docstring for the resource.
   */
  description?: string;
  /**
   * If `true`, don't prefix any type names with this resource or its ancestors' names.
   *
   * For example, consider an `external` resource with `customers` nested under it:
   *
   * With `standalone_api` set to `true`, Go will generate an `ExternalCustomerListParams` type in the `requests` package.
   *
   * With `standalone_api` set to `false`, Go will generate a `CustomerListParams` type in the `external_requests` package.
   */
  standalone_api?: boolean;
  /**
   * Terraform configuration options for this resource.
   */
  terraform?:
    | boolean
    | {
        /**
         * The name of the Terraform resource (prefixed by your organization name)
         */
        name?: string;
        /**
         * whether to enable a timeout property
         */
        timeouts?:
          | boolean
          | {
              /**
               * Attribute name for timeout parameter
               */
              name?: string;
              /**
               * Default timeout for the resource and subresources' Terraform operations in milliseconds or ISO8601.
               *
               * You can override this in subresources or individual methods.
               *
               * This doesn't affect the request-level timeout.
               */
              default?: number | string;
            };
        /**
         * Whether to generate Terraform resource
         */
        resource?: boolean;
        /**
         * Whether to generate Terraform data sources
         */
        data_source?:
          | boolean
          | {
              filtering: {
                /**
                 * Attribute name for the find one by filter for singular data sources
                 */
                find_one_by_property_name?: string;
              };
              response_items: {
                /**
                 * Attribute name for the result collection for list data sources (default: `items`)
                 */
                collection_property_name?: string;
                /**
                 * The maximum number of rows to fetch by default
                 */
                default_max_rows?: number;
                /**
                 * Attribute name for the maximum number of rows in the response
                 */
                max_rows_property_name?: string;
              };
            };
        /**
         * Configures whether a warning message is shown to users when they attempt to delete a resource that
         * does not have a delete endpoint.
         *
         * - warning: Does nothing upon delete, but generates a warning message before the user attempts to create or delete the resource.
         *
         * - no_warning: Does nothing upon delete, and does not generate a warning message.
         */
        unsupported_delete_behavior?: 'warning' | 'no_warning';
        /**
         * Skip tests and provide a reason
         */
        skip_tests_reason?: string;
        /**
         * The schema version for this resource. This is used by Terraform to track schema changes
         * and perform state migrations when the schema evolves over time.
         *
         * Only valid for resources, not data sources. Typically incremented by 1 for each
         * breaking schema change.
         */
        version?: number;
        [k: string]: unknown;
      };
  /**
   * If `true`, always prefix parameters and unnamed response types with resource names.
   *
   * If `false`, don't prefix parameters and unnamed response types with ancestor resource names.
   *
   * If this property isn't present in your Stainless config, the default behavior for the language will be used (e.g., Go always prefixes while Node doesn't).
   *
   * For example, consider the nested resource `/financial_accounts/balances`.  Set `use_namespace_in_type_names` to `true` to generate names like `FinancialAccountBalanceListParams`.
   *
   * Now consider the nested resource `financial_accounts/financial_transactions`.  Set `use_namespace_in_type_names` to `false` to generate names like `FinancialTransactionGetResponse` without the `FinancialAccount` prefix.
   */
  use_namespace_in_type_names?: boolean;
  /**
   * Define nested namespaces for accessing models and methods.
   *
   * For example, with a "cards" resource containing a "transactions" subresource, the
   * generated Python method invocation is `client.cards.transactions.retrieve()`.
   */
  subresources?: {
    [k: string]: ResourceConfig1;
  };
  /**
   * Set the deprecation message for this resource
   */
  deprecated?:
    | string
    | {
        [k: string]: string;
      };
  /**
   * Set to `false` to suppress code snippet documentation output.
   */
  document?: boolean;
  /**
   * whether to generate MCP Server tools for this resource
   */
  mcp?:
    | boolean
    | {
        /**
         * A list of tags that end-users can use to filter which resources and endpoints to include
         */
        tags?: string[];
        [k: string]: unknown;
      };
  /**
   * Configure the default request options for this resource.
   */
  default_request_options?: {
    headers?: {
      [k: string]: string;
    };
    [k: string]: unknown;
  };
  /**
   * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
   */
  skip?:
    | boolean
    | (
        | 'node'
        | 'typescript'
        | 'python'
        | 'go'
        | 'java'
        | 'kotlin'
        | 'ruby'
        | 'terraform'
        | 'cli'
        | 'php'
        | 'csharp'
        | 'sql'
        | 'http'
        | 'openapi'
      )[];
  /**
   * Opposite of `skip`; if set, SDK generation is only performed for this resource.
   */
  only?: (
    | 'node'
    | 'typescript'
    | 'python'
    | 'go'
    | 'java'
    | 'kotlin'
    | 'ruby'
    | 'terraform'
    | 'cli'
    | 'php'
    | 'csharp'
    | 'sql'
    | 'http'
    | 'openapi'
  )[];
  [k: string]: unknown;
}
export interface ResourceConfig1 {
  /**
   * Set `custom: true` to use `customer_code` content for the resource instead of generated code
   */
  custom?: boolean;
  /**
   * Configure the models--named types--defined in the resource.
   *
   * Each key in the object is the name of the model and the value
   * is either the name of a schema in `#/components/schemas` or an object with more detail.
   */
  models?: {
    /**
     * The name of a schema in `#/components/schemas/` in your OpenAPI spec
     */
    [k: string]:
      | string
      | (
          | {
              type?: 'model';
              /**
               * `openapi_uri` is a reference to the OpenAPI definition of the schema, e.g. `#/components/schemas/Card` or `#/components/schemas/CardList/items`.
               *
               * All `$ref` and indirect references to the schema this path points to will have the same canonical name and share a type definition.
               *
               * For schemas under `#/components/schemas/*`, you can specify just the last path segment instead of the full JSON path (e.g. `Card` instead of `#/components/schemas/Card`).
               *
               * If you need a more powerful way to match a schema, `openapi_uri` can also be a [JMESPath](https://jmespath.org/) or a [JSONPath-plus](https://jsonpath-plus.github.io/JSONPath/docs/ts/) query. This query should match exactly one schema, and not mutate or create new references.
               */
              openapi_uri: string;
              /**
               * Declaring a schema to be a model transforms all referentially-equal versions of that schema in the OpenAPI spec to be rendered as a reference, rather than re-declaring the definition.
               *
               * This doesn't work if the schemas you want to reference the model are not referentially the same (i.e. a `$ref` in the OpenAPI spec).
               *
               * `deduplicate` tells the Stainless generator to render the schemas listed on it to render them as a reference to the model. This is useful when you have schemas that are either exactly the same, but referentially not equal (e.g. not defined in `#/components/schemas/…` OR if the schemas are similar, but close enough that you would want them to be the same type in the generated SDK.
               */
              deduplicate?: string[];
              /**
               * Stainless won't generate models that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_generation?: boolean;
              /**
               * Stainless won't generate parameter classes that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_params_generation?: boolean;
              /**
               * The path to the model that is the param version of this response model. Results in generating utilities for converting from this response model to its param version.
               *
               * This should be in the format: `$resource.$model`.
               */
              param_model?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
          | {
              type: 'alias';
              /**
               * The path to the model this alias should point to. The model does not need to be in the same resource.
               *
               * This should be in the format: `$resource.$model`.
               */
              to: string;
              /**
               * Set the deprecation message for this model
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
        );
  };
  /**
   * Configure the methods defined in this resource.
   *
   * Each key in the object is the name of the method and the value
   * is either an endpoint (for example, `get /foo`) or an object with more detail.
   */
  methods?: {
    [k: string]:
      | string
      | (
          | {
              /**
               * Describes a method for a standard http endpoint.
               */
              type?: 'http';
              /**
               * A pair of HTTP verb & path, e.g. `get /foo`, `post /transactions/{user_id}`
               */
              endpoint: string;
              /**
               * Response property to be unwrapped so users don't have to access it themselves
               */
              unwrap_response?: string | false;
              /**
               * Provide `paginated: true` for methods whose responses are paginated. Or provide a string naming the page whose pagination should be matched.
               * If omitted, pagination configuration is automatically determined using matching pages in the configuration.
               *
               * Learn more: https://www.stainless.com/docs/configure/pagination
               */
              paginated?: boolean | string;
              /**
               * Skip the generated unit test in all languages or particular languages with a given reason.
               *
               * This will skip the test in all languages:
               *
               * ~~~yaml
               * skip_test_reason: "Requires body parameter not yet implement by test harness."
               * ~~~
               *
               * This will skip the test in a specific language (multiple languages also can be specified):
               * ~~~yaml
               * skip_test_reason:
               *   ruby: "TODO: Investigate Ruby test failure."
               * ~~~
               */
              skip_test_reason?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Parameter name to be used for the request parameters argument. Required when the request body is not an object (for example, an array).
               */
              body_param_name?: string;
              /**
               * When specified, this overrides the default behavior (to use the *last* path parameter as a positional parameter).
               *
               * If a parameter is in this list, it is generated as a positional parameter in the given order.
               * If a parameter is not in this list, it is generated as a structured/named parameter.
               */
              positional_params?: string[];
              streaming?: {
                /**
                 * The streaming protocol.
                 *
                 * Defaults to the protocol inferred from the method's response schema, or `sse` if none could be inferred.
                 */
                type?: 'sse' | 'jsonl';
                /**
                 * The params type name to use for the generated params types, for example:
                 *
                 * If given: 'create_completion_request' then the Node SDK types will be 'CreateCompletionRequestNonStreaming' & 'CreateCompletionRequestStreaming'.
                 */
                params_type_name?: string;
                /**
                 * The request parameter that indicates whether or not the response is streamed. If the response is always streamed this should be set to `null`
                 */
                param_discriminator: string | null;
                /**
                 * Name for the streaming method variant, in applicable languages.
                 */
                method_name?:
                  | string
                  | {
                      go?: string;
                      ruby?: string;
                      [k: string]: unknown;
                    };
                /**
                 * The path to the model that corresponds to the data that will be sent in each Server-Sent-Event. The model has to be defined in the same resource as this method. If not given, defaults to the same as the response schema.
                 *
                 * This should be in the format: `$resource.$model`.
                 */
                stream_event_model?: string;
                /**
                 * Whether to use the entire Server-Sent-Event, serialized as `{ "event": ..., "data": ... }`, for the `stream_event_model`.
                 */
                synthesize_event_and_data_properties?: boolean;
                /**
                 * Skip configuring streaming in certain languages
                 */
                skip?: (
                  | 'node'
                  | 'typescript'
                  | 'python'
                  | 'go'
                  | 'java'
                  | 'kotlin'
                  | 'ruby'
                  | 'terraform'
                  | 'cli'
                  | 'php'
                  | 'csharp'
                  | 'sql'
                  | 'http'
                  | 'openapi'
                )[];
                [k: string]: unknown;
              };
              /**
               * Configure the default request options for an individual method
               */
              default_request_options?: {
                timeout?: (number | string) | null;
                /**
                 * The default number of times to retry a failing request
                 */
                max_retries?: number;
                [k: string]: unknown;
              };
              docs?: {
                /**
                 * Specify the response property to reference in example snippets. Set to 'false' to disable property references, e.g. for logging the entire response.
                 */
                response_property?: false | string;
                [k: string]: unknown;
              };
              terraform?: {
                /**
                 * Define the type of method this endpoint represents.
                 * - create: handles creation of the resource. Must be a POST
                 * - read: handles reading of the resource. Must be a GET
                 * - list: handles listing of the resource. Must be a GET
                 * - update: handles updating of the resource. Must be a PUT or PATCH
                 * - delete: handles deleting the resource. Must be a DELETE
                 * - upsert: handles both updating and creating of the resource. Can be PUT or PATCH
                 * - skip: don't include this method in generation
                 */
                method?: 'create' | 'read' | 'list' | 'upsert' | 'update' | 'delete' | 'skip';
                /**
                 * Defined the name of the path param that is used as the ID of the resource.
                 */
                id_path_param?: string;
                /**
                 * Define the name of the property that represents the ID for the resource. It may exist as a path, body, or response parameter.
                 */
                id_property?: string;
                /**
                 * Specify how the update method on the resource should encode the request body.
                 * - put: The entire resource is replaced with the new resource.
                 * - json_merge_patch: The resource is updated as a JSON Merge Patch, only providing changed fields.
                 *
                 * If null, then json_merge_patch will be used for PATCH endpoints, and put otherwise.
                 */
                update_behavior?: 'put' | 'json_merge_patch';
                /**
                 * Timeout for the entire Terraform operation in milliseconds or ISO8601.
                 *
                 * This doesn't affect the request-level timeout.
                 */
                default_timeout?: number | string;
              };
              /**
               * whether to generate MCP Server tools for this endpoint
               */
              mcp?:
                | boolean
                | {
                    /**
                     * A list of tags that end-users can use to filter which resources and endpoints to include
                     */
                    tags?: string[];
                    /**
                     * Override the tool name for this endpoint
                     */
                    tool_name?: string;
                    /**
                     * Override the description for this endpoint
                     */
                    description?: string;
                    [k: string]: unknown;
                  };
              cli?: {
                /**
                 * Whether or not to generate this method as a CLI command.
                 */
                enabled?: boolean;
                /**
                 * The GJSON filter to use to filter response data into a more manageable
                 * format. This can be overridden with the `--filter` flag.
                 *
                 * For GJSON syntax, see: https://github.com/tidwall/gjson/blob/master/SYNTAX.md
                 */
                filter?: string;
                /**
                 * The default output format for this endpoint. This can be overridden with
                 * the `--format` flag when running the command.
                 */
                format?: 'auto' | 'explore' | 'json' | 'pretty' | 'raw' | 'yaml';
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'webhook_unwrap';
              /**
               * Map of webhook event types in the format [event_name]: event operation in OAS spec. If empty, it will infer the names and use all operations.
               */
              event_types?: {
                [k: string]: string;
              };
              /**
               * The name of the client option that stores the webhook key used for verifying webhooks (or null to skip verification checks).
               */
              webhook_key_opt?:
                | (
                    | string
                    | {
                        name: string;
                        /**
                         * The encoding that the webhook signing key should be provided in. Defaults to base64.
                         */
                        encoding?: 'base64' | 'utf-8';
                        [k: string]: unknown;
                      }
                  )
                | null;
              description?: string;
              summary?: string;
              /**
               * Specify the name of the field used to differentiate between webhook payloads as a `string`, or provide an [OpenAPI discriminator definition](https://swagger.io/docs/specification/v3_0/data-models/inheritance-and-polymorphism/#discriminator).
               */
              discriminator?: {
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'websocket';
              description?: string;
              summary?: string;
              path: string;
              /**
               * Used to generate helper methods for each client event in Python, e.g. `.` for `audio.input_buffer.commit` to then output `ws.audio.input_buffer.commit(*params)`
               */
              event_name_separator?: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              client_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              server_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              query_params_schema_uri?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              /**
               * Describes an 'alias' to a method in the same resource. Used mainly for renaming a method and maintaining backwards compatibility.
               */
              type: 'alias';
              /**
               * The name of the method that this alias should point to.
               *
               * This currently only supports aliasing HTTP methods within the same resource.
               */
              to: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
        );
  };
  /**
   * Add a docstring for the resource.
   */
  description?: string;
  /**
   * If `true`, don't prefix any type names with this resource or its ancestors' names.
   *
   * For example, consider an `external` resource with `customers` nested under it:
   *
   * With `standalone_api` set to `true`, Go will generate an `ExternalCustomerListParams` type in the `requests` package.
   *
   * With `standalone_api` set to `false`, Go will generate a `CustomerListParams` type in the `external_requests` package.
   */
  standalone_api?: boolean;
  /**
   * Terraform configuration options for this resource.
   */
  terraform?:
    | boolean
    | {
        /**
         * The name of the Terraform resource (prefixed by your organization name)
         */
        name?: string;
        /**
         * whether to enable a timeout property
         */
        timeouts?:
          | boolean
          | {
              /**
               * Attribute name for timeout parameter
               */
              name?: string;
              /**
               * Default timeout for the resource and subresources' Terraform operations in milliseconds or ISO8601.
               *
               * You can override this in subresources or individual methods.
               *
               * This doesn't affect the request-level timeout.
               */
              default?: number | string;
            };
        /**
         * Whether to generate Terraform resource
         */
        resource?: boolean;
        /**
         * Whether to generate Terraform data sources
         */
        data_source?:
          | boolean
          | {
              filtering: {
                /**
                 * Attribute name for the find one by filter for singular data sources
                 */
                find_one_by_property_name?: string;
              };
              response_items: {
                /**
                 * Attribute name for the result collection for list data sources (default: `items`)
                 */
                collection_property_name?: string;
                /**
                 * The maximum number of rows to fetch by default
                 */
                default_max_rows?: number;
                /**
                 * Attribute name for the maximum number of rows in the response
                 */
                max_rows_property_name?: string;
              };
            };
        /**
         * Configures whether a warning message is shown to users when they attempt to delete a resource that
         * does not have a delete endpoint.
         *
         * - warning: Does nothing upon delete, but generates a warning message before the user attempts to create or delete the resource.
         *
         * - no_warning: Does nothing upon delete, and does not generate a warning message.
         */
        unsupported_delete_behavior?: 'warning' | 'no_warning';
        /**
         * Skip tests and provide a reason
         */
        skip_tests_reason?: string;
        /**
         * The schema version for this resource. This is used by Terraform to track schema changes
         * and perform state migrations when the schema evolves over time.
         *
         * Only valid for resources, not data sources. Typically incremented by 1 for each
         * breaking schema change.
         */
        version?: number;
        [k: string]: unknown;
      };
  /**
   * If `true`, always prefix parameters and unnamed response types with resource names.
   *
   * If `false`, don't prefix parameters and unnamed response types with ancestor resource names.
   *
   * If this property isn't present in your Stainless config, the default behavior for the language will be used (e.g., Go always prefixes while Node doesn't).
   *
   * For example, consider the nested resource `/financial_accounts/balances`.  Set `use_namespace_in_type_names` to `true` to generate names like `FinancialAccountBalanceListParams`.
   *
   * Now consider the nested resource `financial_accounts/financial_transactions`.  Set `use_namespace_in_type_names` to `false` to generate names like `FinancialTransactionGetResponse` without the `FinancialAccount` prefix.
   */
  use_namespace_in_type_names?: boolean;
  /**
   * Define nested namespaces for accessing models and methods.
   *
   * For example, with a "cards" resource containing a "transactions" subresource, the
   * generated Python method invocation is `client.cards.transactions.retrieve()`.
   */
  subresources?: {
    [k: string]: ResourceConfig1;
  };
  /**
   * Set the deprecation message for this resource
   */
  deprecated?:
    | string
    | {
        [k: string]: string;
      };
  /**
   * Set to `false` to suppress code snippet documentation output.
   */
  document?: boolean;
  /**
   * whether to generate MCP Server tools for this resource
   */
  mcp?:
    | boolean
    | {
        /**
         * A list of tags that end-users can use to filter which resources and endpoints to include
         */
        tags?: string[];
        [k: string]: unknown;
      };
  /**
   * Configure the default request options for this resource.
   */
  default_request_options?: {
    headers?: {
      [k: string]: string;
    };
    [k: string]: unknown;
  };
  /**
   * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
   */
  skip?:
    | boolean
    | (
        | 'node'
        | 'typescript'
        | 'python'
        | 'go'
        | 'java'
        | 'kotlin'
        | 'ruby'
        | 'terraform'
        | 'cli'
        | 'php'
        | 'csharp'
        | 'sql'
        | 'http'
        | 'openapi'
      )[];
  /**
   * Opposite of `skip`; if set, SDK generation is only performed for this resource.
   */
  only?: (
    | 'node'
    | 'typescript'
    | 'python'
    | 'go'
    | 'java'
    | 'kotlin'
    | 'ruby'
    | 'terraform'
    | 'cli'
    | 'php'
    | 'csharp'
    | 'sql'
    | 'http'
    | 'openapi'
  )[];
  [k: string]: unknown;
}
/**
 * Used to define methods and resources at the client level, e.g. for an API status endpoint
 */
export interface ResourceConfig2 {
  /**
   * Set `custom: true` to use `customer_code` content for the resource instead of generated code
   */
  custom?: boolean;
  /**
   * Configure the models--named types--defined in the resource.
   *
   * Each key in the object is the name of the model and the value
   * is either the name of a schema in `#/components/schemas` or an object with more detail.
   */
  models?: {
    /**
     * The name of a schema in `#/components/schemas/` in your OpenAPI spec
     */
    [k: string]:
      | string
      | (
          | {
              type?: 'model';
              /**
               * `openapi_uri` is a reference to the OpenAPI definition of the schema, e.g. `#/components/schemas/Card` or `#/components/schemas/CardList/items`.
               *
               * All `$ref` and indirect references to the schema this path points to will have the same canonical name and share a type definition.
               *
               * For schemas under `#/components/schemas/*`, you can specify just the last path segment instead of the full JSON path (e.g. `Card` instead of `#/components/schemas/Card`).
               *
               * If you need a more powerful way to match a schema, `openapi_uri` can also be a [JMESPath](https://jmespath.org/) or a [JSONPath-plus](https://jsonpath-plus.github.io/JSONPath/docs/ts/) query. This query should match exactly one schema, and not mutate or create new references.
               */
              openapi_uri: string;
              /**
               * Declaring a schema to be a model transforms all referentially-equal versions of that schema in the OpenAPI spec to be rendered as a reference, rather than re-declaring the definition.
               *
               * This doesn't work if the schemas you want to reference the model are not referentially the same (i.e. a `$ref` in the OpenAPI spec).
               *
               * `deduplicate` tells the Stainless generator to render the schemas listed on it to render them as a reference to the model. This is useful when you have schemas that are either exactly the same, but referentially not equal (e.g. not defined in `#/components/schemas/…` OR if the schemas are similar, but close enough that you would want them to be the same type in the generated SDK.
               */
              deduplicate?: string[];
              /**
               * Stainless won't generate models that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_generation?: boolean;
              /**
               * Stainless won't generate parameter classes that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_params_generation?: boolean;
              /**
               * The path to the model that is the param version of this response model. Results in generating utilities for converting from this response model to its param version.
               *
               * This should be in the format: `$resource.$model`.
               */
              param_model?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
          | {
              type: 'alias';
              /**
               * The path to the model this alias should point to. The model does not need to be in the same resource.
               *
               * This should be in the format: `$resource.$model`.
               */
              to: string;
              /**
               * Set the deprecation message for this model
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
        );
  };
  /**
   * Configure the methods defined in this resource.
   *
   * Each key in the object is the name of the method and the value
   * is either an endpoint (for example, `get /foo`) or an object with more detail.
   */
  methods?: {
    [k: string]:
      | string
      | (
          | {
              /**
               * Describes a method for a standard http endpoint.
               */
              type?: 'http';
              /**
               * A pair of HTTP verb & path, e.g. `get /foo`, `post /transactions/{user_id}`
               */
              endpoint: string;
              /**
               * Response property to be unwrapped so users don't have to access it themselves
               */
              unwrap_response?: string | false;
              /**
               * Provide `paginated: true` for methods whose responses are paginated. Or provide a string naming the page whose pagination should be matched.
               * If omitted, pagination configuration is automatically determined using matching pages in the configuration.
               *
               * Learn more: https://www.stainless.com/docs/configure/pagination
               */
              paginated?: boolean | string;
              /**
               * Skip the generated unit test in all languages or particular languages with a given reason.
               *
               * This will skip the test in all languages:
               *
               * ~~~yaml
               * skip_test_reason: "Requires body parameter not yet implement by test harness."
               * ~~~
               *
               * This will skip the test in a specific language (multiple languages also can be specified):
               * ~~~yaml
               * skip_test_reason:
               *   ruby: "TODO: Investigate Ruby test failure."
               * ~~~
               */
              skip_test_reason?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Parameter name to be used for the request parameters argument. Required when the request body is not an object (for example, an array).
               */
              body_param_name?: string;
              /**
               * When specified, this overrides the default behavior (to use the *last* path parameter as a positional parameter).
               *
               * If a parameter is in this list, it is generated as a positional parameter in the given order.
               * If a parameter is not in this list, it is generated as a structured/named parameter.
               */
              positional_params?: string[];
              streaming?: {
                /**
                 * The streaming protocol.
                 *
                 * Defaults to the protocol inferred from the method's response schema, or `sse` if none could be inferred.
                 */
                type?: 'sse' | 'jsonl';
                /**
                 * The params type name to use for the generated params types, for example:
                 *
                 * If given: 'create_completion_request' then the Node SDK types will be 'CreateCompletionRequestNonStreaming' & 'CreateCompletionRequestStreaming'.
                 */
                params_type_name?: string;
                /**
                 * The request parameter that indicates whether or not the response is streamed. If the response is always streamed this should be set to `null`
                 */
                param_discriminator: string | null;
                /**
                 * Name for the streaming method variant, in applicable languages.
                 */
                method_name?:
                  | string
                  | {
                      go?: string;
                      ruby?: string;
                      [k: string]: unknown;
                    };
                /**
                 * The path to the model that corresponds to the data that will be sent in each Server-Sent-Event. The model has to be defined in the same resource as this method. If not given, defaults to the same as the response schema.
                 *
                 * This should be in the format: `$resource.$model`.
                 */
                stream_event_model?: string;
                /**
                 * Whether to use the entire Server-Sent-Event, serialized as `{ "event": ..., "data": ... }`, for the `stream_event_model`.
                 */
                synthesize_event_and_data_properties?: boolean;
                /**
                 * Skip configuring streaming in certain languages
                 */
                skip?: (
                  | 'node'
                  | 'typescript'
                  | 'python'
                  | 'go'
                  | 'java'
                  | 'kotlin'
                  | 'ruby'
                  | 'terraform'
                  | 'cli'
                  | 'php'
                  | 'csharp'
                  | 'sql'
                  | 'http'
                  | 'openapi'
                )[];
                [k: string]: unknown;
              };
              /**
               * Configure the default request options for an individual method
               */
              default_request_options?: {
                timeout?: (number | string) | null;
                /**
                 * The default number of times to retry a failing request
                 */
                max_retries?: number;
                [k: string]: unknown;
              };
              docs?: {
                /**
                 * Specify the response property to reference in example snippets. Set to 'false' to disable property references, e.g. for logging the entire response.
                 */
                response_property?: false | string;
                [k: string]: unknown;
              };
              terraform?: {
                /**
                 * Define the type of method this endpoint represents.
                 * - create: handles creation of the resource. Must be a POST
                 * - read: handles reading of the resource. Must be a GET
                 * - list: handles listing of the resource. Must be a GET
                 * - update: handles updating of the resource. Must be a PUT or PATCH
                 * - delete: handles deleting the resource. Must be a DELETE
                 * - upsert: handles both updating and creating of the resource. Can be PUT or PATCH
                 * - skip: don't include this method in generation
                 */
                method?: 'create' | 'read' | 'list' | 'upsert' | 'update' | 'delete' | 'skip';
                /**
                 * Defined the name of the path param that is used as the ID of the resource.
                 */
                id_path_param?: string;
                /**
                 * Define the name of the property that represents the ID for the resource. It may exist as a path, body, or response parameter.
                 */
                id_property?: string;
                /**
                 * Specify how the update method on the resource should encode the request body.
                 * - put: The entire resource is replaced with the new resource.
                 * - json_merge_patch: The resource is updated as a JSON Merge Patch, only providing changed fields.
                 *
                 * If null, then json_merge_patch will be used for PATCH endpoints, and put otherwise.
                 */
                update_behavior?: 'put' | 'json_merge_patch';
                /**
                 * Timeout for the entire Terraform operation in milliseconds or ISO8601.
                 *
                 * This doesn't affect the request-level timeout.
                 */
                default_timeout?: number | string;
              };
              /**
               * whether to generate MCP Server tools for this endpoint
               */
              mcp?:
                | boolean
                | {
                    /**
                     * A list of tags that end-users can use to filter which resources and endpoints to include
                     */
                    tags?: string[];
                    /**
                     * Override the tool name for this endpoint
                     */
                    tool_name?: string;
                    /**
                     * Override the description for this endpoint
                     */
                    description?: string;
                    [k: string]: unknown;
                  };
              cli?: {
                /**
                 * Whether or not to generate this method as a CLI command.
                 */
                enabled?: boolean;
                /**
                 * The GJSON filter to use to filter response data into a more manageable
                 * format. This can be overridden with the `--filter` flag.
                 *
                 * For GJSON syntax, see: https://github.com/tidwall/gjson/blob/master/SYNTAX.md
                 */
                filter?: string;
                /**
                 * The default output format for this endpoint. This can be overridden with
                 * the `--format` flag when running the command.
                 */
                format?: 'auto' | 'explore' | 'json' | 'pretty' | 'raw' | 'yaml';
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'webhook_unwrap';
              /**
               * Map of webhook event types in the format [event_name]: event operation in OAS spec. If empty, it will infer the names and use all operations.
               */
              event_types?: {
                [k: string]: string;
              };
              /**
               * The name of the client option that stores the webhook key used for verifying webhooks (or null to skip verification checks).
               */
              webhook_key_opt?:
                | (
                    | string
                    | {
                        name: string;
                        /**
                         * The encoding that the webhook signing key should be provided in. Defaults to base64.
                         */
                        encoding?: 'base64' | 'utf-8';
                        [k: string]: unknown;
                      }
                  )
                | null;
              description?: string;
              summary?: string;
              /**
               * Specify the name of the field used to differentiate between webhook payloads as a `string`, or provide an [OpenAPI discriminator definition](https://swagger.io/docs/specification/v3_0/data-models/inheritance-and-polymorphism/#discriminator).
               */
              discriminator?: {
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'websocket';
              description?: string;
              summary?: string;
              path: string;
              /**
               * Used to generate helper methods for each client event in Python, e.g. `.` for `audio.input_buffer.commit` to then output `ws.audio.input_buffer.commit(*params)`
               */
              event_name_separator?: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              client_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              server_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              query_params_schema_uri?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              /**
               * Describes an 'alias' to a method in the same resource. Used mainly for renaming a method and maintaining backwards compatibility.
               */
              type: 'alias';
              /**
               * The name of the method that this alias should point to.
               *
               * This currently only supports aliasing HTTP methods within the same resource.
               */
              to: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
        );
  };
  /**
   * Add a docstring for the resource.
   */
  description?: string;
  /**
   * If `true`, don't prefix any type names with this resource or its ancestors' names.
   *
   * For example, consider an `external` resource with `customers` nested under it:
   *
   * With `standalone_api` set to `true`, Go will generate an `ExternalCustomerListParams` type in the `requests` package.
   *
   * With `standalone_api` set to `false`, Go will generate a `CustomerListParams` type in the `external_requests` package.
   */
  standalone_api?: boolean;
  /**
   * Terraform configuration options for this resource.
   */
  terraform?:
    | boolean
    | {
        /**
         * The name of the Terraform resource (prefixed by your organization name)
         */
        name?: string;
        /**
         * whether to enable a timeout property
         */
        timeouts?:
          | boolean
          | {
              /**
               * Attribute name for timeout parameter
               */
              name?: string;
              /**
               * Default timeout for the resource and subresources' Terraform operations in milliseconds or ISO8601.
               *
               * You can override this in subresources or individual methods.
               *
               * This doesn't affect the request-level timeout.
               */
              default?: number | string;
            };
        /**
         * Whether to generate Terraform resource
         */
        resource?: boolean;
        /**
         * Whether to generate Terraform data sources
         */
        data_source?:
          | boolean
          | {
              filtering: {
                /**
                 * Attribute name for the find one by filter for singular data sources
                 */
                find_one_by_property_name?: string;
              };
              response_items: {
                /**
                 * Attribute name for the result collection for list data sources (default: `items`)
                 */
                collection_property_name?: string;
                /**
                 * The maximum number of rows to fetch by default
                 */
                default_max_rows?: number;
                /**
                 * Attribute name for the maximum number of rows in the response
                 */
                max_rows_property_name?: string;
              };
            };
        /**
         * Configures whether a warning message is shown to users when they attempt to delete a resource that
         * does not have a delete endpoint.
         *
         * - warning: Does nothing upon delete, but generates a warning message before the user attempts to create or delete the resource.
         *
         * - no_warning: Does nothing upon delete, and does not generate a warning message.
         */
        unsupported_delete_behavior?: 'warning' | 'no_warning';
        /**
         * Skip tests and provide a reason
         */
        skip_tests_reason?: string;
        /**
         * The schema version for this resource. This is used by Terraform to track schema changes
         * and perform state migrations when the schema evolves over time.
         *
         * Only valid for resources, not data sources. Typically incremented by 1 for each
         * breaking schema change.
         */
        version?: number;
        [k: string]: unknown;
      };
  /**
   * If `true`, always prefix parameters and unnamed response types with resource names.
   *
   * If `false`, don't prefix parameters and unnamed response types with ancestor resource names.
   *
   * If this property isn't present in your Stainless config, the default behavior for the language will be used (e.g., Go always prefixes while Node doesn't).
   *
   * For example, consider the nested resource `/financial_accounts/balances`.  Set `use_namespace_in_type_names` to `true` to generate names like `FinancialAccountBalanceListParams`.
   *
   * Now consider the nested resource `financial_accounts/financial_transactions`.  Set `use_namespace_in_type_names` to `false` to generate names like `FinancialTransactionGetResponse` without the `FinancialAccount` prefix.
   */
  use_namespace_in_type_names?: boolean;
  /**
   * Define nested namespaces for accessing models and methods.
   *
   * For example, with a "cards" resource containing a "transactions" subresource, the
   * generated Python method invocation is `client.cards.transactions.retrieve()`.
   */
  subresources?: {
    [k: string]: ResourceConfig1;
  };
  /**
   * Set the deprecation message for this resource
   */
  deprecated?:
    | string
    | {
        [k: string]: string;
      };
  /**
   * Set to `false` to suppress code snippet documentation output.
   */
  document?: boolean;
  /**
   * whether to generate MCP Server tools for this resource
   */
  mcp?:
    | boolean
    | {
        /**
         * A list of tags that end-users can use to filter which resources and endpoints to include
         */
        tags?: string[];
        [k: string]: unknown;
      };
  /**
   * Configure the default request options for this resource.
   */
  default_request_options?: {
    headers?: {
      [k: string]: string;
    };
    [k: string]: unknown;
  };
  /**
   * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
   */
  skip?:
    | boolean
    | (
        | 'node'
        | 'typescript'
        | 'python'
        | 'go'
        | 'java'
        | 'kotlin'
        | 'ruby'
        | 'terraform'
        | 'cli'
        | 'php'
        | 'csharp'
        | 'sql'
        | 'http'
        | 'openapi'
      )[];
  /**
   * Opposite of `skip`; if set, SDK generation is only performed for this resource.
   */
  only?: (
    | 'node'
    | 'typescript'
    | 'python'
    | 'go'
    | 'java'
    | 'kotlin'
    | 'ruby'
    | 'terraform'
    | 'cli'
    | 'php'
    | 'csharp'
    | 'sql'
    | 'http'
    | 'openapi'
  )[];
  [k: string]: unknown;
}
/**
 * Defines a resource and models for your API: https://www.stainless.com/docs/guides/configure#resources
 */
export interface ResourceConfig3 {
  /**
   * Set `custom: true` to use `customer_code` content for the resource instead of generated code
   */
  custom?: boolean;
  /**
   * Configure the models--named types--defined in the resource.
   *
   * Each key in the object is the name of the model and the value
   * is either the name of a schema in `#/components/schemas` or an object with more detail.
   */
  models?: {
    /**
     * The name of a schema in `#/components/schemas/` in your OpenAPI spec
     */
    [k: string]:
      | string
      | (
          | {
              type?: 'model';
              /**
               * `openapi_uri` is a reference to the OpenAPI definition of the schema, e.g. `#/components/schemas/Card` or `#/components/schemas/CardList/items`.
               *
               * All `$ref` and indirect references to the schema this path points to will have the same canonical name and share a type definition.
               *
               * For schemas under `#/components/schemas/*`, you can specify just the last path segment instead of the full JSON path (e.g. `Card` instead of `#/components/schemas/Card`).
               *
               * If you need a more powerful way to match a schema, `openapi_uri` can also be a [JMESPath](https://jmespath.org/) or a [JSONPath-plus](https://jsonpath-plus.github.io/JSONPath/docs/ts/) query. This query should match exactly one schema, and not mutate or create new references.
               */
              openapi_uri: string;
              /**
               * Declaring a schema to be a model transforms all referentially-equal versions of that schema in the OpenAPI spec to be rendered as a reference, rather than re-declaring the definition.
               *
               * This doesn't work if the schemas you want to reference the model are not referentially the same (i.e. a `$ref` in the OpenAPI spec).
               *
               * `deduplicate` tells the Stainless generator to render the schemas listed on it to render them as a reference to the model. This is useful when you have schemas that are either exactly the same, but referentially not equal (e.g. not defined in `#/components/schemas/…` OR if the schemas are similar, but close enough that you would want them to be the same type in the generated SDK.
               */
              deduplicate?: string[];
              /**
               * Stainless won't generate models that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_generation?: boolean;
              /**
               * Stainless won't generate parameter classes that aren't used in an endpoint.  Set this to `true` to generate this model even if it's not used.
               */
              force_params_generation?: boolean;
              /**
               * The path to the model that is the param version of this response model. Results in generating utilities for converting from this response model to its param version.
               *
               * This should be in the format: `$resource.$model`.
               */
              param_model?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
          | {
              type: 'alias';
              /**
               * The path to the model this alias should point to. The model does not need to be in the same resource.
               *
               * This should be in the format: `$resource.$model`.
               */
              to: string;
              /**
               * Set the deprecation message for this model
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              [k: string]: unknown;
            }
        );
  };
  /**
   * Configure the methods defined in this resource.
   *
   * Each key in the object is the name of the method and the value
   * is either an endpoint (for example, `get /foo`) or an object with more detail.
   */
  methods?: {
    [k: string]:
      | string
      | (
          | {
              /**
               * Describes a method for a standard http endpoint.
               */
              type?: 'http';
              /**
               * A pair of HTTP verb & path, e.g. `get /foo`, `post /transactions/{user_id}`
               */
              endpoint: string;
              /**
               * Response property to be unwrapped so users don't have to access it themselves
               */
              unwrap_response?: string | false;
              /**
               * Provide `paginated: true` for methods whose responses are paginated. Or provide a string naming the page whose pagination should be matched.
               * If omitted, pagination configuration is automatically determined using matching pages in the configuration.
               *
               * Learn more: https://www.stainless.com/docs/configure/pagination
               */
              paginated?: boolean | string;
              /**
               * Skip the generated unit test in all languages or particular languages with a given reason.
               *
               * This will skip the test in all languages:
               *
               * ~~~yaml
               * skip_test_reason: "Requires body parameter not yet implement by test harness."
               * ~~~
               *
               * This will skip the test in a specific language (multiple languages also can be specified):
               * ~~~yaml
               * skip_test_reason:
               *   ruby: "TODO: Investigate Ruby test failure."
               * ~~~
               */
              skip_test_reason?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Parameter name to be used for the request parameters argument. Required when the request body is not an object (for example, an array).
               */
              body_param_name?: string;
              /**
               * When specified, this overrides the default behavior (to use the *last* path parameter as a positional parameter).
               *
               * If a parameter is in this list, it is generated as a positional parameter in the given order.
               * If a parameter is not in this list, it is generated as a structured/named parameter.
               */
              positional_params?: string[];
              streaming?: {
                /**
                 * The streaming protocol.
                 *
                 * Defaults to the protocol inferred from the method's response schema, or `sse` if none could be inferred.
                 */
                type?: 'sse' | 'jsonl';
                /**
                 * The params type name to use for the generated params types, for example:
                 *
                 * If given: 'create_completion_request' then the Node SDK types will be 'CreateCompletionRequestNonStreaming' & 'CreateCompletionRequestStreaming'.
                 */
                params_type_name?: string;
                /**
                 * The request parameter that indicates whether or not the response is streamed. If the response is always streamed this should be set to `null`
                 */
                param_discriminator: string | null;
                /**
                 * Name for the streaming method variant, in applicable languages.
                 */
                method_name?:
                  | string
                  | {
                      go?: string;
                      ruby?: string;
                      [k: string]: unknown;
                    };
                /**
                 * The path to the model that corresponds to the data that will be sent in each Server-Sent-Event. The model has to be defined in the same resource as this method. If not given, defaults to the same as the response schema.
                 *
                 * This should be in the format: `$resource.$model`.
                 */
                stream_event_model?: string;
                /**
                 * Whether to use the entire Server-Sent-Event, serialized as `{ "event": ..., "data": ... }`, for the `stream_event_model`.
                 */
                synthesize_event_and_data_properties?: boolean;
                /**
                 * Skip configuring streaming in certain languages
                 */
                skip?: (
                  | 'node'
                  | 'typescript'
                  | 'python'
                  | 'go'
                  | 'java'
                  | 'kotlin'
                  | 'ruby'
                  | 'terraform'
                  | 'cli'
                  | 'php'
                  | 'csharp'
                  | 'sql'
                  | 'http'
                  | 'openapi'
                )[];
                [k: string]: unknown;
              };
              /**
               * Configure the default request options for an individual method
               */
              default_request_options?: {
                timeout?: (number | string) | null;
                /**
                 * The default number of times to retry a failing request
                 */
                max_retries?: number;
                [k: string]: unknown;
              };
              docs?: {
                /**
                 * Specify the response property to reference in example snippets. Set to 'false' to disable property references, e.g. for logging the entire response.
                 */
                response_property?: false | string;
                [k: string]: unknown;
              };
              terraform?: {
                /**
                 * Define the type of method this endpoint represents.
                 * - create: handles creation of the resource. Must be a POST
                 * - read: handles reading of the resource. Must be a GET
                 * - list: handles listing of the resource. Must be a GET
                 * - update: handles updating of the resource. Must be a PUT or PATCH
                 * - delete: handles deleting the resource. Must be a DELETE
                 * - upsert: handles both updating and creating of the resource. Can be PUT or PATCH
                 * - skip: don't include this method in generation
                 */
                method?: 'create' | 'read' | 'list' | 'upsert' | 'update' | 'delete' | 'skip';
                /**
                 * Defined the name of the path param that is used as the ID of the resource.
                 */
                id_path_param?: string;
                /**
                 * Define the name of the property that represents the ID for the resource. It may exist as a path, body, or response parameter.
                 */
                id_property?: string;
                /**
                 * Specify how the update method on the resource should encode the request body.
                 * - put: The entire resource is replaced with the new resource.
                 * - json_merge_patch: The resource is updated as a JSON Merge Patch, only providing changed fields.
                 *
                 * If null, then json_merge_patch will be used for PATCH endpoints, and put otherwise.
                 */
                update_behavior?: 'put' | 'json_merge_patch';
                /**
                 * Timeout for the entire Terraform operation in milliseconds or ISO8601.
                 *
                 * This doesn't affect the request-level timeout.
                 */
                default_timeout?: number | string;
              };
              /**
               * whether to generate MCP Server tools for this endpoint
               */
              mcp?:
                | boolean
                | {
                    /**
                     * A list of tags that end-users can use to filter which resources and endpoints to include
                     */
                    tags?: string[];
                    /**
                     * Override the tool name for this endpoint
                     */
                    tool_name?: string;
                    /**
                     * Override the description for this endpoint
                     */
                    description?: string;
                    [k: string]: unknown;
                  };
              cli?: {
                /**
                 * Whether or not to generate this method as a CLI command.
                 */
                enabled?: boolean;
                /**
                 * The GJSON filter to use to filter response data into a more manageable
                 * format. This can be overridden with the `--filter` flag.
                 *
                 * For GJSON syntax, see: https://github.com/tidwall/gjson/blob/master/SYNTAX.md
                 */
                filter?: string;
                /**
                 * The default output format for this endpoint. This can be overridden with
                 * the `--format` flag when running the command.
                 */
                format?: 'auto' | 'explore' | 'json' | 'pretty' | 'raw' | 'yaml';
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'webhook_unwrap';
              /**
               * Map of webhook event types in the format [event_name]: event operation in OAS spec. If empty, it will infer the names and use all operations.
               */
              event_types?: {
                [k: string]: string;
              };
              /**
               * The name of the client option that stores the webhook key used for verifying webhooks (or null to skip verification checks).
               */
              webhook_key_opt?:
                | (
                    | string
                    | {
                        name: string;
                        /**
                         * The encoding that the webhook signing key should be provided in. Defaults to base64.
                         */
                        encoding?: 'base64' | 'utf-8';
                        [k: string]: unknown;
                      }
                  )
                | null;
              description?: string;
              summary?: string;
              /**
               * Specify the name of the field used to differentiate between webhook payloads as a `string`, or provide an [OpenAPI discriminator definition](https://swagger.io/docs/specification/v3_0/data-models/inheritance-and-polymorphism/#discriminator).
               */
              discriminator?: {
                [k: string]: unknown;
              };
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              type: 'websocket';
              description?: string;
              summary?: string;
              path: string;
              /**
               * Used to generate helper methods for each client event in Python, e.g. `.` for `audio.input_buffer.commit` to then output `ws.audio.input_buffer.commit(*params)`
               */
              event_name_separator?: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              client_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              server_event_schema_uri: string;
              /**
               * The reference path to the schema, e.g. `#/components/schemas/Card`, or the shorthand Card.
               */
              query_params_schema_uri?: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
          | {
              /**
               * Describes an 'alias' to a method in the same resource. Used mainly for renaming a method and maintaining backwards compatibility.
               */
              type: 'alias';
              /**
               * The name of the method that this alias should point to.
               *
               * This currently only supports aliasing HTTP methods within the same resource.
               */
              to: string;
              /**
               * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
               */
              skip?:
                | boolean
                | (
                    | 'node'
                    | 'typescript'
                    | 'python'
                    | 'go'
                    | 'java'
                    | 'kotlin'
                    | 'ruby'
                    | 'terraform'
                    | 'cli'
                    | 'php'
                    | 'csharp'
                    | 'sql'
                    | 'http'
                    | 'openapi'
                  )[];
              /**
               * Opposite of `skip`; if set, SDK generation is only performed for this resource.
               */
              only?: (
                | 'node'
                | 'typescript'
                | 'python'
                | 'go'
                | 'java'
                | 'kotlin'
                | 'ruby'
                | 'terraform'
                | 'cli'
                | 'php'
                | 'csharp'
                | 'sql'
                | 'http'
                | 'openapi'
              )[];
              /**
               * Set the deprecation message for this method.
               *
               * To set different deprecation messages for each language, provide an object with properties for each language and a `default` to use for unspecified languages.  For example:
               *
               * ~~~yaml
               * deprecated:
               *   typescript: "Use `fooBar` instead."
               *   go: "Use `FooBar` instead."
               *   default: "Use /v2/foo/bar instead."
               * ~~~
               *
               * To set the same deprecation message for all languages, provide only the deprecation message as a string:
               *
               * ~~~yaml
               * deprecated: "Use /v2/foo/bar instead."
               * ~~~
               */
              deprecated?:
                | string
                | {
                    [k: string]: string;
                  };
              /**
               * Set to `false` to suppress code snippet documentation output.
               */
              document?: boolean;
              [k: string]: unknown;
            }
        );
  };
  /**
   * Add a docstring for the resource.
   */
  description?: string;
  /**
   * If `true`, don't prefix any type names with this resource or its ancestors' names.
   *
   * For example, consider an `external` resource with `customers` nested under it:
   *
   * With `standalone_api` set to `true`, Go will generate an `ExternalCustomerListParams` type in the `requests` package.
   *
   * With `standalone_api` set to `false`, Go will generate a `CustomerListParams` type in the `external_requests` package.
   */
  standalone_api?: boolean;
  /**
   * Terraform configuration options for this resource.
   */
  terraform?:
    | boolean
    | {
        /**
         * The name of the Terraform resource (prefixed by your organization name)
         */
        name?: string;
        /**
         * whether to enable a timeout property
         */
        timeouts?:
          | boolean
          | {
              /**
               * Attribute name for timeout parameter
               */
              name?: string;
              /**
               * Default timeout for the resource and subresources' Terraform operations in milliseconds or ISO8601.
               *
               * You can override this in subresources or individual methods.
               *
               * This doesn't affect the request-level timeout.
               */
              default?: number | string;
            };
        /**
         * Whether to generate Terraform resource
         */
        resource?: boolean;
        /**
         * Whether to generate Terraform data sources
         */
        data_source?:
          | boolean
          | {
              filtering: {
                /**
                 * Attribute name for the find one by filter for singular data sources
                 */
                find_one_by_property_name?: string;
              };
              response_items: {
                /**
                 * Attribute name for the result collection for list data sources (default: `items`)
                 */
                collection_property_name?: string;
                /**
                 * The maximum number of rows to fetch by default
                 */
                default_max_rows?: number;
                /**
                 * Attribute name for the maximum number of rows in the response
                 */
                max_rows_property_name?: string;
              };
            };
        /**
         * Configures whether a warning message is shown to users when they attempt to delete a resource that
         * does not have a delete endpoint.
         *
         * - warning: Does nothing upon delete, but generates a warning message before the user attempts to create or delete the resource.
         *
         * - no_warning: Does nothing upon delete, and does not generate a warning message.
         */
        unsupported_delete_behavior?: 'warning' | 'no_warning';
        /**
         * Skip tests and provide a reason
         */
        skip_tests_reason?: string;
        /**
         * The schema version for this resource. This is used by Terraform to track schema changes
         * and perform state migrations when the schema evolves over time.
         *
         * Only valid for resources, not data sources. Typically incremented by 1 for each
         * breaking schema change.
         */
        version?: number;
        [k: string]: unknown;
      };
  /**
   * If `true`, always prefix parameters and unnamed response types with resource names.
   *
   * If `false`, don't prefix parameters and unnamed response types with ancestor resource names.
   *
   * If this property isn't present in your Stainless config, the default behavior for the language will be used (e.g., Go always prefixes while Node doesn't).
   *
   * For example, consider the nested resource `/financial_accounts/balances`.  Set `use_namespace_in_type_names` to `true` to generate names like `FinancialAccountBalanceListParams`.
   *
   * Now consider the nested resource `financial_accounts/financial_transactions`.  Set `use_namespace_in_type_names` to `false` to generate names like `FinancialTransactionGetResponse` without the `FinancialAccount` prefix.
   */
  use_namespace_in_type_names?: boolean;
  /**
   * Define nested namespaces for accessing models and methods.
   *
   * For example, with a "cards" resource containing a "transactions" subresource, the
   * generated Python method invocation is `client.cards.transactions.retrieve()`.
   */
  subresources?: {
    [k: string]: ResourceConfig1;
  };
  /**
   * Set the deprecation message for this resource
   */
  deprecated?:
    | string
    | {
        [k: string]: string;
      };
  /**
   * Set to `false` to suppress code snippet documentation output.
   */
  document?: boolean;
  /**
   * whether to generate MCP Server tools for this resource
   */
  mcp?:
    | boolean
    | {
        /**
         * A list of tags that end-users can use to filter which resources and endpoints to include
         */
        tags?: string[];
        [k: string]: unknown;
      };
  /**
   * Configure the default request options for this resource.
   */
  default_request_options?: {
    headers?: {
      [k: string]: string;
    };
    [k: string]: unknown;
  };
  /**
   * Skip SDK generation for a resource by specifying `skip: true`. Skip SDK generation for a resource per language by specifying the languages to skip (for example, `skip: [go, node]`). Skipping generation can aid in debugging code generation errors.
   */
  skip?:
    | boolean
    | (
        | 'node'
        | 'typescript'
        | 'python'
        | 'go'
        | 'java'
        | 'kotlin'
        | 'ruby'
        | 'terraform'
        | 'cli'
        | 'php'
        | 'csharp'
        | 'sql'
        | 'http'
        | 'openapi'
      )[];
  /**
   * Opposite of `skip`; if set, SDK generation is only performed for this resource.
   */
  only?: (
    | 'node'
    | 'typescript'
    | 'python'
    | 'go'
    | 'java'
    | 'kotlin'
    | 'ruby'
    | 'terraform'
    | 'cli'
    | 'php'
    | 'csharp'
    | 'sql'
    | 'http'
    | 'openapi'
  )[];
  [k: string]: unknown;
}
