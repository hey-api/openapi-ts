{{~#if @root.$config.name~}}
{{#if @root.$config.useOptions~}}
{{#if parameters}}
const {
{{#each parameters}}
{{{name}}}{{#if default}} = {{{default}}}{{/if}},
{{/each}}
} = data;
{{/if}}
{{/if}}
{{else~}}
{{#if @root.$config.useOptions~}}
{{~#notEquals @root.$config.serviceResponse 'generics'~}}
{{~#if parameters~}}
const {
{{#each parameters}}
{{{name}}}{{#if default}} = {{{default}}}{{/if}},
{{/each}}
} = data;
{{/if}}
{{else}}
const {
{{#each parameters}}
{{{name}}}{{#if default}} = {{{default}}}{{/if}},
{{/each}}
    ...overrides
} = data;
{{/notEquals}}
{{/if}}
{{/if}}
