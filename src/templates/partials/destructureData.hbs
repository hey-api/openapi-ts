{{~#if @root.exportClient~}}
{{#if @root.useOptions~}}
{{#if parameters}}
const {
{{#each parameters}}
{{{name}}}{{#if default}} = {{{default}}}{{/if}},
{{/each}}
} = data;
{{/if}}
{{/if}}
{{~else~}}
{{#if @root.useOptions~}}
const {
{{#each parameters}}
{{{name}}}{{#if default}} = {{{default}}}{{/if}},
{{/each}}
    ...overrides
} = data;
{{/if}}
{{~/if~}}
