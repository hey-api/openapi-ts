{
{{#if description}}
	description: `{{{escapeDescription description}}}`,
{{/if}}
	properties: {
{{#if properties}}
	{{#each properties}}
		{{#notEquals name "[key: string]"}}
		{{{name}}}: {{>schema}},
		{{/notEquals}}
	{{/each}}
{{/if}}
	},
{{#ifNotUndefined default}}
	default: {{{default}}},
{{/ifNotUndefined}}
{{#if isReadOnly}}
	isReadOnly: {{{isReadOnly}}},
{{/if}}
{{#if isRequired}}
	isRequired: {{{isRequired}}},
{{/if}}
{{#if isNullable}}
	isNullable: {{{isNullable}}},
{{/if}}
}
