{{>header}}

{{{modelImports this './'}}}

{{#equals export 'interface'}}
{{>exportInterface}}
{{else equals export 'one-of'}}
{{>exportComposition}}
{{else equals export 'any-of'}}
{{>exportComposition}}
{{else equals export 'all-of'}}
{{>exportComposition}}
{{else equals export 'enum'}}
{{>exportEnum}}
{{>exportEnum exportType="type"}}
{{else}}
{{>exportType}}
{{/equals}}
