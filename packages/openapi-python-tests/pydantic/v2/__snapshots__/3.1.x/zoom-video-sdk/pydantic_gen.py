# This file is auto-generated by @hey-api/openapi-python

from datetime import datetime
from enum import Enum
from typing import Optional

from pydantic import BaseModel, ConfigDict, EmailStr, Field


class SessionUserPhoneCalloutRingingBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited user."""

    model_config = ConfigDict(extra="forbid")
    invitee_name: str = Field(..., description="The user's name to display in the session.")
    phone_number: int = Field(..., description="The user's phone number.")
    from_number: int = Field(..., description="The number used to call out to the invited user.")


class SessionUserPhoneCalloutRingingBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    user_key: str = Field(..., description="Another identifier for the inviter. Can be a number or characters.")
    host_id: str = Field(..., description="The user ID of the session host.")
    participant: SessionUserPhoneCalloutRingingBodyPayloadObjectParticipant = Field(..., description="Information about the invited user.")


class SessionUserPhoneCalloutRingingBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserPhoneCalloutRingingBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserPhoneCalloutRingingBody(BaseModel):
    """# session.user_phone_callout_ringing

    The **Invited party's phone (call out) ringing** event is triggered every time a user's phone is ringing when they were invited to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Invited party's phone (call out) ringing** subscription enabled under the **Session** event.




    **Event type**: `session.user_phone_callout_ringing`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserPhoneCalloutRingingBodyPayload


class SessionUserPhoneCalloutRingingWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserPhoneCalloutRingingBody] = Field(default=None, description="""# session.user_phone_callout_ringing

The **Invited party's phone (call out) ringing** event is triggered every time a user's phone is ringing when they were invited to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Invited party's phone (call out) ringing** subscription enabled under the **Session** event.




**Event type**: `session.user_phone_callout_ringing`
""")


class SessionUserRoomSystemCalloutRingingBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="allow")
    call_type: str = Field(..., description="The type of call out. Use a value of h323 or sip.")
    device_ip: str = Field(..., description="The user's device IP address.")


class SessionUserRoomSystemCalloutRingingBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    host_id: str = Field(..., description="The user ID of the session host.")
    message_id: str = Field(..., description="The request unique identifier (UUID).")
    inviter_name: str = Field(..., description="The user name of the event's trigger.")
    participant: SessionUserRoomSystemCalloutRingingBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserRoomSystemCalloutRingingBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserRoomSystemCalloutRingingBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserRoomSystemCalloutRingingBody(BaseModel):
    """# session.user_room_system_callout_ringing

    The **Invited party's phone (call out) ringing in Zoom room** event is triggered every time a user's phone is ringing when they were invited to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
    * A valid **Event Notification Endpoint URL**.
    * The **Invited party's phone (call out) ringing in Zoom room** subscription enabled under the **Session** event.



    **Event type**: `session.user_room_system_callout_ringing`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserRoomSystemCalloutRingingBodyPayload


class SessionUserRoomSystemCalloutRingingWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserRoomSystemCalloutRingingBody] = Field(default=None, description="""# session.user_room_system_callout_ringing

The **Invited party's phone (call out) ringing in Zoom room** event is triggered every time a user's phone is ringing when they were invited to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
* A valid **Event Notification Endpoint URL**.
* The **Invited party's phone (call out) ringing in Zoom room** subscription enabled under the **Session** event.



**Event type**: `session.user_room_system_callout_ringing`
""")


class SessionRecordingStartedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_STARTED = "session.recording_started"


class SessionRecordingStartedBodyPayloadObjectRecordingFile(BaseModel):
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")


class SessionRecordingStartedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_file: SessionRecordingStartedBodyPayloadObjectRecordingFile


class SessionRecordingStartedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingStartedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingStartedBody(BaseModel):
    """# session.recording_started
    ---

    Event: `session.recording_started`

    ## Event description

    The **Session recording started** event is triggered every time a recording is started by one of your app users or account users.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * **Session recording started** subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_started` event notification:



    **Event type**: `session.recording_started`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingStartedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingStartedBodyPayload


class SessionRecordingStartedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingStartedBody] = Field(default=None, description="""# session.recording_started
---

Event: `session.recording_started`

## Event description

The **Session recording started** event is triggered every time a recording is started by one of your app users or account users.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * **Session recording started** subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_started` event notification:



**Event type**: `session.recording_started`
""")


class SessionRecordingResumedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_RESUMED = "session.recording_resumed"


class SessionRecordingResumedBodyPayloadObjectRecordingFile(BaseModel):
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")


class SessionRecordingResumedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_file: SessionRecordingResumedBodyPayloadObjectRecordingFile


class SessionRecordingResumedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingResumedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingResumedBody(BaseModel):
    """# session.recording_resumed
    ---

    Event: `session.recording_resumed`

    ## Event description

    The **Session recording resumed** event is triggered every time a previously paused recording of a session is resumed.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * **Session recording resumed** subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_resumed` event notification:



    **Event type**: `session.recording_resumed`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingResumedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingResumedBodyPayload


class SessionRecordingResumedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingResumedBody] = Field(default=None, description="""# session.recording_resumed
---

Event: `session.recording_resumed`

## Event description

The **Session recording resumed** event is triggered every time a previously paused recording of a session is resumed.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * **Session recording resumed** subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_resumed` event notification:



**Event type**: `session.recording_resumed`
""")


class SessionLiveStreamingStoppedBodyEvent(str, Enum):
    """The name of the event."""

    SESSION_LIVE_STREAMING_STOPPED = "session.live_streaming_stopped"


class SessionLiveStreamingStoppedBodyPayloadObjectLiveStreamingService(str, Enum):
    """The name of the Live Streaming service."""

    FACEBOOK = "Facebook"
    WORKPLACE_BY_FACEBOOK = "Workplace_by_Facebook"
    YOU_TUBE = "YouTube"
    CUSTOM_LIVE_STREAMING_SERVICE = "Custom_Live_Streaming_Service"


class SessionLiveStreamingStoppedBodyPayloadObjectLiveStreamingCustomLiveStreamingSettings(BaseModel):
    """The live stream settings."""

    stream_url: str = Field(..., description="The stream's URL.")
    stream_key: str = Field(..., description="The stream's Key.")
    page_url: str = Field(..., description="The live stream's page URL.")
    resolution: Optional[str] = Field(default=None, description="The number of pixels in each dimension that the video camera can display.")


class SessionLiveStreamingStoppedBodyPayloadObjectLiveStreaming(BaseModel):
    """Information about the participant."""

    service: SessionLiveStreamingStoppedBodyPayloadObjectLiveStreamingService = Field(..., description="The name of the Live Streaming service.")
    custom_live_streaming_settings: SessionLiveStreamingStoppedBodyPayloadObjectLiveStreamingCustomLiveStreamingSettings = Field(..., description="The live stream settings.")
    date_time: datetime = Field(..., description="The live stream's stop time.")


class SessionLiveStreamingStoppedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The session's start time.")
    live_streaming: SessionLiveStreamingStoppedBodyPayloadObjectLiveStreaming = Field(..., description="Information about the participant.")


class SessionLiveStreamingStoppedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionLiveStreamingStoppedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionLiveStreamingStoppedBody(BaseModel):
    """# session.live_streaming_stopped
    ---


    The Session Live Streaming Stopped event is triggered every time a user stops a live stream.

    ## Prerequisites
    Event Subscriptions must be enabled for your app with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Live Streaming Stopped** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.live_streaming_stopped` event notification:



    **Event type**: `session.live_streaming_stopped`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionLiveStreamingStoppedBodyEvent = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) at which the event occurred. The value of this field is returned in long(int64) format.")
    payload: SessionLiveStreamingStoppedBodyPayload


class SessionLiveStreamingStoppedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionLiveStreamingStoppedBody] = Field(default=None, description="""# session.live_streaming_stopped
---


The Session Live Streaming Stopped event is triggered every time a user stops a live stream.

## Prerequisites
Event Subscriptions must be enabled for your app with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Live Streaming Stopped** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.live_streaming_stopped` event notification:



**Event type**: `session.live_streaming_stopped`
""")


class SessionStreamIngestionStoppedBodyEvent(str, Enum):
    """Event name."""

    SESSION_STREAM_INGESTION_STOPPED = "session.stream_ingestion_stopped"


class SessionStreamIngestionStoppedBodyPayloadObjectStreamIngestion(BaseModel):
    """Information about the stream ingestion."""

    stream_id: str = Field(..., description="The stream ingestion ID.")
    stream_name: str = Field(..., description="The stream ingestion name.")
    stream_description: Optional[str] = Field(default=None, description="The stream ingestion description.")
    stream_key: str = Field(..., description="The stream ingestion key.")
    stream_url: str = Field(..., description="The stream URL.")
    backup_stream_url: str = Field(..., description="The backup stream URL.")


class SessionStreamIngestionStoppedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    stream_ingestion: SessionStreamIngestionStoppedBodyPayloadObjectStreamIngestion = Field(..., description="Information about the stream ingestion.")


class SessionStreamIngestionStoppedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStreamIngestionStoppedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStreamIngestionStoppedBody(BaseModel):
    """# session.stream_ingestion_stopped
    Zoom triggers the **Session stream ingestion stopped** event every time a user stops sending live incoming streams.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Session stream ingestion stopped** subscription enabled under the **Video SDK** event.


    **Event type**: `session.stream_ingestion_stopped`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStreamIngestionStoppedBodyEvent = Field(..., description="Event name.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) when the event occurred. The value of this field is returned in long (int64) format.")
    payload: SessionStreamIngestionStoppedBodyPayload


class SessionStreamIngestionStoppedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStreamIngestionStoppedBody] = Field(default=None, description="""# session.stream_ingestion_stopped
Zoom triggers the **Session stream ingestion stopped** event every time a user stops sending live incoming streams.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Session stream ingestion stopped** subscription enabled under the **Video SDK** event.


**Event type**: `session.stream_ingestion_stopped`
""")


class SessionUserRoomSystemCalloutRejectedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="allow")
    call_type: str = Field(..., description="The type of call out. Use a value of h323 or sip.")
    device_ip: str = Field(..., description="The user's device IP address.")


class SessionUserRoomSystemCalloutRejectedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    host_id: str = Field(..., description="The user ID of the session host.")
    message_id: str = Field(..., description="The request unique identifier (UUID).")
    inviter_name: str = Field(..., description="The user name of the event's trigger.")
    participant: SessionUserRoomSystemCalloutRejectedBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserRoomSystemCalloutRejectedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserRoomSystemCalloutRejectedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserRoomSystemCalloutRejectedBody(BaseModel):
    """# session.user_room_system_callout_rejected

    The **Invited party rejected a session invitation through phone (call out) via Zoom room** event is triggered every time a user rejects an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
    * A valid **Event Notification Endpoint URL**.
    * The **Invited party rejected a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



    **Event type**: `session.user_room_system_callout_rejected`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserRoomSystemCalloutRejectedBodyPayload


class SessionUserRoomSystemCalloutRejectedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserRoomSystemCalloutRejectedBody] = Field(default=None, description="""# session.user_room_system_callout_rejected

The **Invited party rejected a session invitation through phone (call out) via Zoom room** event is triggered every time a user rejects an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
* A valid **Event Notification Endpoint URL**.
* The **Invited party rejected a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



**Event type**: `session.user_room_system_callout_rejected`
""")


class SessionAlertBodyEvent(str, Enum):
    """The event's name."""

    SESSION_ALERT = "session.alert"


class SessionAlertBodyPayloadObjectIssues(str, Enum):
    UNSTABLE_AUDIO_QUALITY = "Unstable audio quality"
    UNSTABLE_VIDEO_QUALITY = "Unstable video quality"
    UNSTABLE_SCREEN_SHARE_QUALITY = "Unstable screen share quality"
    HIGH_CPU_OCCUPATION = "High CPU occupation"
    CALL_RECONNECTION = "Call Reconnection"


class SessionAlertBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    issues: list[SessionAlertBodyPayloadObjectIssues] = Field(..., description="Issues that occurred during the session.")


class SessionAlertBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionAlertBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionAlertBody(BaseModel):
    """# session.alert
    ---


    The Session Alert event is triggered every time a service issue is encountered during a session in your account.

    The following quality metrics can trigger an alert:

    * Unstable audio quality
    * Unstable video quality
    * Unstable screen share quality
    * High CPU occupation
    * Call Reconnection

    ## Prerequisites
    Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

    * A valid Event Notification Endpoint URL.
    * Session Alert subscription enabled under the Video SDK event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.alert` event notification:


    **Event type**: `session.alert`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionAlertBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionAlertBodyPayload


class SessionAlertWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionAlertBody] = Field(default=None, description="""# session.alert
---


The Session Alert event is triggered every time a service issue is encountered during a session in your account.

The following quality metrics can trigger an alert:

* Unstable audio quality
* Unstable video quality
* Unstable screen share quality
* High CPU occupation
* Call Reconnection

## Prerequisites
Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

* A valid Event Notification Endpoint URL.
* Session Alert subscription enabled under the Video SDK event.

The tabs below display the complete schema, payload with data types, and an example of the `session.alert` event notification:


**Event type**: `session.alert`
""")


class SessionSharingEndedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_SHARING_ENDED = "session.sharing_ended"


class SessionSharingEndedBodyPayloadObjectUserSharingDetailsContent(str, Enum):
    """The type of shared content:
    * application — An application. For example, a web browser.
    * whiteboard — The Zoom [whiteboard](https://support.zoom.us/hc/en-us/articles/205677665-Sharing-a-whiteboard).
    * desktop — A user's desktop.
    * unknown — An unrecognized application, such as a third party app.
    """

    APPLICATION = "application"
    WHITEBOARD = "whiteboard"
    DESKTOP = "desktop"
    UNKNOWN = "unknown"


class SessionSharingEndedBodyPayloadObjectUserSharingDetails(BaseModel):
    """Information about the session's screen sharing."""

    model_config = ConfigDict(extra="forbid")
    content: SessionSharingEndedBodyPayloadObjectUserSharingDetailsContent = Field(..., description="""The type of shared content:
* application — An application. For example, a web browser.
* whiteboard — The Zoom [whiteboard](https://support.zoom.us/hc/en-us/articles/205677665-Sharing-a-whiteboard).
* desktop — A user's desktop.
* unknown — An unrecognized application, such as a third party app.""")
    date_time: datetime = Field(..., description="The session's screen sharing date and time.")


class SessionSharingEndedBodyPayloadObjectUser(BaseModel):
    """Information about the user."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The user's unique ID. This value is assigned to the user upon joining a session and is only valid for that session.")
    name: str = Field(..., description="The user's username.")
    user_key: Optional[str] = Field(default=None, description="Another identifier for the user. Can be a number or characters.")
    sharing_details: SessionSharingEndedBodyPayloadObjectUserSharingDetails = Field(..., description="Information about the session's screen sharing.")


class SessionSharingEndedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    user: SessionSharingEndedBodyPayloadObjectUser = Field(..., description="Information about the user.")


class SessionSharingEndedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionSharingEndedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionSharingEndedBody(BaseModel):
    """# session.sharing_ended
    ---


    The Session Sharing Ended event is triggered every time a user stops sharing the screen during a session.

    ## Prerequisites
    Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Sharing Ended** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.sharing_ended` event notification:



    **Event type**: `session.sharing_ended`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionSharingEndedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionSharingEndedBodyPayload


class SessionSharingEndedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionSharingEndedBody] = Field(default=None, description="""# session.sharing_ended
---


The Session Sharing Ended event is triggered every time a user stops sharing the screen during a session.

## Prerequisites
Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Sharing Ended** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.sharing_ended` event notification:



**Event type**: `session.sharing_ended`
""")


class SessionRecordingPausedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_PAUSED = "session.recording_paused"


class SessionRecordingPausedBodyPayloadObjectRecordingFile(BaseModel):
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")


class SessionRecordingPausedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_file: SessionRecordingPausedBodyPayloadObjectRecordingFile


class SessionRecordingPausedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingPausedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingPausedBody(BaseModel):
    """# session.recording_paused
    ---

    Event: `session.recording_paused`

    ## Event description

    The **Session recording paused** event is triggered every time a recording is paused by one of your app or account users.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * "**Session recording paused**" subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_paused` event notification:



    **Event type**: `session.recording_paused`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingPausedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingPausedBodyPayload


class SessionRecordingPausedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingPausedBody] = Field(default=None, description='''# session.recording_paused
---

Event: `session.recording_paused`

## Event description

The **Session recording paused** event is triggered every time a recording is paused by one of your app or account users.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * "**Session recording paused**" subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_paused` event notification:



**Event type**: `session.recording_paused`
''')


class SessionEndedBodyEvent(str, Enum):
    """The name of the event."""

    SESSION_ENDED = "session.ended"


class SessionEndedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The session's start time.")
    end_time: datetime = Field(..., description="The session's end time.")


class SessionEndedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionEndedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionEndedBody(BaseModel):
    """# session.ended
    ---


    The Session Ended event is triggered when the host ends the session, or when all users have left the session.

    ## Prerequisites
    Event Subscriptions must be enabled for your app with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Ended** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.ended` event notification:



    **Event type**: `session.ended`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionEndedBodyEvent = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="Timestamp at which the event occurred, in milliseconds since epoch. The value of this field is returned in long(int64) format.")
    payload: SessionEndedBodyPayload


class SessionEndedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionEndedBody] = Field(default=None, description="""# session.ended
---


The Session Ended event is triggered when the host ends the session, or when all users have left the session.

## Prerequisites
Event Subscriptions must be enabled for your app with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Ended** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.ended` event notification:



**Event type**: `session.ended`
""")


class SessionStartedBodyEvent(str, Enum):
    """The name of the event."""

    SESSION_STARTED = "session.started"


class SessionStartedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session's ID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The session's start time.")


class SessionStartedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStartedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStartedBody(BaseModel):
    """# session.started
    ---


    The Session Start event is triggered when the first user (including host and co-host) joins.

    ## Prerequisites
    Event Subscriptions must be enabled for your app with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Started** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.started` event notification:



    **Event type**: `session.started`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStartedBodyEvent = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) at which the event occurred. The value of this field is returned in long(int64) format.")
    payload: SessionStartedBodyPayload


class SessionStartedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStartedBody] = Field(default=None, description="""# session.started
---


The Session Start event is triggered when the first user (including host and co-host) joins.

## Prerequisites
Event Subscriptions must be enabled for your app with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Started** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.started` event notification:



**Event type**: `session.started`
""")


class SessionStreamIngestionUnbindBodyEvent(str, Enum):
    """Event name."""

    SESSION_STREAM_INGESTION_UNBIND = "session.stream_ingestion_unbind"


class SessionStreamIngestionUnbindBodyPayloadObjectStreamIngestion(BaseModel):
    """Information about the stream ingestion."""

    stream_id: str = Field(..., description="The stream ingestion ID.")
    stream_name: str = Field(..., description="The stream ingestion name.")
    stream_description: Optional[str] = Field(default=None, description="The stream ingestion description.")
    stream_key: str = Field(..., description="The stream ingestion key.")
    stream_url: str = Field(..., description="The stream URL.")
    backup_stream_url: str = Field(..., description="The backup stream URL.")


class SessionStreamIngestionUnbindBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    stream_ingestion: SessionStreamIngestionUnbindBodyPayloadObjectStreamIngestion = Field(..., description="Information about the stream ingestion.")


class SessionStreamIngestionUnbindBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStreamIngestionUnbindBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStreamIngestionUnbindBody(BaseModel):
    """# session.stream_ingestion_unbind
    Zoom triggers the **Session stream ingestion unbind** event every time a session unbinds a stream key.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Session stream ingestion unbind** subscription enabled under the **Video SDK** event.


    **Event type**: `session.stream_ingestion_unbind`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStreamIngestionUnbindBodyEvent = Field(..., description="Event name.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) at which the event occurred. The value of this field is returned in long (int64) format.")
    payload: SessionStreamIngestionUnbindBodyPayload


class SessionStreamIngestionUnbindWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStreamIngestionUnbindBody] = Field(default=None, description="""# session.stream_ingestion_unbind
Zoom triggers the **Session stream ingestion unbind** event every time a session unbinds a stream key.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Session stream ingestion unbind** subscription enabled under the **Video SDK** event.


**Event type**: `session.stream_ingestion_unbind`
""")


class SessionLiveStreamingStartedBodyEvent(str, Enum):
    """Name of the event."""

    SESSION_LIVE_STREAMING_STARTED = "session.live_streaming_started"


class SessionLiveStreamingStartedBodyPayloadObjectLiveStreamingService(str, Enum):
    """The name of the Live Streaming service."""

    FACEBOOK = "Facebook"
    WORKPLACE_BY_FACEBOOK = "Workplace_by_Facebook"
    YOU_TUBE = "YouTube"
    CUSTOM_LIVE_STREAMING_SERVICE = "Custom_Live_Streaming_Service"


class SessionLiveStreamingStartedBodyPayloadObjectLiveStreamingCustomLiveStreamingSettings(BaseModel):
    """The live stream settings."""

    stream_url: str = Field(..., description="The stream's URL.")
    stream_key: str = Field(..., description="The stream's key.")
    page_url: str = Field(..., description="The live stream's page URL.")
    resolution: Optional[str] = Field(default=None, description="The number of pixels in each dimension that the video camera can display.")


class SessionLiveStreamingStartedBodyPayloadObjectLiveStreaming(BaseModel):
    """Information about the participant."""

    service: SessionLiveStreamingStartedBodyPayloadObjectLiveStreamingService = Field(..., description="The name of the Live Streaming service.")
    custom_live_streaming_settings: SessionLiveStreamingStartedBodyPayloadObjectLiveStreamingCustomLiveStreamingSettings = Field(..., description="The live stream settings.")
    date_time: datetime = Field(..., description="The live stream's start time.")


class SessionLiveStreamingStartedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The session's start time.")
    live_streaming: SessionLiveStreamingStartedBodyPayloadObjectLiveStreaming = Field(..., description="Information about the participant.")


class SessionLiveStreamingStartedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionLiveStreamingStartedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionLiveStreamingStartedBody(BaseModel):
    """# session.live_streaming_started
    ---


    The Session Live Streaming Started event is triggered every time a user starts a live stream.

    ## Prerequisites
    Event Subscriptions must be enabled for your app with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Live Streaming Started** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.live_streaming_started` event notification:



    **Event type**: `session.live_streaming_started`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionLiveStreamingStartedBodyEvent = Field(..., description="Name of the event.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) at which the event occurred. The value of this field is returned in long(int64) format.")
    payload: SessionLiveStreamingStartedBodyPayload


class SessionLiveStreamingStartedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionLiveStreamingStartedBody] = Field(default=None, description="""# session.live_streaming_started
---


The Session Live Streaming Started event is triggered every time a user starts a live stream.

## Prerequisites
Event Subscriptions must be enabled for your app with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Live Streaming Started** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.live_streaming_started` event notification:



**Event type**: `session.live_streaming_started`
""")


class SessionUserRoomSystemCalloutMissedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="allow")
    call_type: str = Field(..., description="The type of call out. Use a value of h323 or sip.")
    device_ip: str = Field(..., description="The user's device IP address.")


class SessionUserRoomSystemCalloutMissedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    host_id: str = Field(..., description="The user ID of the session host.")
    message_id: str = Field(..., description="The request unique identifier (UUID).")
    inviter_name: str = Field(..., description="The user name of the event's trigger.")
    participant: SessionUserRoomSystemCalloutMissedBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserRoomSystemCalloutMissedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserRoomSystemCalloutMissedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserRoomSystemCalloutMissedBody(BaseModel):
    """# session.user_room_system_callout_missed

    The **Invited party missed a session invitation through phone (call out) via Zoom room** event is triggered every time an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035) times out.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
    * A valid **Event Notification Endpoint URL**.
    * The **Invited party missed a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



    **Event type**: `session.user_room_system_callout_missed`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserRoomSystemCalloutMissedBodyPayload


class SessionUserRoomSystemCalloutMissedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserRoomSystemCalloutMissedBody] = Field(default=None, description="""# session.user_room_system_callout_missed

The **Invited party missed a session invitation through phone (call out) via Zoom room** event is triggered every time an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035) times out.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
* A valid **Event Notification Endpoint URL**.
* The **Invited party missed a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



**Event type**: `session.user_room_system_callout_missed`
""")


class SessionUserPhoneCalloutAcceptedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="forbid")
    invitee_name: str = Field(..., description="The user's name to display in the session.")
    phone_number: int = Field(..., description="The user's phone number.")
    from_number: int = Field(..., description="The number used to call out to the invited user.")


class SessionUserPhoneCalloutAcceptedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    user_key: str = Field(..., description="Another identifier for the inviter. Can be a number or characters.")
    host_id: str = Field(..., description="The user ID of the session host.")
    participant: SessionUserPhoneCalloutAcceptedBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserPhoneCalloutAcceptedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserPhoneCalloutAcceptedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserPhoneCalloutAcceptedBody(BaseModel):
    """# session.user_phone_callout_accepted

    The **Invited party answered a session invitation through phone (call out)** event is triggered every time a user accepts an invitation to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Invited party answered a session invitation through phone (call out)** subscription enabled under the **Session** event.



    **Event type**: `session.user_phone_callout_accepted`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserPhoneCalloutAcceptedBodyPayload


class SessionUserPhoneCalloutAcceptedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserPhoneCalloutAcceptedBody] = Field(default=None, description="""# session.user_phone_callout_accepted

The **Invited party answered a session invitation through phone (call out)** event is triggered every time a user accepts an invitation to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Invited party answered a session invitation through phone (call out)** subscription enabled under the **Session** event.



**Event type**: `session.user_phone_callout_accepted`
""")


class SessionUserLeftBodyEvent(str, Enum):
    """The event's name."""

    SESSION_USER_LEFT = "session.user_left"


class SessionUserLeftBodyPayloadObjectUser(BaseModel):
    """Information about the user."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The user's unique ID. This value is assigned to the user upon joining a session and is only valid for that session.")
    name: str = Field(..., description="The user's username.")
    leave_time: datetime = Field(..., description="The time at which the user left the session.")
    leave_reason: Optional[str] = Field(default=None, description="""The reason why the user left the session, where $name is the participant's username: 
* $name left the session. 
* $name got disconnected from the session. 
* Host ended the session. 
* Host closed the session. 
* Host started a new session. 
* Network connection error. 
* Host did not join. 
* Exceeded free session minutes limit. 
* Removed by host. 
* Unknown reason. 
* Leave waiting room. 
* Removed by host from waiting room.""")
    user_key: Optional[str] = Field(default=None, description="Another identifier for the user. Can be a number or characters.")
    phone_number: Optional[str] = Field(default=None, description="Phone number of participant joined via PSTN.")
    participant_uuid: str = Field(..., description="The participant's UUID. This value is assigned to a participant upon joining a session and is only valid for the session's duration.")


class SessionUserLeftBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    user: SessionUserLeftBodyPayloadObjectUser = Field(..., description="Information about the user.")


class SessionUserLeftBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionUserLeftBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionUserLeftBody(BaseModel):
    """# session.user_left


    The Session User Left event is triggered every time a user (including host and co-host) leaves a session.

    ## Prerequisites
    Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

    * A valid Event Notification Endpoint URL.
    * Session User Left subscription enabled under the Video SDK event.



    **Event type**: `session.user_left`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionUserLeftBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionUserLeftBodyPayload


class SessionUserLeftWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserLeftBody] = Field(default=None, description="""# session.user_left


The Session User Left event is triggered every time a user (including host and co-host) leaves a session.

## Prerequisites
Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

* A valid Event Notification Endpoint URL.
* Session User Left subscription enabled under the Video SDK event.



**Event type**: `session.user_left`
""")


class SessionSharingStartedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_SHARING_STARTED = "session.sharing_started"


class SessionSharingStartedBodyPayloadObjectUserSharingDetailsContent(str, Enum):
    """The type of shared content:
    * application — An application. For example, a web browser.
    * whiteboard — The Zoom [whiteboard](https://support.zoom.us/hc/en-us/articles/205677665-Sharing-a-whiteboard).
    * desktop — A user's desktop.
    * unknown — An unrecognized application, such as a third party app.
    """

    APPLICATION = "application"
    WHITEBOARD = "whiteboard"
    DESKTOP = "desktop"
    UNKNOWN = "unknown"


class SessionSharingStartedBodyPayloadObjectUserSharingDetails(BaseModel):
    """Information about the session's screen sharing."""

    model_config = ConfigDict(extra="forbid")
    content: SessionSharingStartedBodyPayloadObjectUserSharingDetailsContent = Field(..., description="""The type of shared content:
* application — An application. For example, a web browser.
* whiteboard — The Zoom [whiteboard](https://support.zoom.us/hc/en-us/articles/205677665-Sharing-a-whiteboard).
* desktop — A user's desktop.
* unknown — An unrecognized application, such as a third party app.""")
    date_time: datetime = Field(..., description="The session's screen sharing date and time.")


class SessionSharingStartedBodyPayloadObjectUser(BaseModel):
    """Information about the user."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The user's unique ID. This value is assigned to the user upon joining a session and is only valid for that session.")
    name: str = Field(..., description="The user's username.")
    user_key: Optional[str] = Field(default=None, description="Another identifier for the user. Can be a number or characters.")
    sharing_details: SessionSharingStartedBodyPayloadObjectUserSharingDetails = Field(..., description="Information about the session's screen sharing.")


class SessionSharingStartedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    user: SessionSharingStartedBodyPayloadObjectUser = Field(..., description="Information about the user.")


class SessionSharingStartedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionSharingStartedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionSharingStartedBody(BaseModel):
    """# session.sharing_started


    The Session Sharing Started event is triggered every time a user starts sharing the screen during a session.

    ## Prerequisites
    Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

    * A valid Event Notification Endpoint URL.
    * **Session Sharing Started** subscription enabled under the **Video SDK** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.sharing_started` event notification:



    **Event type**: `session.sharing_started`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionSharingStartedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionSharingStartedBodyPayload


class SessionSharingStartedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionSharingStartedBody] = Field(default=None, description="""# session.sharing_started


The Session Sharing Started event is triggered every time a user starts sharing the screen during a session.

## Prerequisites
Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

* A valid Event Notification Endpoint URL.
* **Session Sharing Started** subscription enabled under the **Video SDK** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.sharing_started` event notification:



**Event type**: `session.sharing_started`
""")


class SessionUserPhoneCalloutCanceledBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="forbid")
    invitee_name: str = Field(..., description="The user's name to display in the session.")
    phone_number: int = Field(..., description="The user's phone number.")
    from_number: int = Field(..., description="The number used to call out to the invited user.")


class SessionUserPhoneCalloutCanceledBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    user_key: str = Field(..., description="Another identifier for the inviter. Can be a number or characters.")
    participant: SessionUserPhoneCalloutCanceledBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserPhoneCalloutCanceledBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserPhoneCalloutCanceledBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserPhoneCalloutCanceledBody(BaseModel):
    """# session.user_phone_callout_canceled
    The **Invited party's phone (call out) canceled** event is triggered every time the inviter cancels the call out when they were invited to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Invited party's phone (call out) canceled** subscription enabled under the **Session** event.


    **Event type**: `session.user_phone_callout_canceled`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserPhoneCalloutCanceledBodyPayload


class SessionUserPhoneCalloutCanceledWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserPhoneCalloutCanceledBody] = Field(default=None, description="""# session.user_phone_callout_canceled
The **Invited party's phone (call out) canceled** event is triggered every time the inviter cancels the call out when they were invited to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Invited party's phone (call out) canceled** subscription enabled under the **Session** event.


**Event type**: `session.user_phone_callout_canceled`
""")


class SessionRecordingTranscriptCompletedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_TRANSCRIPT_COMPLETED = "session.recording_transcript_completed"


class SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesFileType(str, Enum):
    """The recording file type. The value of this field could be one of the following:<br>
    MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
    <br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

    <br>

    A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
    	id, status, file_size, recording_type.
    """

    MP4 = "MP4"
    M4A = "M4A"
    CHAT = "CHAT"
    TRANSCRIPT = "TRANSCRIPT"
    CSV = "CSV"
    CC = "CC"
    TB = "TB"
    CHAT_MESSAGE = "CHAT_MESSAGE"
    TIMELINE = "TIMELINE"


class SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesFileExtension(str, Enum):
    """The archived file's file extension."""

    MP4 = "MP4"
    M4A = "M4A"
    TXT = "TXT"
    VTT = "VTT"
    CSV = "CSV"
    JSON = "JSON"
    JPG = "JPG"


class SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesStatus(str, Enum):
    """The recording status."""

    COMPLETED = "completed"


class SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesRecordingType(str, Enum):
    """The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption"""

    SHARED_SCREEN_WITH_SPEAKER_VIEW_CC = "shared_screen_with_speaker_view(CC)"
    SHARED_SCREEN_WITH_SPEAKER_VIEW = "shared_screen_with_speaker_view"
    SHARED_SCREEN_WITH_GALLERY_VIEW = "shared_screen_with_gallery_view"
    GALLERY_VIEW = "gallery_view"
    SHARED_SCREEN = "shared_screen"
    AUDIO_ONLY = "audio_only"
    AUDIO_TRANSCRIPT = "audio_transcript"
    CHAT_FILE = "chat_file"
    ACTIVE_SPEAKER = "active_speaker"
    HOST_VIDEO = "host_video"
    AUDIO_ONLY_EACH_PARTICIPANT = "audio_only_each_participant"
    CC_TRANSCRIPT = "cc_transcript"
    CLOSED_CAPTION = "closed_caption"
    POLL = "poll"
    TIMELINE = "timeline"
    THUMBNAIL = "thumbnail"
    CHAT_MESSAGE = "chat_message"


class SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFiles(BaseModel):
    id: Optional[str] = Field(default=None, description="The recording file ID. Included in the response of general query.")
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")
    file_name: Optional[str] = Field(default=None, description="Name of the file.")
    file_path: Optional[str] = Field(default=None, description="""The file path to the On-Premise account recording. 

**Note:** This API only returns this field for [Zoom On-Premise accounts](https://support.zoom.us/hc/en-us/articles/360034064852-Zoom-On-Premise-Deployment). It does **not** return the download_url field.""")
    file_type: Optional[SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesFileType] = Field(default=None, description="""The recording file type. The value of this field could be one of the following:<br>
MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
<br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

<br>

A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
	id, status, file_size, recording_type.""")
    file_size: Optional[float] = Field(default=None, description="The recording file size.")
    file_extension: Optional[SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesFileExtension] = Field(default=None, description="The archived file's file extension.")
    download_url: Optional[str] = Field(default=None, description="""The URL at which to download the the recording. 

To access a private or password-protected cloud recording of a user in your account, use your [Video SDK API JWT](https://marketplace.zoom.us/docs/guides/build/video-sdk-app/#video-sdk-and-api-credentials). You can either pass the generated JWT as a Bearer token in the Authorization header of your HTTP request (recommended) or include it as a query parameter at the end of the URL. See the following for examples. 

 **Using an Authorization header (Recommended)** 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {JWT} \\
  --header 'content-type: application/json'
 

**Using a query parameter** 

 
{download_url}/?access_token={download_token}
 

 For example: https://{base-domain}/recording/download/{path-to-file-download}?access_token={JWT}.""")
    status: Optional[SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesStatus] = Field(default=None, description="The recording status.")
    recording_type: Optional[SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFilesRecordingType] = Field(default=None, description="The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption")


class SessionRecordingTranscriptCompletedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_files: list[SessionRecordingTranscriptCompletedBodyPayloadObjectRecordingFiles] = Field(..., title="Recording file List", description="List of recording file.")


class SessionRecordingTranscriptCompletedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingTranscriptCompletedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingTranscriptCompletedBody(BaseModel):
    """# session.recording_transcript_completed
    ---

    Event: `session.recording_transcript_completed`

    ## Event description

    The **Session recording transcript completed** event is triggered every time the transcript of the recording of a session becomes available to view or download.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * Session recording transcript completed subscription enabled under the Recording event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_transcript_completed` event notification:



    **Event type**: `session.recording_transcript_completed`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingTranscriptCompletedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    download_token: str = Field(..., description="""A generated token used to download the recording. The download token only lasts for 24 hours after its creation and you can only download the file within 24 hours of receiving the event notification. You can either pass the download_token as a Bearer token in the Authorization header of your HTTP request (recommended) or include it as a query parameter. See the following for examples. 

 **Using an Authorization header (Recommended)** 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {download_token} \\
  --header 'content-type: application/json'
 

**Using a query parameter** 

 
{download_url}/?access_token={download_token}
 

 For example: https://zoom.us/recording/download/123456?access_token=abcdefgh""")
    payload: SessionRecordingTranscriptCompletedBodyPayload


class SessionRecordingTranscriptCompletedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingTranscriptCompletedBody] = Field(default=None, description="""# session.recording_transcript_completed
---

Event: `session.recording_transcript_completed`

## Event description

The **Session recording transcript completed** event is triggered every time the transcript of the recording of a session becomes available to view or download.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * Session recording transcript completed subscription enabled under the Recording event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_transcript_completed` event notification:



**Event type**: `session.recording_transcript_completed`
""")


class SessionRecordingDeletedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_DELETED = "session.recording_deleted"


class SessionRecordingDeletedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")


class SessionRecordingDeletedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    operator: EmailStr = Field(..., description="The email address of the user who deleted the recording.")
    operator_id: str = Field(..., description="The user ID of the user who deleted the recording.")
    object: SessionRecordingDeletedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingDeletedBody(BaseModel):
    """# session.recording_deleted
    ---

    Event: `session.recording_deleted`

    ## Event description

    The **Session recording permanently deleted** event is triggered every time one of your app users or account users permanently deletes a cloud recording.

    When a user initially deletes a recording, it is sent to the trash. At this stage, the recording can still be recovered for up to 30 days after deletion. To permanently delete a recording, users must delete it from the trash. Users can permanently delete a recording using:

    * The [Delete session's recording API](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingDelete) **with the delete query parameter** or
    * The Zoom web portal. See [Video SDK account](https://developers.zoom.us/docs/video-sdk/account/) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
    	* A valid Event Notification Endpoint URL.
    	* "**Session recording permanently deleted**" subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_deleted` event notification:



    **Event type**: `session.recording_deleted`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingDeletedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingDeletedBodyPayload


class SessionRecordingDeletedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingDeletedBody] = Field(default=None, description="""# session.recording_deleted
---

Event: `session.recording_deleted`

## Event description

The **Session recording permanently deleted** event is triggered every time one of your app users or account users permanently deletes a cloud recording.

When a user initially deletes a recording, it is sent to the trash. At this stage, the recording can still be recovered for up to 30 days after deletion. To permanently delete a recording, users must delete it from the trash. Users can permanently delete a recording using:

* The [Delete session's recording API](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingDelete) **with the delete query parameter** or
* The Zoom web portal. See [Video SDK account](https://developers.zoom.us/docs/video-sdk/account/) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
	* A valid Event Notification Endpoint URL.
	* "**Session recording permanently deleted**" subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_deleted` event notification:



**Event type**: `session.recording_deleted`
""")


class SessionUserRoomSystemCalloutFailedBodyPayloadObjectReasonType(int, Enum):
    """reason type for failure: 
    * 1 — Encryption_Fail. 
    * 2 — Disconnected_By_Remote. 
    * 3 — Retry. 
    * 4 — Bad_Seq. 
    * 5 — Call_Limit. 
    * 6 — Not_Registered. 
    * 7 — Timeout. 
    * 8 — Bad_Addr. 
    * 9 — Unreachable 
    * 10 — Disconnect_By_Local 
    * 11 — Server_Internal_Error 
    * 12 — Exceed_Free_Port 
    * 13 — Connect_Error 
    * 14 — Proxy_Connect_Error 
    * 0 — Other_Failed_Reason.
    """

    VALUE_0 = 0
    VALUE_1 = 1
    VALUE_2 = 2
    VALUE_3 = 3
    VALUE_4 = 4
    VALUE_7 = 7
    VALUE_8 = 8
    VALUE_9 = 9
    VALUE_10 = 10
    VALUE_11 = 11
    VALUE_12 = 12
    VALUE_13 = 13
    VALUE_14 = 14


class SessionUserRoomSystemCalloutFailedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="allow")
    call_type: str = Field(..., description="The type of call out. Use a value of h323 or sip.")
    device_ip: str = Field(..., description="The user's device IP address.")


class SessionUserRoomSystemCalloutFailedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    host_id: str = Field(..., description="The user ID of the session host.")
    message_id: str = Field(..., description="The request unique identifier (UUID).")
    inviter_name: str = Field(..., description="The user name of the event's trigger.")
    reason_type: SessionUserRoomSystemCalloutFailedBodyPayloadObjectReasonType = Field(..., description="""reason type for failure: 
* 1 — Encryption_Fail. 
* 2 — Disconnected_By_Remote. 
* 3 — Retry. 
* 4 — Bad_Seq. 
* 5 — Call_Limit. 
* 6 — Not_Registered. 
* 7 — Timeout. 
* 8 — Bad_Addr. 
* 9 — Unreachable 
* 10 — Disconnect_By_Local 
* 11 — Server_Internal_Error 
* 12 — Exceed_Free_Port 
* 13 — Connect_Error 
* 14 — Proxy_Connect_Error 
* 0 — Other_Failed_Reason.""")
    participant: SessionUserRoomSystemCalloutFailedBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserRoomSystemCalloutFailedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserRoomSystemCalloutFailedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserRoomSystemCalloutFailedBody(BaseModel):
    """# session.user_room_system_callout_failed

    The **Session invitation through phone (call out) via Zoom room failed** event is triggered every time an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035) fails.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
    * A valid **Event Notification Endpoint URL**.
    * The **Session invitation through phone (call out) via Zoom room failed** subscription enabled under the **Session** event.



    **Event type**: `session.user_room_system_callout_failed`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserRoomSystemCalloutFailedBodyPayload


class SessionUserRoomSystemCalloutFailedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserRoomSystemCalloutFailedBody] = Field(default=None, description="""# session.user_room_system_callout_failed

The **Session invitation through phone (call out) via Zoom room failed** event is triggered every time an invitation to [join a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035) fails.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
* A valid **Event Notification Endpoint URL**.
* The **Session invitation through phone (call out) via Zoom room failed** subscription enabled under the **Session** event.



**Event type**: `session.user_room_system_callout_failed`
""")


class SessionRecordingCompletedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_COMPLETED = "session.recording_completed"


class SessionRecordingCompletedBodyPayloadObjectRecordingFilesFileType(str, Enum):
    """The recording file type. The value of this field could be one of the following:<br>
    MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
    <br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

    <br>

    A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
    	id, status, file_size, recording_type.
    """

    MP4 = "MP4"
    M4A = "M4A"
    CHAT = "CHAT"
    TRANSCRIPT = "TRANSCRIPT"
    CSV = "CSV"
    CC = "CC"
    TB = "TB"
    CHAT_MESSAGE = "CHAT_MESSAGE"


class SessionRecordingCompletedBodyPayloadObjectRecordingFilesFileExtension(str, Enum):
    """The archived file's file extension."""

    MP4 = "MP4"
    M4A = "M4A"
    TXT = "TXT"
    VTT = "VTT"
    CSV = "CSV"
    JSON = "JSON"
    JPG = "JPG"


class SessionRecordingCompletedBodyPayloadObjectRecordingFilesStatus(str, Enum):
    """The recording status."""

    COMPLETED = "completed"


class SessionRecordingCompletedBodyPayloadObjectRecordingFilesRecordingType(str, Enum):
    """The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption"""

    SHARED_SCREEN_WITH_SPEAKER_VIEW_CC = "shared_screen_with_speaker_view(CC)"
    SHARED_SCREEN_WITH_SPEAKER_VIEW = "shared_screen_with_speaker_view"
    SHARED_SCREEN_WITH_GALLERY_VIEW = "shared_screen_with_gallery_view"
    GALLERY_VIEW = "gallery_view"
    SHARED_SCREEN = "shared_screen"
    AUDIO_ONLY = "audio_only"
    AUDIO_TRANSCRIPT = "audio_transcript"
    CHAT_FILE = "chat_file"
    ACTIVE_SPEAKER = "active_speaker"
    HOST_VIDEO = "host_video"
    AUDIO_ONLY_EACH_PARTICIPANT = "audio_only_each_participant"
    CC_TRANSCRIPT = "cc_transcript"
    CLOSED_CAPTION = "closed_caption"
    POLL = "poll"
    TIMELINE = "timeline"
    THUMBNAIL = "thumbnail"
    CHAT_MESSAGE = "chat_message"


class SessionRecordingCompletedBodyPayloadObjectRecordingFiles(BaseModel):
    id: Optional[str] = Field(default=None, description="The recording file ID. Included in the response of general query.")
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")
    file_name: Optional[str] = Field(default=None, description="Name of the file.")
    file_path: Optional[str] = Field(default=None, description="""The file path to the On-Premise account recording. 

**Note:** This API only returns this field for [Zoom On-Premise accounts](https://support.zoom.us/hc/en-us/articles/360034064852-Zoom-On-Premise-Deployment). It does **not** return the download_url field.""")
    file_type: Optional[SessionRecordingCompletedBodyPayloadObjectRecordingFilesFileType] = Field(default=None, description="""The recording file type. The value of this field could be one of the following:<br>
MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
<br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

<br>

A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
	id, status, file_size, recording_type.""")
    file_size: Optional[float] = Field(default=None, description="The recording file size.")
    file_extension: Optional[SessionRecordingCompletedBodyPayloadObjectRecordingFilesFileExtension] = Field(default=None, description="The archived file's file extension.")
    download_url: Optional[str] = Field(default=None, description="""The URL at which to download the the recording. 

To access a private or password-protected cloud recording of a user in your account, use your [Video SDK API JWT](https://marketplace.zoom.us/docs/guides/build/video-sdk-app/#video-sdk-and-api-credentials). Pass the generated JWT as a Bearer token in the Authorization header of your HTTP request. For example: 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {JWT} \\
  --header 'content-type: application/json'
""")
    status: Optional[SessionRecordingCompletedBodyPayloadObjectRecordingFilesStatus] = Field(default=None, description="The recording status.")
    recording_type: Optional[SessionRecordingCompletedBodyPayloadObjectRecordingFilesRecordingType] = Field(default=None, description="The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption")


class SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesFileType(str, Enum):
    """The recording file type. The value of this field could be one of the following:<br>
    MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
    <br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

    <br>

    A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
    	id, status, file_size, recording_type.
    """

    MP4 = "MP4"
    M4A = "M4A"
    CHAT = "CHAT"
    TRANSCRIPT = "TRANSCRIPT"
    CSV = "CSV"
    CC = "CC"
    TB = "TB"
    CHAT_MESSAGE = "CHAT_MESSAGE"


class SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesFileExtension(str, Enum):
    """The archived file's file extension."""

    MP4 = "MP4"
    M4A = "M4A"
    TXT = "TXT"
    VTT = "VTT"
    CSV = "CSV"
    JSON = "JSON"
    JPG = "JPG"


class SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesStatus(str, Enum):
    """The recording status."""

    COMPLETED = "completed"


class SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesRecordingType(str, Enum):
    """The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption"""

    SHARED_SCREEN_WITH_SPEAKER_VIEW_CC = "shared_screen_with_speaker_view(CC)"
    SHARED_SCREEN_WITH_SPEAKER_VIEW = "shared_screen_with_speaker_view"
    SHARED_SCREEN_WITH_GALLERY_VIEW = "shared_screen_with_gallery_view"
    GALLERY_VIEW = "gallery_view"
    SHARED_SCREEN = "shared_screen"
    AUDIO_ONLY = "audio_only"
    AUDIO_TRANSCRIPT = "audio_transcript"
    CHAT_FILE = "chat_file"
    ACTIVE_SPEAKER = "active_speaker"
    HOST_VIDEO = "host_video"
    AUDIO_ONLY_EACH_PARTICIPANT = "audio_only_each_participant"
    CC_TRANSCRIPT = "cc_transcript"
    CLOSED_CAPTION = "closed_caption"
    POLL = "poll"
    TIMELINE = "timeline"
    THUMBNAIL = "thumbnail"
    CHAT_MESSAGE = "chat_message"


class SessionRecordingCompletedBodyPayloadObjectParticipantAudioFiles(BaseModel):
    id: Optional[str] = Field(default=None, description="The recording file ID. Included in the response of general query.")
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")
    file_name: Optional[str] = Field(default=None, description="Name of the file.")
    file_path: Optional[str] = Field(default=None, description="""The file path to the On-Premise account recording. 

**Note:** This API only returns this field for [Zoom On-Premise accounts](https://support.zoom.us/hc/en-us/articles/360034064852-Zoom-On-Premise-Deployment). It does **not** return the download_url field.""")
    file_type: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesFileType] = Field(default=None, description="""The recording file type. The value of this field could be one of the following:<br>
MP4: Video file of the recording.<br>M4A Audio-only file of the recording.<br>TIMELINE: Timestamp file of the recording in JSON file format. To get a timeline file, the "Add a timestamp to the recording" setting must be enabled in the [recording settings](https://support.zoom.us/hc/en-us/articles/360060316092). The time will display in the host's timezone, set on their Zoom profile.
<br> TRANSCRIPT: Transcription file of the recording in VTT format.<br> CHAT: A TXT file containing in-session chat messages that were sent during the session.<br>CC: File containing closed captions of the recording in VTT file format.<br>CSV: File containing polling data in csv format.

<br>

A recording file object with file type of either CC or TIMELINE **does not have** the following properties:<br>
	id, status, file_size, recording_type.""")
    file_size: Optional[float] = Field(default=None, description="The recording file size.")
    file_extension: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesFileExtension] = Field(default=None, description="The archived file's file extension.")
    download_url: Optional[str] = Field(default=None, description="""The URL at which to download the the recording. 

To access a private or password-protected cloud recording of a user in your account, use your [Video SDK API JWT](https://marketplace.zoom.us/docs/guides/build/video-sdk-app/#video-sdk-and-api-credentials). Pass the generated JWT as a Bearer token in the Authorization header of your HTTP request. For example: 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {JWT} \\
  --header 'content-type: application/json'
""")
    status: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesStatus] = Field(default=None, description="The recording status.")
    recording_type: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantAudioFilesRecordingType] = Field(default=None, description="The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption")
    user_id: Optional[str] = Field(default=None, description="The participant's session user ID. This value is assigned to a participant upon joining a session and is only valid for the duration of the session.")
    user_key: Optional[str] = Field(default=None, description="The participant's SDK identifier. Set with the user_identity key in the Video SDK JWT payload. This value can be alphanumeric, up to a maximum length of 35 characters.")


class SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesFileType(str, Enum):
    """The recording file type. The value of this field could be one of the following:<br>
    MP4: Video file of the recording.
    """

    MP4 = "MP4"


class SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesFileExtension(str, Enum):
    """The archived file's file extension."""

    MP4 = "MP4"


class SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesStatus(str, Enum):
    """The recording status."""

    COMPLETED = "completed"


class SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesRecordingType(str, Enum):
    """The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption"""

    INDIVIDUAL_USER = "individual_user"
    INDIVIDUAL_SHARED_SCREEN = "individual_shared_screen"


class SessionRecordingCompletedBodyPayloadObjectParticipantVideoFiles(BaseModel):
    id: Optional[str] = Field(default=None, description="The recording file ID. Included in the response of general query.")
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")
    file_name: Optional[str] = Field(default=None, description="Name of the file.")
    file_path: Optional[str] = Field(default=None, description="""The file path to the On-Premise account recording. 

**Note:** This API only returns this field for [Zoom On-Premise accounts](https://support.zoom.us/hc/en-us/articles/360034064852-Zoom-On-Premise-Deployment). It does **not** return the download_url field.""")
    file_type: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesFileType] = Field(default=None, description="""The recording file type. The value of this field could be one of the following:<br>
MP4: Video file of the recording.""")
    file_size: Optional[float] = Field(default=None, description="The recording file size.")
    file_extension: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesFileExtension] = Field(default=None, description="The archived file's file extension.")
    download_url: Optional[str] = Field(default=None, description="""The URL at which to download the the recording. 

To access a private or password-protected cloud recording of a user in your account, use your [Video SDK API JWT](https://marketplace.zoom.us/docs/guides/build/video-sdk-app/#video-sdk-and-api-credentials). Pass the generated JWT as a Bearer token in the Authorization header of your HTTP request. For example: 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {JWT} \\
  --header 'content-type: application/json'
""")
    status: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesStatus] = Field(default=None, description="The recording status.")
    recording_type: Optional[SessionRecordingCompletedBodyPayloadObjectParticipantVideoFilesRecordingType] = Field(default=None, description="The recording type. The value of this field can be one of the following:<br>shared_screen_with_speaker_view(CC)<br>shared_screen_with_speaker_view<br>shared_screen_with_gallery_view<br>speaker_view<br>gallery_view<br>shared_screen<br>audio_only<br>audio_transcript<br>chat_file<br>active_speaker<br>poll<br>timeline<br>closed_caption")
    user_id: Optional[str] = Field(default=None, description="The participant's session user ID. This value is assigned to a participant upon joining a session and is only valid for the duration of the session.")
    user_key: Optional[str] = Field(default=None, description="The participant's SDK identifier. Set with the user_identity key in the Video SDK JWT payload. This value can be alphanumeric, up to a maximum length of 35 characters.")


class SessionRecordingCompletedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_files: list[SessionRecordingCompletedBodyPayloadObjectRecordingFiles] = Field(..., title="Recording file List", description="List of recording file.")
    participant_audio_files: Optional[list[SessionRecordingCompletedBodyPayloadObjectParticipantAudioFiles]] = Field(default=None, title="Recording file List", description="List of recording file.")
    participant_video_files: Optional[list[SessionRecordingCompletedBodyPayloadObjectParticipantVideoFiles]] = Field(default=None, title="Recording file List", description="List of recording file.")


class SessionRecordingCompletedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingCompletedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingCompletedBody(BaseModel):
    """# session.recording_completed
    ---

    Event: `session.recording_completed`

    ## Event description

    The **Session recording completed** event is triggered every time a recording of a session becomes available to view and/or download.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * Session recording completed subscription enabled under the Recording event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_completed` event notification:



    **Event type**: `session.recording_completed`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingCompletedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    download_token: str = Field(..., description="""A generated token used to download the recording. The download token only lasts for 24 hours after its creation and you can only download the file within 24 hours of receiving the event notification. Pass the download_token as a Bearer token in the Authorization header of your HTTP request. For example: 

 
curl --request GET \\
  --url {download_url} \\
  --header 'authorization: Bearer {download_token} \\
  --header 'content-type: application/json'
""")
    payload: SessionRecordingCompletedBodyPayload


class SessionRecordingCompletedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingCompletedBody] = Field(default=None, description="""# session.recording_completed
---

Event: `session.recording_completed`

## Event description

The **Session recording completed** event is triggered every time a recording of a session becomes available to view and/or download.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * Session recording completed subscription enabled under the Recording event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_completed` event notification:



**Event type**: `session.recording_completed`
""")


class SessionRecordingTranscriptFailedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_TRANSCRIPT_FAILED = "session.recording_transcript_failed"


class SessionRecordingTranscriptFailedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time when the session started.")
    timezone: str = Field(..., description="The user's timezone.")


class SessionRecordingTranscriptFailedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingTranscriptFailedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingTranscriptFailedBody(BaseModel):
    """# session.recording_transcript_failed
    ---

    Event: `session.recording_transcript_failed`

    ## Event description

    The **Session recording transcript failed** event is triggered every time the transcript of the recording of a session generated failed.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Enable Cloud Recording on the Video SDK account.
    * Enable Event Subscriptions for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * Enable session recording transcript completed subscription under the Recording event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_transcript_completed` event notification:



    **Event type**: `session.recording_transcript_failed`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingTranscriptFailedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, when the event occurred.")
    payload: SessionRecordingTranscriptFailedBodyPayload


class SessionRecordingTranscriptFailedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingTranscriptFailedBody] = Field(default=None, description="""# session.recording_transcript_failed
---

Event: `session.recording_transcript_failed`

## Event description

The **Session recording transcript failed** event is triggered every time the transcript of the recording of a session generated failed.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Enable Cloud Recording on the Video SDK account.
* Enable Event Subscriptions for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * Enable session recording transcript completed subscription under the Recording event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_transcript_completed` event notification:



**Event type**: `session.recording_transcript_failed`
""")


class SessionRecordingTrashedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_TRASHED = "session.recording_trashed"


class SessionRecordingTrashedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")


class SessionRecordingTrashedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    operator: EmailStr = Field(..., description="The email address of the user who deleted the recording.")
    operator_id: str = Field(..., description="The user ID of the user who deleted the recording.")
    object: SessionRecordingTrashedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingTrashedBody(BaseModel):
    """# session.recording_trashed
    ---

    Event: `session.recording_trashed`

    ## Event description

    The **Session recording deleted to trash** event is triggered every time one of your app users or account users temporarily delete a cloud recording.

    When a user initially deletes a recording, it is deleted to the trash. Recordings deleted to the trash can be recovered up to 30 days after deletion. Users can move a recording to the trash using:

    * The [Delete session's recording](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingDelete) API **with the trash query parameter** or
    * The Zoom web portal. See [Video SDK account](https://developers.zoom.us/docs/video-sdk/account/) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
    	* A valid Event Notification Endpoint URL.
    	* **Session recording deleted to trash** subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_trashed` event notification:



    **Event type**: `session.recording_trashed`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingTrashedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingTrashedBodyPayload


class SessionRecordingTrashedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingTrashedBody] = Field(default=None, description="""# session.recording_trashed
---

Event: `session.recording_trashed`

## Event description

The **Session recording deleted to trash** event is triggered every time one of your app users or account users temporarily delete a cloud recording.

When a user initially deletes a recording, it is deleted to the trash. Recordings deleted to the trash can be recovered up to 30 days after deletion. Users can move a recording to the trash using:

* The [Delete session's recording](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingDelete) API **with the trash query parameter** or
* The Zoom web portal. See [Video SDK account](https://developers.zoom.us/docs/video-sdk/account/) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
	* A valid Event Notification Endpoint URL.
	* **Session recording deleted to trash** subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_trashed` event notification:



**Event type**: `session.recording_trashed`
""")


class SessionUserJoinedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_USER_JOINED = "session.user_joined"


class SessionUserJoinedBodyPayloadObjectUser(BaseModel):
    """Information about the user."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The user's unique ID. This value is assigned to the user upon joining a session and is only valid for that session.")
    name: str = Field(..., description="The user's username.")
    join_time: datetime = Field(..., description="The time at which the user joined the session.")
    user_key: Optional[str] = Field(default=None, description="Another identifier for the user. Can be a number or characters.")
    phone_number: Optional[str] = Field(default=None, description="Phone number of participant joined via PSTN.")
    participant_uuid: str = Field(..., description="The participant's UUID. This value is assigned to a participant upon joining a session and is only valid for the session's duration.")


class SessionUserJoinedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    id: str = Field(..., description="The session ID.")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    user: SessionUserJoinedBodyPayloadObjectUser = Field(..., description="Information about the user.")


class SessionUserJoinedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionUserJoinedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionUserJoinedBody(BaseModel):
    """# session.user_joined
    ---


    The Session User Joined event is triggered every time a user (including host and co-host) joins a session.

    ## Prerequisites
    Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

    * A valid Event Notification Endpoint URL.
    * Session User Joined subscription enabled under the Video SDK event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.user_joined` event notification:



    **Event type**: `session.user_joined`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionUserJoinedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionUserJoinedBodyPayload


class SessionUserJoinedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserJoinedBody] = Field(default=None, description="""# session.user_joined
---


The Session User Joined event is triggered every time a user (including host and co-host) joins a session.

## Prerequisites
Event Subscriptions must be enabled for your [Marketplace app](https://marketplace.zoom.us/user/build) with the following configurations:

* A valid Event Notification Endpoint URL.
* Session User Joined subscription enabled under the Video SDK event.

The tabs below display the complete schema, payload with data types, and an example of the `session.user_joined` event notification:



**Event type**: `session.user_joined`
""")


class SessionStreamIngestionStartedBodyEvent(str, Enum):
    """Event name."""

    SESSION_STREAM_INGESTION_STARTED = "session.stream_ingestion_started"


class SessionStreamIngestionStartedBodyPayloadObjectStreamIngestion(BaseModel):
    """Information about the stream ingestion."""

    stream_id: str = Field(..., description="The stream ingestion ID.")
    stream_name: str = Field(..., description="The stream ingestion name.")
    stream_description: Optional[str] = Field(default=None, description="The stream ingestion description.")
    stream_key: str = Field(..., description="The stream ingestion key.")
    stream_url: str = Field(..., description="The stream URL.")
    backup_stream_url: str = Field(..., description="The backup stream URL.")


class SessionStreamIngestionStartedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    stream_ingestion: SessionStreamIngestionStartedBodyPayloadObjectStreamIngestion = Field(..., description="Information about the stream ingestion.")


class SessionStreamIngestionStartedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStreamIngestionStartedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStreamIngestionStartedBody(BaseModel):
    """# session.stream_ingestion_started
    Zoom triggers the **Session stream ingestion started** event every time a host starts sending an incoming live stream to a session. The session user can subscribe to it to see what's being streamed live into this session.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Session stream ingestion started** subscription enabled under the **Video SDK** event.


    **Event type**: `session.stream_ingestion_started`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStreamIngestionStartedBodyEvent = Field(..., description="Event name.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) when the event occurred. The value of this field is returned in long (int64) format.")
    payload: SessionStreamIngestionStartedBodyPayload


class SessionStreamIngestionStartedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStreamIngestionStartedBody] = Field(default=None, description="""# session.stream_ingestion_started
Zoom triggers the **Session stream ingestion started** event every time a host starts sending an incoming live stream to a session. The session user can subscribe to it to see what's being streamed live into this session.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Session stream ingestion started** subscription enabled under the **Video SDK** event.


**Event type**: `session.stream_ingestion_started`
""")


class SessionStreamIngestionConnectedBodyEvent(str, Enum):
    """Name of the event."""

    SESSION_STREAM_INGESTION_CONNECTED = "session.stream_ingestion_connected"


class SessionStreamIngestionConnectedBodyPayloadObjectStreamIngestion(BaseModel):
    """Information about the stream ingestion."""

    stream_id: str = Field(..., description="The stream ingestion ID.")
    stream_name: str = Field(..., description="The stream ingestion name.")
    stream_description: Optional[str] = Field(default=None, description="The stream ingestion description.")
    stream_key: str = Field(..., description="The stream ingestion key.")
    stream_url: str = Field(..., description="The stream URL.")
    backup_stream_url: str = Field(..., description="The backup stream URL.")


class SessionStreamIngestionConnectedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    stream_ingestion: SessionStreamIngestionConnectedBodyPayloadObjectStreamIngestion = Field(..., description="Information about the stream ingestion.")


class SessionStreamIngestionConnectedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStreamIngestionConnectedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStreamIngestionConnectedBody(BaseModel):
    """# session.stream_ingestion_connected
    Zoom triggers the **Session stream ingestion connected** event every time live streaming software connects to the Zoom Video SDK session. This indicates that the Zoom streaming platform is receiving data from the streaming software, but the received Real-Time Messaging Protocol (RTMP) data is not sent to the session yet.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Session stream ingestion connected** subscription enabled under the **Video SDK** event.


    **Event type**: `session.stream_ingestion_connected`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStreamIngestionConnectedBodyEvent = Field(..., description="Name of the event.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) when the event occurred. The value of this field is returned in long (int64) format.")
    payload: SessionStreamIngestionConnectedBodyPayload


class SessionStreamIngestionConnectedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStreamIngestionConnectedBody] = Field(default=None, description="""# session.stream_ingestion_connected
Zoom triggers the **Session stream ingestion connected** event every time live streaming software connects to the Zoom Video SDK session. This indicates that the Zoom streaming platform is receiving data from the streaming software, but the received Real-Time Messaging Protocol (RTMP) data is not sent to the session yet.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Session stream ingestion connected** subscription enabled under the **Video SDK** event.


**Event type**: `session.stream_ingestion_connected`
""")


class SessionStreamIngestionDisconnectedBodyEvent(str, Enum):
    """Name of the event."""

    SESSION_STREAM_INGESTION_DISCONNECTED = "session.stream_ingestion_disconnected"


class SessionStreamIngestionDisconnectedBodyPayloadObjectStreamIngestion(BaseModel):
    """Information about the stream ingestion."""

    stream_id: str = Field(..., description="The stream ingestion ID.")
    stream_name: str = Field(..., description="The stream ingestion name.")
    stream_description: Optional[str] = Field(default=None, description="The stream ingestion description.")
    stream_key: str = Field(..., description="The stream ingestion key.")
    stream_url: str = Field(..., description="The stream URL.")
    backup_stream_url: str = Field(..., description="The backup stream URL.")


class SessionStreamIngestionDisconnectedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: Optional[str] = Field(default=None, description="The Video SDK custom session ID.")
    stream_ingestion: SessionStreamIngestionDisconnectedBodyPayloadObjectStreamIngestion = Field(..., description="Information about the stream ingestion.")


class SessionStreamIngestionDisconnectedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionStreamIngestionDisconnectedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionStreamIngestionDisconnectedBody(BaseModel):
    """# session.stream_ingestion_disconnected
    Zoom triggers the **Session stream ingestion disconnected** event every time a Real-Time Messaging Protocol (RTMP) software program disconnects.

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Session stream ingestion disconnected** subscription enabled under the **Video SDK** event.


    **Event type**: `session.stream_ingestion_disconnected`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionStreamIngestionDisconnectedBodyEvent = Field(..., description="Name of the event.")
    event_ts: int = Field(..., description="Timestamp (in milliseconds since epoch) when the event occurred. The value of this field is returned in long (int64) format.")
    payload: SessionStreamIngestionDisconnectedBodyPayload


class SessionStreamIngestionDisconnectedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionStreamIngestionDisconnectedBody] = Field(default=None, description="""# session.stream_ingestion_disconnected
Zoom triggers the **Session stream ingestion disconnected** event every time a Real-Time Messaging Protocol (RTMP) software program disconnects.

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Session stream ingestion disconnected** subscription enabled under the **Video SDK** event.


**Event type**: `session.stream_ingestion_disconnected`
""")


class SessionRecordingRecoveredBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_RECOVERED = "session.recording_recovered"


class SessionRecordingRecoveredBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")


class SessionRecordingRecoveredBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    operator: EmailStr = Field(..., description="The email address of the user who deleted the recording.")
    operator_id: str = Field(..., description="The user ID of the user who deleted the recording.")
    object: SessionRecordingRecoveredBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingRecoveredBody(BaseModel):
    """# session.recording_recovered
    ---

    Event: `session.recording_recovered`

    ## Event description

    The **Session recording recovered** event is triggered every time one of your app users or account users recover a recording from the trash.

    Zoom allows users to recover their cloud recordings from trash within 30 days of deletion. You can recover a deleted cloud recording with:

    * The tab in the Zoom web client. See [Video SDK recordings and reports](https://developers.zoom.us/docs/video-sdk/reports/) for details.
    * The [Recover a single recording](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingStatusUpdateOne) or [Recover session's recordings](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingStatusUpdate) API.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
    	* A valid Event Notification Endpoint URL.
    	* "**Session recording recovered**" subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_recovered` event notification:



    **Event type**: `session.recording_recovered`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingRecoveredBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingRecoveredBodyPayload


class SessionRecordingRecoveredWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingRecoveredBody] = Field(default=None, description="""# session.recording_recovered
---

Event: `session.recording_recovered`

## Event description

The **Session recording recovered** event is triggered every time one of your app users or account users recover a recording from the trash.

Zoom allows users to recover their cloud recordings from trash within 30 days of deletion. You can recover a deleted cloud recording with:

* The tab in the Zoom web client. See [Video SDK recordings and reports](https://developers.zoom.us/docs/video-sdk/reports/) for details.
* The [Recover a single recording](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingStatusUpdateOne) or [Recover session's recordings](https://developers.zoom.us/docs/api/rest/reference/video-sdk/methods/#operation/recordingStatusUpdate) API.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
	* A valid Event Notification Endpoint URL.
	* "**Session recording recovered**" subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_recovered` event notification:



**Event type**: `session.recording_recovered`
""")


class SessionUserPhoneCalloutMissedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited user."""

    model_config = ConfigDict(extra="forbid")
    invitee_name: str = Field(..., description="The user's name to display in the session.")
    phone_number: int = Field(..., description="The user's phone number.")
    from_number: int = Field(..., description="The number used to call out to the invited user.")


class SessionUserPhoneCalloutMissedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    user_key: str = Field(..., description="Another identifier for the inviter. Can be a number or characters.")
    host_id: str = Field(..., description="The user ID of the session host.")
    participant: SessionUserPhoneCalloutMissedBodyPayloadObjectParticipant = Field(..., description="Information about the invited user.")


class SessionUserPhoneCalloutMissedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserPhoneCalloutMissedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserPhoneCalloutMissedBody(BaseModel):
    """# session.user_phone_callout_missed

    The **Invited party missed a session invitation through phone (call out)** event is triggered every time an invitation to join a session through phone (call out) times out. Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Invited party missed a session invitation through phone (call out)** subscription enabled under the **Session** event.




    **Event type**: `session.user_phone_callout_missed`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserPhoneCalloutMissedBodyPayload


class SessionUserPhoneCalloutMissedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserPhoneCalloutMissedBody] = Field(default=None, description="""# session.user_phone_callout_missed

The **Invited party missed a session invitation through phone (call out)** event is triggered every time an invitation to join a session through phone (call out) times out. Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Invited party missed a session invitation through phone (call out)** subscription enabled under the **Session** event.




**Event type**: `session.user_phone_callout_missed`
""")


class SessionUserPhoneCalloutRejectedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited user."""

    model_config = ConfigDict(extra="forbid")
    invitee_name: str = Field(..., description="The user's name to display in the session.")
    phone_number: int = Field(..., description="The user's phone number.")
    from_number: int = Field(..., description="The number used to call out to the invited user.")


class SessionUserPhoneCalloutRejectedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    user_key: str = Field(..., description="Another identifier for the inviter. Can be a number or characters.")
    host_id: str = Field(..., description="The user ID of the session host.")
    participant: SessionUserPhoneCalloutRejectedBodyPayloadObjectParticipant = Field(..., description="Information about the invited user.")


class SessionUserPhoneCalloutRejectedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserPhoneCalloutRejectedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserPhoneCalloutRejectedBody(BaseModel):
    """# session.user_phone_callout_rejected

    The **Invited party rejected a session invitation through phone (call out)** event is triggered every time a user rejects an invitation to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
      * A valid **Event Notification Endpoint URL**.
      * The **Invited party rejected a session invitation through phone (call out)** subscription enabled under the **Session** event.




    **Event type**: `session.user_phone_callout_rejected`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserPhoneCalloutRejectedBodyPayload


class SessionUserPhoneCalloutRejectedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserPhoneCalloutRejectedBody] = Field(default=None, description="""# session.user_phone_callout_rejected

The **Invited party rejected a session invitation through phone (call out)** event is triggered every time a user rejects an invitation to join a session through phone (call out). Video SDK call out operates in a similar manner as [Zoom meeting call out](https://support.zoom.com/hc/article?id=zm_kb&sysparm_article=KB0062038).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
  * A valid **Event Notification Endpoint URL**.
  * The **Invited party rejected a session invitation through phone (call out)** subscription enabled under the **Session** event.




**Event type**: `session.user_phone_callout_rejected`
""")


class SessionUserRoomSystemCalloutAcceptedBodyPayloadObjectParticipant(BaseModel):
    """Information about the invited participant."""

    model_config = ConfigDict(extra="allow")
    call_type: str = Field(..., description="The type of call out. Use a value of h323 or sip.")
    device_ip: str = Field(..., description="The user's device IP address.")


class SessionUserRoomSystemCalloutAcceptedBodyPayloadObject(BaseModel):
    """Information about the session."""

    model_config = ConfigDict(extra="forbid")
    id: Optional[int] = Field(default=None, description="The session ID.", deprecated=True)
    uuid: Optional[str] = Field(default=None, description="The session's universally unique identifier (UUID). Each session instance generates a session UUID.", deprecated=True)
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    host_id: str = Field(..., description="The user ID of the session host.")
    message_id: str = Field(..., description="The request unique identifier (UUID).")
    inviter_name: str = Field(..., description="The user name of the event's trigger.")
    participant: SessionUserRoomSystemCalloutAcceptedBodyPayloadObjectParticipant = Field(..., description="Information about the invited participant.")


class SessionUserRoomSystemCalloutAcceptedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The account ID of the session host.")
    object: SessionUserRoomSystemCalloutAcceptedBodyPayloadObject = Field(..., description="Information about the session.")


class SessionUserRoomSystemCalloutAcceptedBody(BaseModel):
    """# session.user_room_system_callout_accepted

    The **Invited party answered a session invitation through phone (call out) via Zoom room** event is triggered every time a user [joins a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

    ## Prerequisites

    * **Event Subscriptions** must be enabled for your app with the following configurations:
    * A valid **Event Notification Endpoint URL**.
    * The **Invited party answered a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



    **Event type**: `session.user_room_system_callout_accepted`

    """

    model_config = ConfigDict(extra="forbid")
    event: str = Field(..., description="The name of the event.")
    event_ts: int = Field(..., description="A timestamp at which the event occurred.")
    payload: SessionUserRoomSystemCalloutAcceptedBodyPayload


class SessionUserRoomSystemCalloutAcceptedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionUserRoomSystemCalloutAcceptedBody] = Field(default=None, description="""# session.user_room_system_callout_accepted

The **Invited party answered a session invitation through phone (call out) via Zoom room** event is triggered every time a user [joins a session through phone (call out) from a Zoom room](https://support.zoom.us/hc/en-us/articles/205369035).

## Prerequisites

* **Event Subscriptions** must be enabled for your app with the following configurations:
* A valid **Event Notification Endpoint URL**.
* The **Invited party answered a session invitation through phone (call out) via Zoom room** subscription enabled under the **Session** event.



**Event type**: `session.user_room_system_callout_accepted`
""")


class SessionRecordingStoppedBodyEvent(str, Enum):
    """The event's name."""

    SESSION_RECORDING_STOPPED = "session.recording_stopped"


class SessionRecordingStoppedBodyPayloadObjectRecordingFile(BaseModel):
    recording_start: Optional[str] = Field(default=None, description="The recording start time.")
    recording_end: Optional[str] = Field(default=None, description="The recording end time. Response in general query.")


class SessionRecordingStoppedBodyPayloadObject(BaseModel):
    """Information about the session. This object only returns updated properties."""

    model_config = ConfigDict(extra="forbid")
    session_id: str = Field(..., description="Unique session identifier. Each instance of the session will have its own session_id.")
    session_name: str = Field(..., description="Session name.")
    session_key: str = Field(..., description="The Video SDK custom session ID.")
    start_time: datetime = Field(..., description="The time at which the session started.")
    timezone: str = Field(..., description="The user's timezone.")
    recording_file: SessionRecordingStoppedBodyPayloadObjectRecordingFile


class SessionRecordingStoppedBodyPayload(BaseModel):
    model_config = ConfigDict(extra="forbid")
    account_id: str = Field(..., description="The Video SDK account ID.")
    object: SessionRecordingStoppedBodyPayloadObject = Field(..., description="Information about the session. This object only returns updated properties.")


class SessionRecordingStoppedBody(BaseModel):
    """# session.recording_stopped
    ---

    Event: `session.recording_stopped`

    ## Event description

    The **Session recording stopped** event is triggered every time a previously paused recording of a session is resumed.

    Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

    ## Prerequisites

    * Cloud Recording must be enabled on the Video SDK account.
    * Event Subscriptions must be enabled for your app with the following configurations:
      * A valid Event Notification Endpoint URL.
      * **Session recording stopped** subscription enabled under the **Recording** event.

    The tabs below display the complete schema, payload with data types, and an example of the `session.recording_stopped` event notification:



    **Event type**: `session.recording_stopped`

    """

    model_config = ConfigDict(extra="forbid")
    event: SessionRecordingStoppedBodyEvent = Field(..., description="The event's name.")
    event_ts: int = Field(..., description="A timestamp, in milliseconds since epoch, at which the event occurred.")
    payload: SessionRecordingStoppedBodyPayload


class SessionRecordingStoppedWebhook(BaseModel):
    model_config = ConfigDict(extra="forbid")
    body: Optional[SessionRecordingStoppedBody] = Field(default=None, description="""# session.recording_stopped
---

Event: `session.recording_stopped`

## Event description

The **Session recording stopped** event is triggered every time a previously paused recording of a session is resumed.

Only a session host or co-host can start, pause, resume, or stop a cloud recording. A recording is considered complete **after** the host or co-host ends the session. See [Enable cloud recording](https://developers.zoom.us/docs/video-sdk/account/#enable-cloud-recording) for details.

## Prerequisites

* Cloud Recording must be enabled on the Video SDK account.
* Event Subscriptions must be enabled for your app with the following configurations:
  * A valid Event Notification Endpoint URL.
  * **Session recording stopped** subscription enabled under the **Recording** event.

The tabs below display the complete schema, payload with data types, and an example of the `session.recording_stopped` event notification:



**Event type**: `session.recording_stopped`
""")
