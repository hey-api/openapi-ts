import path from 'node:path';

/**
 * Replicates the outputHeaderToPrefix logic from generate/client.ts for testing.
 */
function outputHeaderToPrefix(header: unknown): string {
  if (header == null || typeof header === 'function') return '';
  const lines =
    typeof header === 'string'
      ? header.split(/\r?\n/)
      : (header as string[]).flatMap((line) => line.split(/\r?\n/));
  const content = lines.join('\n');
  return content ? `${content}\n\n` : '';
}

/**
 * Simulates the isDevMode logic from generate/client.ts for testing.
 * packageJsonDirs is a list of directories that contain a package.json file.
 */
function simulateIsDevMode(dirname: string, packageJsonDirs: string[]): boolean {
  let dir = dirname;
  for (let depth = 0; depth < 20; depth++) {
    if (packageJsonDirs.includes(dir)) {
      const relPath = path.relative(dir, dirname);
      if (!relPath) return false;
      const relParts = relPath.split(path.sep);
      return relParts.length === 2 && relParts[0] === 'src' && relParts[1] === 'generate';
    }
    const parent = path.dirname(dir);
    if (parent === dir) break;
    dir = parent;
  }
  return false;
}

describe('outputHeaderToPrefix logic', () => {
  it('returns default comment for string header', () => {
    const result = outputHeaderToPrefix('// This file is auto-generated by @hey-api/openapi-ts');
    expect(result).toBe('// This file is auto-generated by @hey-api/openapi-ts\n\n');
  });

  it('returns joined lines for array header', () => {
    const result = outputHeaderToPrefix([
      '// This file is auto-generated by @hey-api/openapi-ts',
      '// @ts-nocheck',
    ]);
    expect(result).toBe(
      '// This file is auto-generated by @hey-api/openapi-ts\n// @ts-nocheck\n\n',
    );
  });

  it('returns empty string for null header', () => {
    expect(outputHeaderToPrefix(null)).toBe('');
  });

  it('returns empty string for undefined header', () => {
    expect(outputHeaderToPrefix(undefined)).toBe('');
  });

  it('returns empty string for function header', () => {
    expect(outputHeaderToPrefix(() => '// dynamic')).toBe('');
  });

  it('handles string with embedded newlines', () => {
    const result = outputHeaderToPrefix('// line1\n// line2');
    expect(result).toBe('// line1\n// line2\n\n');
  });
});

describe('isDevMode logic', () => {
  const packageRoot = ['', 'home', 'user', 'packages', 'openapi-ts'].join(path.sep);

  const scenarios: ReadonlyArray<{
    description: string;
    dirname: string;
    expected: boolean;
    packageJsonDirs: string[];
  }> = [
    {
      description: 'returns true in dev mode (src/generate)',
      dirname: ['', 'home', 'user', 'packages', 'openapi-ts', 'src', 'generate'].join(path.sep),
      expected: true,
      packageJsonDirs: [packageRoot],
    },
    {
      description: 'returns false in prod mode (dist/ contains the bundle)',
      dirname: ['', 'home', 'user', 'packages', 'openapi-ts', 'dist'].join(path.sep),
      expected: false,
      packageJsonDirs: [packageRoot],
    },
    {
      description: 'returns false in prod mode (dist/generate)',
      dirname: ['', 'home', 'user', 'packages', 'openapi-ts', 'dist', 'generate'].join(path.sep),
      expected: false,
      packageJsonDirs: [packageRoot],
    },
    {
      description:
        'returns false when parent path contains /src/ but package is in dist (pnpm case from issue)',
      dirname: [
        '',
        'home',
        'user',
        'src',
        'thcdb',
        'worktree',
        'schema-gen',
        'web',
        'node_modules',
        '.pnpm',
        '@hey-api+openapi-ts@0.91.1',
        'node_modules',
        '@hey-api',
        'openapi-ts',
        'dist',
      ].join(path.sep),
      expected: false,
      packageJsonDirs: [
        [
          '',
          'home',
          'user',
          'src',
          'thcdb',
          'worktree',
          'schema-gen',
          'web',
          'node_modules',
          '.pnpm',
          '@hey-api+openapi-ts@0.91.1',
          'node_modules',
          '@hey-api',
          'openapi-ts',
        ].join(path.sep),
      ],
    },
    {
      description:
        'returns false on Arch Linux build path where /dist/ is not present (exact error from issue)',
      dirname: [
        '',
        'build',
        'wrangler',
        'src',
        'workers-sdk-wrangler-4.69.0',
        'node_modules',
        '.pnpm',
        '@hey-api+openapi-ts@0.91.1_typescript@5.8.3',
        'node_modules',
        '@hey-api',
        'openapi-ts',
      ].join(path.sep),
      expected: false,
      packageJsonDirs: [
        [
          '',
          'build',
          'wrangler',
          'src',
          'workers-sdk-wrangler-4.69.0',
          'node_modules',
          '.pnpm',
          '@hey-api+openapi-ts@0.91.1_typescript@5.8.3',
          'node_modules',
          '@hey-api',
          'openapi-ts',
        ].join(path.sep),
      ],
    },
    {
      description:
        'returns false when parent path contains /src/ and no dist exists (Arch Linux bundle path)',
      dirname: [
        '',
        'home',
        'user',
        'src',
        'thcdb',
        'worktree',
        'schema-gen',
        'web',
        'node_modules',
        '.pnpm',
        '@hey-api+openapi-ts@0.91.1_magicast@0.5.1_typescript@5.9.3',
        'node_modules',
        '@hey-api',
        'openapi-ts',
        'plugins',
        '@hey-api',
        'client-core',
        'bundle',
      ].join(path.sep),
      expected: false,
      packageJsonDirs: [
        [
          '',
          'home',
          'user',
          'src',
          'thcdb',
          'worktree',
          'schema-gen',
          'web',
          'node_modules',
          '.pnpm',
          '@hey-api+openapi-ts@0.91.1_magicast@0.5.1_typescript@5.9.3',
          'node_modules',
          '@hey-api',
          'openapi-ts',
        ].join(path.sep),
      ],
    },
    {
      description: 'returns true only when ending with src/generate (even with src in parent path)',
      dirname: [
        '',
        'home',
        'user',
        'src',
        'backup',
        'packages',
        'openapi-ts',
        'src',
        'generate',
      ].join(path.sep),
      expected: true,
      packageJsonDirs: [
        ['', 'home', 'user', 'src', 'backup', 'packages', 'openapi-ts'].join(path.sep),
      ],
    },
    {
      description: 'returns false when not in src/generate (in src/plugins)',
      dirname: ['', 'home', 'user', 'packages', 'openapi-ts', 'src', 'plugins'].join(path.sep),
      expected: false,
      packageJsonDirs: [packageRoot],
    },
    {
      description: 'returns false when package.json not found anywhere',
      dirname: ['', 'home', 'user', 'packages', 'openapi-ts', 'src', 'generate'].join(path.sep),
      expected: false,
      packageJsonDirs: [],
    },
    {
      description: 'returns false when dirname is the same as package root',
      dirname: packageRoot,
      expected: false,
      packageJsonDirs: [packageRoot],
    },
  ];

  it.each(scenarios)('$description', ({ dirname, expected, packageJsonDirs }) => {
    const result = simulateIsDevMode(dirname, packageJsonDirs);
    expect(result).toBe(expected);
  });
});
